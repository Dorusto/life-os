# Code audit — majordom-financiar backend + frontend (2026-09-15)

Audit performed natively via opencode subagents (model: GLM Flash latest), dispatched from the
live session at the user's request. This file is the live progress tracker; consolidated findings
land here as batches complete. Nothing is modified by the audit agents — read-only review only.

## Method

- Scope: `majordom-financiar` backend (`api/`, `core/`, `tools/`, `services/`) + frontend (`src/`)
- Audited against `docs/architecture.md#critical-technical-rules` (rules 1–44) + general correctness/security
- Batch 1 (backend, 3 parallel read-only agents): A1 `api/` routes, A2 `core/` clients+config, A3 chat/tools/services
- Batch 2 (frontend, 2 parallel read-only agents): F1 `lib/`+`components/`, F2 `pages/`+app shell
- Issue filing: only after the user reviews the consolidated report

## Status log

- 2026-09-15 (UTC morning) — audit started; batch 1 dispatched (A1, A2, A3)
- 2026-09-15 (UTC morning) — batch 1 complete: 44 findings (7 HIGH, 19 MEDIUM, 18 LOW); orchestrator spot-checked the top claims (marked ✓ below); batch 2 (frontend) dispatched
- 2026-09-15 (UTC morning) — batch 2 complete: 42 findings (6 HIGH, 14 MEDIUM, 21 LOW + 1 cross-ref); top claims spot-checked ✓. **Audit total: 86 findings — 13 HIGH, 33 MEDIUM, 39 LOW.**

## Batch 1 — backend (A1 `api/`, A2 `core/`, A3 chat/tools/services)

Private-data note: findings involving real personal values (vehicle/account nicknames, a personal
email) are deliberately genericized here per the repo's private-data rule — the file:line
references locate the exact spots without duplicating the values into this tracked doc.

### HIGH

1. [A1] ✓ spot-checked — `backend/api/home.py:143` — in `get_duplicate_pairs()`, `client` is never assigned (assignments jump 110→160; line 110 uses `get_provider()` inline), so `await client.get_categories()` raises NameError on every call; the surrounding `except Exception` swallows it and the duplicate-review page permanently gets `available_categories: []`. Silent-failure class (rules 12/14). Fix: assign `client = get_provider()` in the function.
2. [A1] `backend/api/auth.py:32` — `JWT_SECRET` falls back to a hardcoded known constant with no warning; tokens forgeable if unset. Also a rule-4 violation (`os.environ` reads for JWT_SECRET + USER{i}_*; `jwt_secret` missing from settings entirely). Fix: move to settings singleton; fail fast or log loudly on fallback.
3. [A2] `backend/core/actual_client/client.py:4521-4531` — `get_or_create_payee_id()` creates a payee (flush only) but never `commit()`s; actualpy's `__exit__` rolls the session back, so the payee row is discarded while callers (`api/category_actions.py:294,323`) store the dead id on surviving transactions → broken payee display. Fix: commit before returning.
4. [A2] `backend/core/actual_client/client.py:711` + `finance/actual_budget_provider.py:21-30` — a fresh `ActualBudgetClient` (→ fresh `ThreadPoolExecutor(max_workers=1)`) is constructed per method call, never shut down: one leaked non-daemon thread per finance-touching call, unbounded. Fix: module-level shared executor or `asyncio.to_thread`.
5. [A3] ✓ spot-checked — `backend/api/chat.py` — `finance__get_savings_rate_chart` returns a chart card but is missing from `_PROPOSAL_TOOLS`, so its JSON goes back to the LLM as a tool result instead of reaching the frontend: chart never renders. Fix: add to the set.
6. [A3] `backend/core/ocr/vision_engine.py:121-126`, `core/csv_importer/detector.py:115`, `api/csv_import.py:180`, `services/notification_service.py:113` — ✓ spot-checked (vision_engine) — 4 of 5 LLM call sites nest `temperature`/`num_predict`/`num_ctx` inside `"options": {...}` on the OpenAI-compatible endpoint, where `options` is ignored: vision, CSV detection, categorization and digest actually run at provider-default temperature 1.0 (the #172 fabrication root cause, reintroduced). Fix: hoist temperature/max_tokens to payload top level.
7. [A3] `backend/api/chat.py:110` + `tools/registry.py:331` — system prompt still instructs calling `finance__propose_clarification`, but the tool is disabled/removed from `TOOLS` (dead dispatch + dead `_PROPOSAL_TOOLS` entry): prompt points at a nonexistent tool on every missing-amount request (#160/#166 shape). Fix: remove the prompt bullet or re-enable the tool.

### MEDIUM

8. [A1] 9 confirm endpoints (category_actions.py:429, close_account.py:50, balance_adjustments.py:39, proposals.py:77, notification_actions.py:63, transfer_conversion.py:38, vehicle_log_actions.py:50, vehicle_reminder_actions.py:101, vehicle_status_actions.py:40) — pending proposal deleted in `finally`, so a transient write failure (AB blip) destroys the confirmation card forever; `vehicle_proposals.py:148` does it correctly. Fix: delete only on success/cancel.
9. [A1] transactions.py:224-228/314-318, receipts.py:262-266, csv_import.py:487-490 — failed "create new category" degrades silently: warning logged, execution continues writing a category NAME where an ID is required, or leaves rows uncategorized; user sees a generic error or nothing. Fix: abort with a clear error.
10. [A1] setup.py:246-263 — `/setup/complete` marks setup done even when balance adjustments failed; failures invisible in the response. Fix: surface failures; complete only when applied.
11. [A1] category_actions.py:147-162 — `budget_copy` confirm loop has no per-item error isolation (one failing category 500s after partial writes, then `finally` destroys the proposal); `apply_budget_overview` (553-565) already does isolation. Fix: per-item try/except.
12. [A1] transactions.py:196 + receipts attach path (receipt_service.py:347) — `ConfirmResponse.transaction_id` returns the client-supplied `attach_to` financial_id instead of the row's primary key; `split_transaction` matches on `id` only → "Transaction not found" on split. Fix: resolve the primary key.
13. [A1] budget.py:35-42 — malformed `month` silently falls back to current month (wrong-month budget write). Fix: 400 on unparseable month.
14. [A2] client.py:2395-2405 — `get_home_data()`'s inline unreconciled count uses a weaker filter than the shared `_unreconciled_filter_clauses()` (missing starting_balance_flag, Starting-Balance exclusions, tombstone filter): Home count can exceed the Inbox list (rule-20/34 drift). Fix: count via the shared helper.
15. [A2] client.py:1593 — `add_transaction()` sets a random `imported_id`, contradicting rule 7's SHA-based dedup contract (docstring also stale): exact-dedup between import and manual/receipt paths can never match. Fix: use shared `financial_id()` or record a deliberate rule change.
16. [A2] client.py:2759-2768 + 2204-2213 — transfer legs created with no `imported_id` on either leg (rule-7 gap #102 fixed for CSV imports): overlapping imports can duplicate transfers. Fix: assign dedup ids post-create.
17. [A2] client.py:3447-3520 — `merge_duplicate_transaction()` tombstones the manual side without checking `transferred_id` (rule 31's own mandate); safety depends on caller routing by scan-time tags. Fix: guard inside the method.
18. [A2] `backend/core/finance/fire.py:11` — PRIVATE DATA: real account/vehicle names hardcoded in a tracked file (same leak class as #244), and exclude-by-name is fragile (rename silently changes FIRE numbers). Fix: move to `user_preferences`.
19. [A3] `backend/tools/registry.py:1004,1065,1096,1125,1171` + `tools/finance/vehicle.py:98` — PRIVATE DATA: real vehicle nicknames hardcoded in tool-schema descriptions sent to the LLM on every call (rule 21 was fixed in `_build_system_prompt()` but never in schemas); moto heuristic also hardcodes brand names instead of using `vehicle_type`. Fix: genericize schemas; derive from vehicle_type.
20. [A3] `tools/finance/vehicle.py:82-104` — `ab_cats` assigned inside `try`; AB-down → `NameError` crashes the whole refuel proposal instead of the intended empty-fallback. Fix: initialize before the try.
21. [A3] registry.py:451,772,231 — stale tool descriptions: `delete_category`/`rename_category`/`set_notification_time` claim "executes immediately" while all three return confirmation cards. Fix: update descriptions.
22. [A3] `TransferConversionCard.tsx` + `VehicleStatusCard.tsx` (rule 5) — confirmation cards with ZERO editable fields (target account/vehicle static); payloads don't even carry the lists to power selects. Fix: pass lists, render selects.
23. [A3] `_build_system_prompt()` — missing tool-guide bullets for: `propose_budget_copy`, `delete_category`, `propose_categorize_with_rule` (indirect only), `vehicle__delete_vehicle_log_entry`, `propose_set_vehicle_active`, `get_vehicle_consumption_chart`, `get_vehicle_distance_chart`; `get_budget_status`/`rename_category` appear only as negative mentions (#160/#166/#255 class). Fix: add bullets.
24. [A3] vehicle.py:159-171,213-216,256-258,531-533 — proposal tools return plain-text errors on not-found, which `_PROPOSAL_TOOLS` yields straight to the frontend: LLM never sees the failure, can't recover. Fix: return `{"type":"error"}`.
25. [A3] csv_importer/normalizer.py — per-row parse failures swallowed at debug level; preview exposes total vs kept but no skipped-reasons; `normalize()` is a byte-identical duplicate of `normalize_all()` with a lying docstring. Fix: surface skip counts; deduplicate.
26. [A2] client.py:2091 — `execute_csv_import()` uses actualpy's `get_or_create_payee` instead of the in-file `_safe_get_or_create_payee`: crashes on duplicate payee names (the exact case the helper exists for).
27. [A2] client.py:1291-1306 — `get_budget_pacing_totals()` rebuilds budget history ~9× (once per month) via `_compute_budget_vs_spent()`; runs on Home bell checks (rule 41 pattern). Fix: hoist one history, slice per month.

### LOW (condensed)

28. [A1] transfer_conversion.py:36 — raw exception text leaked to client.
29. [A1] vehicle_accounts_internal.py — internal routes have no shared secret (asymmetric with vehicle-manager's X-Service-Token).
30. [A1] receipts.py/csv_import.py/fuelio_import.py — upload size checks after full read; MIME trusted without magic-byte sniff.
31. [A1] receipts.py:230-304 — grocery confirm never deletes the uploaded image (fuel path does).
32. [A1] proposals.py:79 — dead `duplicate` variable, info lost.
33. [A1] setup.py:134-186 — SSRF surface (authenticated) via ab-test-connection arbitrary URL; reaches into actualpy private attr.
34. [A1] missing response_model on several read endpoints (accounts balance-history, home, transactions split).
35. [A2] finance/provider.py:289 — `os.getenv("FINANCE_BACKEND")` outside config (rule 4).
36. [A2] dead code, zero callers: `get_full_context()` (rule-20-divergent inline copy that would inflate spending if revived), `get_default_account()`, `get_total_balance()`, `update_transaction_category()`.
37. [A2] client.py:4540-4548 — `get_payees()` N+1 with full ORM hydration for counts.
38. [A2] ab_credential_store.py:46-47 — key file 0644 briefly before chmod; MemoryDB re-runs full schema init on every construction (hot paths).
39. [A2] main.py:214 — `/uploads` static mount serves receipt photos with no auth (Tailscale-only exposure mitigates).
40. [A3] ocr/parser.py:74 — `\\d` in raw string: first TOTAL_PATTERNS entry can never match.
41. [A3] push_service.py:19,54 — PRIVATE DATA: real personal email as VAPID_CONTACT in tracked file; VAPID private PEM written without 0600.
42. [A3] notification_service.py:37-59 — sync sqlite3 on the event loop inside async jobs (rule 1).
43. [A3] vision_engine.py:88-105 — EXIF orientation ignored before resize (sideways OCR); parser.py:56 hardcodes "RON".
44. [A2] client.py:3 other minor dead/legacy items per report (see agent log).

### Verified clean (batch 1)

- No route shadowing anywhere (~89 routes / 28 modules + registration order checked) — the W35 shadowed-`GET /accounts` class is currently absent.
- Auth coverage complete except documented public endpoints (+ finding 29's internal routes).
- Provider wiring: `check_provider_wiring.py` PASS (76 methods, 3 layers); `check_silent_exceptions.py` PASS; no `# silent-ok` abuse.
- actualpy discipline: download→ops→commit correct everywhere except finding 3; EUR floats everywhere; no double downloads.
- No merchant_mappings rebirth; rules-engine-first categorization intact (rules 27/28); memory DB parameterized throughout.
- Chat history filter (rule 43) matches the documented mechanical fix exactly; `json.loads` discipline (rule 7) correct at the single consumer; `/v1` handling correct; `think:false` present.

### Batch 2 — frontend (F1 `lib/`+`components/`, F2 `pages/`+shell)

Private-data note: placeholders/user-visible strings containing a real location and a real first
name were found (rule 21); genericized here, file:line locates them.

### HIGH (frontend)

45. [F1] ✓ spot-checked — `components/CsvImportCard.tsx:73,84 vs 104+` — rules-of-hooks violation: two early returns (`status === 'loading'/'error'`) precede the 7 `useState` calls; Chat mounts the card as `loading` then updates the same fiber to `ready` → hook count changes → React error #310, Chat page crash. Fix: hooks above early returns (or key by status).
46. [F1] ✓ spot-checked — `pages/AddButton.tsx:28` + `pages/ReceiptFlow.tsx:86` — the Add sheet's "Photo — scan a receipt" flow is dead: `sessionStorage.pendingReceiptDataUrl` is only ever READ/REMOVED, never set (its setter page no longer exists) — the button bounces back to `/`. Fix: restore the handoff or repoint at the chat upload path.
47. [F1] `components/FuelReceiptCard.tsx:380` + `receipt_service.py:141-158` — photo mode sends a category UUID where the backend expects the display NAME; works only on first paint (the service stuffs a name into `suggested_category_id`); picking any other category from the dropdown creates a bogus AB category. Fix: send name (or make backend accept ids).
48. [F2] `App.tsx:60-72` + `AbSetupWizard.tsx:100-108` — completing the AB wizard within 60s bounces the user back into it: the gate's `['setup-status','ab-connected-gate']` query (staleTime 60s) is never invalidated after a successful save → cached `ab_connected: false` loops until the cache ages out. Fix: invalidate the query on save.
49. [F2] `lib/api.ts:697-709` + `pages/Chat.tsx:867-1012` — chat stream framing is TCP-fragile: card JSON forwarded as raw chunks with no buffering; a card split across network reads fails JSON.parse and renders as visible raw JSON, card lost. Also no AbortController. Fix: buffer per top-level JSON object; add AbortSignal.
50. [F2] `pages/Transactions.tsx:600,613` — bulk-selection bar and bulk notice are `fixed bottom-0 z-40` directly under BottomNav (`z-50`): the primary bulk action is covered/untappable on mobile. Dashboard already uses the correct `bottom-16 lg:bottom-0` pattern. Fix: same offset.
51. [F2 cross-ref, fix belongs in lib] `lib/auth.ts` `isAuthenticated()` — decodes the JWT payload with plain `atob()`: JWT segments are base64url, so any payload containing `-`/`_` throws → caught → returns false → spurious logout. Fix: base64url-safe decode.

### MEDIUM (frontend)

52. [F1] `Chart.tsx:163-168` — `useChartRefetch` never checks `res.ok`: a 500 JSON body sets undefined state → next render crashes on `.slice()`.
53. [F1] Confirm-card API-failure pattern — 15+ cards (`ProposalCard.tsx:62`, `GoalProposalCard.tsx:35`, `CategoryActionCard.tsx:106`, `AccountTransferCard.tsx:47`, `BudgetRebalanceCard.tsx:49`, `CloseAccountCard.tsx:23`, `BalanceAdjustmentCard.tsx:20`, `TransferConversionCard.tsx:20`, `BudgetOverviewCard.tsx:50`, `CategoryOverviewCard.tsx:159`, `ReachedGoalsCard.tsx:41`, `VehicleReminderCard.tsx:46`, VehicleLogAction/VehicleStatus/NotificationTime) all `catch → onConfirmed("Error: …")` → card replaced with status text, every user edit lost, no retry — pairs with backend finding 8. Correct inline-error pattern exists in `SetupBalancesCard.tsx:74`/`IncomeSourceCard.tsx:48`. Fix: adopt it.
54. [F1] PRIVATE DATA — `FuelReceiptCard.tsx:328` placeholder contains a specific real-world location; `IncomeSourceCard.tsx:96` contains a real first name. Fix: genericize.
55. [F1] Dead code, zero importers (grep-verified): `ui/Modal.tsx` (unportaled `fixed inset-0` — a revived rule-44 trap), `ui/Button.tsx`, `ui/Delta.tsx`, `ui/Feedback.tsx`, `ui/Form.tsx`, `ui/MetricTile.tsx`, `ui/Pill.tsx`, `ui/Segmented.tsx`, root `components/Card.tsx` (live one is `ui/Card.tsx`), `lib/ui.ts` `seriesColor`, `api.ts` non-streaming `sendChatMessage`, junk `refetch?: any` field.
56. [F1] `CategoryActionCard.tsx:66-100` — `parseFloat(x) || fallback`: typing `0` silently becomes the proposed amount (€0 budget/goal impossible from the card); cleared FIRE fields send NaN→null.
57. [F1] `lib/auth.ts:88-90` — ANY 200 response clears the AB-down flag (e.g. chat streaming succeeds while AB tools fail) → #254 banner false-clears and flickers. Fix: only clear from AB-touching responses.
58. [F1] `lib/vehicleValueApi.ts:10-22` — verbatim copy of api.ts's request plumbing (second occurrence → extract).
59. [F2] `pages/Chat.tsx:957-987` — `saveChatHistory()` called inside `setMessages()` updaters (impure; double-invoked under StrictMode → duplicate persisted entries).
60. [F2] `App.tsx:113-117` — once ANY `chart`/`transaction_list` message exists, `loadChatHistory()` is permanently skipped for the session (they're read-only/persisted) → push-triggered history reload (rule 17's purpose) never runs again until full reload.
61. [F2] 4 review pages (`UncategorizedReviewPage.tsx:28`, `UnreconciledReviewPage.tsx:26`, `BudgetRealismReviewPage.tsx:27`, `RecurringReviewPage.tsx:26`) — query errors render as the SUCCESS empty state ("all clear 🎉") while items are pending. Fix: `isError` branch (Analytics already does it).
62. [F2] `DuplicatesReviewPage.tsx:60-82` — confirm failure swallowed (`catch {}`), cancel has no catch: a failed merge looks like a dead button.
63. [F2] `pages/ImportPage.tsx:180` — rule 42 violation: `min-h-dvh` wrapper (the one page that missed the fix); inner preview's `overflow-y-auto`/sticky thead never engage properly.
64. [F2] Cross-page staleness after writes — DuplicatesReviewPage merge invalidates only `['duplicates','months']`; ReceiptFlow misses `['home']`/`['account-list']`; Transactions `applyBulk` invalidates nothing; Settings sync misses `['transactions','latest']`. Fix: standard invalidation set from every write path.
65. [F2] `pages/Transactions.tsx:330-332` — load-more/filter errors silent when rows already visible.

### LOW (frontend, condensed)

66. [F1] `BalanceAdjustmentCard.tsx:38-50` — only confirm card with zero editable fields (`real_balance` static; rule 5 spirit).
67. [F1] `CategoryActionCard.tsx:146` + `CsvImportCard.tsx:389` — inline `€…toFixed(2)` formatting bypassing formatCurrency (#211 class).
68. [F1] `FuelReceiptCard.tsx:97` — `account_id: accountId || 'dummy'` magic-string fallback sent to backend.
69. [F1] `FuelReceiptCard.tsx:147-160` — confirm catch discards the error object: zero error detail for the user.
70. [F1] `NewGoalSheet.tsx:77-92` — `JSON.parse` per streaming chunk; split payload silently drops the goal proposal flow.
71. [F1] `CsvImportCard.tsx:138-141` — auto-select via `setTimeout` during render instead of `useEffect`.
72. [F1] `lib/groupByMonth.ts:26` — `new Date('YYYY-MM-DD')` UTC parse read in local tz (latent, west-of-UTC only).
73. [F1] Two near-identical merchant tokenizers (`lib/csvImportUtils.ts` vs `CsvImportCard.tsx:44-53`) — extract one.
74. [F2] `Dashboard.tsx:128-135` — Categories Watchlist widget silently disappears if `budget-period` errors (neighboring widget has an explicit error branch).
75. [F2] `pages/Accounts.tsx:38,62-65` — loading/error renders a big "Total €0" (#234 class).
76. [F2] `AccountDetail.tsx:208-209` — Transactions tab flashes "No transactions yet" while loading.
77. [F2] `ReceiptFlow.tsx:109,153` — default date uses UTC date; receipts confirmed 00:00–03:00 local default to yesterday.
78. [F2] `Chat.tsx:1055-1059` — `handleClearHistory` has no error handling/feedback.
79. [F2] `Settings.tsx:519-541` — ConnectionsPage hardcodes "Connected" for all 3 services with no health check + hardcoded ports.
80. [F2] `Settings.tsx:756` — AboutPage "Version 2026.08.28" hardcoded, stale.
81. [F2] `Settings.tsx:568-691` — BudgetPacingPage: no error state for the config query.
82. [F2] `Dashboard.tsx:469-477 vs 593-596` — Trend and NetWorth widgets fetch identical data under different query keys (duplicate requests).
83. [F2] `ImportPage.tsx:429` — preview shows raw `MM-DD` (year dropped) instead of formatDate (#237 class).
84. [F2] `Dashboard.tsx:753-760` — permanent "coming soon" CashFlow widget occupying a slot; unused `periodLabel` prop.
85. [F2] `AccountDetail.tsx:131` — wrong error message ("Failed to update category" for an account-type change).
86. [F2] `App.tsx:225` + `ImportPage.tsx:54-57` — dead `initialFile`/`onDone` props from the old chat-embedded flow.

### Verified clean (batch 2)

- Auth transport: every `/api` call goes through `authFetch()`; token key `majordom_token` only in auth.ts; single 401 path; zero raw fetch/EventSource bypasses.
- Rule 44 in frontend: only BottomNav + AbConnectionBanner are fixed, both at App root outside blur/transform ancestors; BottomSheet portals; all header-subtree overlays go through it; chart tooltips are absolute. Dead `ui/Modal.tsx` flagged (56 would re-trap if revived).
- Rule 42: correct on all pages except ImportPage (finding 63); Login/ReceiptFlow/Wizard use the rule's explicit min-h-dvh carve-out.
- #208 retry:false fix still in place; no component-scope query needs another override; queryKeys consistent across bell + review pages; #256 duplicate chart implementation is gone; frontend-shared generated files byte-identical; `tsc --noEmit` clean.
- Chat: all 19 backend card roles have renderers; unknown types fall back to text; rule 43 history filter matches the documented fix.
- #232 quirk fixed (drill-down dates honored); #237 formatDate consistency mostly held (exceptions at 83); rule 28 null-financial_id skip present.
- Private data in pages/: none beyond finding 54 (merchant examples are generic brands).
