# Week 2026-W22 (May 30–31)

---

## 2026-05-30 — Transfer detection fail + CSV profile corruption

### Resolved
- Transfer detection via CSV fields disabled — `col_transfer_indicator=""` (was producing corrupt profiles)
- Transfers excluded manually in preview

### Files modified
- `backend/core/csv_importer/builtin_profiles.py` — transfer detection disabled

### Lessons
- Don't implement transfer detection via CSV fields — profiles are fragile and hard to test correctly.
- ING puts IBAN in description for ALL transactions (not just own-account transfers) — IBAN regex always produces false positives.
- `bank2ynab` is a CLI tool, not a Python library — can't be imported, profiles were copied manually.
- **Correct solution (implemented 2026-05-31):** `Code=GT` column in ING CSV — Geldtransfer flag.
- Always verify SQLite after seed: `docker exec ... sqlite3 /app/data/memory.db` — what's in file vs what's in DB can differ if container wasn't restarted.

---

## 2026-05-30 — Ollama corrupts CSV profiles + SmartCategorizer stale data

### Resolved
- Anti-corruption rule: if a confirmed profile exists for `source_name`, Ollama-detected profiles for the same bank are NOT saved
- Stale SmartCategorizer mappings deleted manually

### Files modified
- `backend/api/csv_import.py` — anti-corruption check before saving Ollama-detected profiles
- SQLite `merchant_mappings` — manual cleanup via Docker exec

### Lessons
- Ollama can save wrong profiles when delimiter is misdetected (European amounts have more commas than periods in early rows → wrong delimiter → weird column names → Ollama generates wrong mapping).
- SmartCategorizer stale mappings: if results are unexpected, delete with `DELETE FROM merchant_mappings` and let it re-learn from fresh imports.
- `OLLAMA_CATEGORIZE_MODEL` config: separate model for CSV categorization — useful to use a smaller model without affecting chat quality.

---

## 2026-05-30 — Setup flow inline + decimal_to_cents bug + balance adjustment

### Resolved
- `SetupBalancesModal` → `SetupBalancesCard` (inline in chat, same styling as ProposalCard)
- `decimal_to_cents` bug fixed: `adjust_account_balance()` was passing cents to `create_transaction()` which multiplies by 100 again
- `propose_balance_adjustment` tool: LLM finds account by name, calculates diff, returns editable card

### Files modified
- `frontend/src/components/SetupBalancesCard.tsx` — new inline card
- `backend/tools/finance/actual_budget.py` — `adjust_account_balance()` fix (EUR not cents)
- `backend/api/chat.py` — `propose_balance_adjustment` added to `_PROPOSAL_TOOLS`

### Lessons
- `create_transaction()` in actualpy receives **EUR** (float), not cents. If you calculate in cents and pass cents, the result is ×100 wrong.
- Any new `propose_*` tool → **add to `_PROPOSAL_TOOLS` immediately** in `backend/api/chat.py`. Without it, the JSON goes to LLM not frontend — card never appears.
- UI in chat = inline card, NOT modal overlay. Everything that appears as assistant response must be styled like ProposalCard.
- Balance adjustment card intentionally has no category — AB shows "needs category", user decides what it is.

---

## 2026-05-30 — Web Push VAPID keys — file not JSON string

### Resolved
- VAPID private key stored as file `vapid_private.pem` in `/app/data/`
- `pywebpush` receives file path, not PEM string

### Files modified
- `backend/core/memory/push_service.py` — key generation and loading

### Lessons
- VAPID private key → store as PEM file, NOT as string in JSON. Any string conversion risks silent corruption.
- When changing VAPID keys: must also delete subscriptions from DB AND revoke permission in browser (old subscriptions don't work with new keys).
- `APScheduler==3.10.4` — asyncio support is built-in, no `APScheduler[asyncio]` extra needed.
- Brave on Android blocks push notifications automatically even with valid HTTPS. Use Chrome for Majordom on phone.
- Firefox: to delete an old push subscription → `about:preferences#privacy` → Permissions → Notifications → remove site.

---

## 2026-05-30 — M2.1: APScheduler daily digest + Web Push

### Resolved
- `AsyncIOScheduler` started in FastAPI lifespan
- `notification_rules`, `notification_log`, `push_subscriptions` tables in SQLite
- VAPID keys generated at first start
- `run_daily_summary()` at 20:00: fetch AB data → Ollama → Web Push
- `set_notification_time` chat tool: reschedules job live without restart

### Files modified
- `backend/main.py` — APScheduler in lifespan
- `backend/core/memory/scheduler.py` — AsyncIOScheduler singleton
- `backend/services/notifications.py` — run_daily_summary
- `backend/api/push.py` — push subscription endpoint
- `backend/tools/registry.py` — set_notification_time tool

### Lessons
- `actualpy` queries: `from actual.queries import get_transactions; get_transactions(actual.session, ...)` — no `actual.get_transactions()` method.
- `scheduler.reschedule_job("job_id", trigger="cron", hour=H, minute=M)` — atomic rescheduling, better than remove+add.
- Scheduler singleton is process-scoped — testing via `docker exec` creates a new process with empty scheduler. Test via HTTP.

---

## 2026-05-30 — Tailscale HTTPS for Web Push on mobile

### Resolved
- Web Push works on Chrome Android via Tailscale Serve
- Proxmox LXC configured with TUN device

### Files modified
- `/etc/pve/lxc/<ID>.conf` — TUN device for LXC
- Tailscale configured on LXC

### Lessons
- Web Push requires HTTPS on mobile — no exceptions, not even for local IP.
- `tailscale serve --bg http://localhost:3000` → automatic Let's Encrypt (CT-logged) → Chrome accepts.
- `tailscale cert` generates Tailscale-signed cert (NOT CT-logged) — browsers reject. Use `tailscale serve` instead.
- For Tailscale in unprivileged LXC: add `lxc.cgroup2.devices.allow: c 10:200 rwm` and `lxc.mount.entry: /dev/net/tun ...` to `/etc/pve/lxc/<ID>.conf`.

---

## 2026-05-30 — CSV import moved to chat + IncomeSourceCard

### Resolved
- Import tab removed from BottomNav
- CSV import via `+` button in chat input → `CsvImportCard` inline
- `IncomeSourceCard`: name unknown income payees as Income or Transfer from account
- SmartCategorizer: never learns "Other", amounts >€50 always get "?" badge

### Files modified
- `frontend/src/components/Chat.tsx` — file picker + CsvImportCard inline
- `frontend/src/components/IncomeSourceCard.tsx` — new component
- `backend/api/csv_import.py` — unknown income rows, SmartCategorizer rules

### Lessons
- Tailwind classes with numbers (`w-52`, `z-50`) trigger pre-commit hook as plate numbers. Workaround: `w-[208px]`, `z-[50]`.
- "Other" is never a real category — don't memorize it, don't propagate it.
- IncomeSourceCard accounts dropdown: On budget / Off budget separated with `<optgroup>`.
- `update_uncategorized_by_payee`: uses ilike join Transactions → Payees. If actualpy join fails, fallback to fetch all + filter in Python.

---

## 2026-05-31 — ING transfer detection: Code=GT

### Resolved
- `col_transfer_indicator: "Code"`, `transfer_indicator_value: "GT"` in all 3 ING built-in profiles
- `seed_builtin_profiles()` UPSERT at startup — overwrites any old Ollama-detected profiles

### Files modified
- `backend/core/csv_importer/builtin_profiles.py` — Code=GT detection for ING

### Lessons
- **Golden rule for transfer detection:** use the bank's native indicator in CSV (ING: `Code=GT`), not string matching in descriptions.
- Descriptions are for humans — they're inconsistent. CSV codes are for machines — they're reliable.

---

## 2026-05-31 — Three React bugs in CsvImportCard

### Resolved
- `<option value="" disabled>` separator causing blank selected value — removed
- "← back to category" `onChange` not firing — replaced with `×` button that calls handler directly
- `setState` in render body causing infinite loop in `ReceiptCard` — moved to `useEffect`

### Files modified
- `frontend/src/components/CsvImportCard.tsx`
- `frontend/src/components/ReceiptCard.tsx`

### Lessons
- `onChange` on `<select>` doesn't fire if `value` doesn't change. For a "reset" button, use a separate element (button, `×`), not an `<option>` with the same current value.
- Never `setState` in render function body — state mutations on mount/update always go in `useEffect` with correct dependencies.

---

## 2026-05-31 — Receipt inline in chat + SQLite cleanup

### Resolved
- Receipt flow moved from `/receipt` page to inline chat card (no `sessionStorage`, no navigation)
- `IncomeSourceCard` + transfer row overlap fixed: `transfer_to_account_id` check added
- `budget_limits` and `transactions` tables dropped from SQLite (orphaned, 0 rows, not in schema)

### Files modified
- `frontend/src/components/Chat.tsx` — `handleReceiptFile()` → inline receipt message
- `backend/api/csv_import.py` — `unknown_income_rows` filter

### Lessons
- Tables can exist in DB without being in `CREATE TABLE` schema — `DROP TABLE` via Docker exec when owned by root in container.
- Any row processed as a transfer must be explicitly excluded from ALL other flows (IncomeSourceCard, categorization). Backend doesn't automatically know a row was "consumed" by another logic.

---

## 2026-05-31 — M2.4 import nudge + M2.3 pending review

### Resolved
- `import_nudge`: checks `notification_log` for last `csv_import` entry — push if >7 days without import
- `pending_review` table: tracks LLM-suggested (not history-confirmed) categorizations
- Nudge after 48h for unreviewed rows

### Files modified
- `backend/core/memory/database.py` — `pending_review` table + CRUD
- `backend/services/notifications.py` — `run_import_nudge()`, `run_pending_review_nudge()`
- `backend/api/csv_import.py` — write to `pending_review` on confirm

### Lessons
- `notification_log` is sufficient for import_nudge — no extra table, just the timestamp of last `csv_import` entry.
- `pending_review` needs its own table — you need to know WHICH transactions are pending, not just WHEN the last event happened.
- `INSERT OR IGNORE` on `pending_review` — if user re-imports the same CSV, no duplicates. `financial_id` is PRIMARY KEY.
- `category_confirmed` field must be in both `ImportRowPreview` and `ImportRowConfirm` payloads — backend needs to know which rows are from history vs LLM.

---

## 2026-05-31 — Home redesign: Cashflow + Net Worth + Goals

### Resolved
- Home screen: MetricCard(Cashflow) + MetricCard(Net Worth) + Goals section + Budget bars
- `get_monthly_stats()` now calculates income (positive transactions without `transferred_id`)
- Goals: `TARGET: N` and `DEADLINE: YYYY-MM` in AB account `notes` field
- `set_account_goal` chat tool writes TARGET/DEADLINE to account notes

### Files modified
- `backend/core/actual_client/client.py` — `get_monthly_stats()` income calculation, `get_goals()`
- `backend/api/transactions.py` — `/api/accounts/goals` endpoint
- `backend/tools/finance/actual_budget.py` — `set_account_goal` tool
- `frontend/src/pages/Home.tsx` — full redesign

### Lessons
- `acc.notes` in actualpy: account notes field can be read/written directly. `KEY: value` pattern in notes is a simple way to store metadata without extra tables.
- `re.sub()` vs append: when updating a structured note field, use `re.sub()` to replace the existing value, not append again.
- Cashflow: positive transactions without `transferred_id` = income. Those with `transferred_id` = internal transfer halves (not real income).
- `set_account_goal` executes directly (no card) — it's a preference setting, not a financial transaction. Exception to the confirmation card rule.

---

## 2026-05-31 — Category groups in BudgetDashboard

### Resolved
- Budget bars grouped by top-level AB group (Housing, Daily Living, etc.)
- `cat.group.name` from actualpy exposed in `get_budget_status()` response
- Tap on group → expand subcategories inline
- Custom groups (not in 7 standard) appear after the standard ones

### Files modified
- `backend/core/actual_client/client.py` — `cat_group_map` in `get_budget_status()`
- `backend/api/transactions.py` — `group_name` in `BudgetCategory` model
- `frontend/src/components/BudgetDashboard.tsx` — grouped bars with expand
- `backend/api/setup.py` — `_ensure_default_categories()` creates 7 groups at first setup

### Lessons
- `cat.group.name` in actualpy: group is available directly on the category object from `get_categories()`. No extra query needed.
- Unknown groups at end, not errors: when AB data doesn't match standard nomenclature (user can rename groups), don't block display — show known groups in fixed order, rest at end.
- Pre-commit hook: `settings\.` as safe prefix — any value starting with `settings.` is a config reference, not a credential.

---

## 2026-05-31 — Category management + goal deadline + confirmation card rule

### Resolved
- `rename_category`, `delete_category`, `create_category` tools + confirmation cards
- `set_account_goal` updated: supports `DEADLINE: YYYY-MM` in notes
- Home Goals show "by Mon YYYY" + "€X/mo needed"
- Emoji fallback for custom group names: keyword matching (Savings→💰, Food→🛒)
- `setup_default_groups` tool: creates 7 standard groups, skips existing ones

### Files modified
- `backend/tools/finance/actual_budget.py` — rename/delete/create category tools
- `backend/tools/category_actions.py` — in-memory store for category proposals
- `backend/api/category_actions.py` — confirm/cancel endpoints
- `backend/api/chat.py` — all new tools added to `_PROPOSAL_TOOLS`
- `frontend/src/components/CategoryActionCard.tsx` — new component
- `frontend/src/pages/Home.tsx` — goal deadline display

### Lessons
- `_PROPOSAL_TOOLS` in `chat.py`: any new write tool must be added here. If missing, JSON goes to LLM not frontend.
- Override pattern in confirm endpoint: store LLM values, accept override in body. Frontend sends user-edited values, backend uses override if present, falls back to stored value.
- `input type="month"` for deadline: native `YYYY-MM` format, calendar picker on Android/Chrome without library.
- Python closure bug in generators: `except HTTPException as e: async def gen(): yield e.detail` → `e` doesn't exist when generator runs. Capture value first: `detail = e.detail; async def gen(): yield detail`.

---

## 2026-05-31 — Migration to LXC Proxmox: 5 bugs in chain

### Resolved
- `actual-budget` started in Docker Compose network (was running outside)
- Tailscale port conflict resolved: `127.0.0.1:5006:5006` instead of `0.0.0.0:5006`
- `httpx.ReadTimeout` caught (was causing NetworkError in frontend)
- `num_ctx: 8192` set in Ollama payload (was overflowing with 20+ tool schemas)
- `docker compose up -d --force-recreate ollama` to reload correct model from env

### Files modified
- `docker-compose.yml` — port binding fix
- `backend/services/chat_service.py` — `num_ctx: 8192`, ReadTimeout catch

### Lessons
- `docker compose restart` ≠ `docker compose up -d`: restart doesn't re-read `.env`. Use `up -d` to apply env changes.
- Calculate tokens before setting `num_ctx`: 20 tool schemas (~3500 tokens) + system prompt + message = easily >4096. Minimum with 20+ tools: 8192.
- `docker compose up -d --force-recreate <service>`: forces complete container recreation, re-reads env vars, loads new model.
- qwen3.5:9b: multimodal (vision + tools + thinking), ~4 min on CPU-only LXC. Acceptable compromise until GPU or cloud.

---

## 2026-05-31 — M4.6 vehicle reminders + notification bundling

### Resolved
- Single `run_daily_digest()` job bundles all alerts (was 4 separate push notifications)
- Vehicle reminders: APK/insurance expiry (30 days), service by km (2000 km), service by date
- `get_vehicle_stats` extended with full profile (plate, make/model, APK/insurance due, service interval)
- `VehicleReminderCard`: different UI for date-based vs km-based reminders

### Files modified
- `backend/services/notifications.py` — bundling into run_daily_digest
- `backend/tools/finance/vehicle.py` — get_vehicle_stats extended
- `frontend/src/components/VehicleReminderCard.tsx` — new component

### Lessons
- One push per day, not per alert — correct design for daily digest. Multiple pushes = user disables notifications.
- Checker functions = pure (return text or None, no side effects). Orchestrator decides what/when/how to send.
- Push subscription Firefox vs Chrome Android: subscriptions are per-browser. Notifications only reach the subscribed browser.
- For manual testing of digest: delete anti-spam entry from `notification_log`. Anti-spam correctly blocks second send same day.

---

## 2026-05-31 — M3.6 vehicle log management via chat

### Resolved
- `get_vehicle_log(vehicle_name, limit)` tool — returns text with visible IDs
- `delete_vehicle_log_entry(entry_id)` tool — confirmation card → DELETE from SQLite

### Files modified
- `backend/tools/finance/vehicle.py` — two new tools
- `backend/tools/vehicle_log_actions.py` — in-memory store
- `backend/api/vehicle_log_actions.py` — confirm/cancel endpoints
- `frontend/src/components/VehicleLogActionCard.tsx` — new component

### Lessons
- `financial_id` in `vehicle_log`: present for refuels recorded through Majordom (photo/text), NULL for Fuelio imports. Complete delete (SQLite + AB) must check this field. Issue #83 tracks this.
- Confirmation cards without editable fields: not all cards need editable fields. `VehicleLogActionCard` is simple — show details + Delete/Cancel.
- Claude direct vs DeepSeek: tasks with >3 tightly coupled files and codebase conventions → Claude. Verification costs more than direct implementation.

---

## 2026-05-31 — M3.3 FuelReceiptCard unified + log_refuel endpoint

### Resolved
- `FuelReceiptCard` unified: single component for both photo and text input (removed `FuelLogData` + `isTextMode`)
- `log_refuel` tool returns `ReceiptDraft`-compatible JSON
- `last_odo` read BEFORE insert (not after) — critical for km_since_last calculation
- Auth in fetch: `getToken()` from `auth.ts`, not `localStorage.getItem`

### Files modified
- `frontend/src/components/FuelReceiptCard.tsx` — unified component
- `backend/tools/finance/vehicle.py` — `log_refuel` tool
- `backend/api/vehicle_proposals.py` — confirm endpoint with correct operation order

### Lessons
- **Last ODO must be read BEFORE inserting the new entry.** If read after insert, `get_last_fuel_entry()` returns the just-inserted entry → `km_since_last = 0`.
- DeepSeek for frontend refactors: always include in prompt: "use `getToken()` from `../lib/auth`, not `localStorage.getItem`". The key is `'majordom_token'`.
- `_PROPOSAL_TOOLS` must contain `"log_refuel"` — if missing, the JSON card goes to the LLM instead of the frontend.
- `docker compose restart` doesn't rebuild — always `docker compose up -d --build` to apply code changes.
