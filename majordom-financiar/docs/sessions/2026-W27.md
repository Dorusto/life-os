# Week 2026-W27 (June 29 – July 5)

---

## 2026-07-02 — Vault findings triage + CSV import cleared/imported_id fix

### Resolved
- Migrated ~30 bug/feature findings from the Second Brain vault (2026-07-01/02 sessions) into GitHub issues #101–#131, with `docs/roadmap.md` backlog entries and `docs/architecture.md` gotchas (goal template syntax, Starting Balances income artifact)
- **Issue #101:** `_do_import()` never set `cleared` on `create_transaction()` — every CSV-imported transaction stayed permanently unreconciled. Fix: `cleared=True` added, matching the `client.py:1075` pattern.
- **Issue #102:** `create_transfer()` from actualpy has no `imported_id`/`cleared` parameters at all (confirmed via `inspect.signature`) — the transfer branch of `_do_import()` never set them, so `existing_tx_map` never saw transfer transactions on a later import → duplicate transfers on any overlapping reimport. Fix: set `financial_id`/`cleared` directly on both `Transactions` objects returned by `create_transfer()`, using distinct hashes per leg (positive/negative amount) to avoid collision in the dedup dict.

### Verified live (local test AB instance, no real data)
- Rebuilt `majordom-api`, imported `tests/CSV/test_ing_2026-05.csv` into the `test` account → all 20 transactions confirmed `cleared=1` in AB (direct actualpy query)
- Reimported `tests/CSV/test_ing_2026-05_overlap.csv` → correct dedup on the 6 overlapping rows (`duplicate=True`), 14 new/unique rows unaffected
- Simulated a transfer reimport (same synthetic row confirmed twice, `transfer_to_account_id` set): 1st confirm → `imported:1`; 2nd confirm (reimport) → `imported:0, skipped:1` (previously would have created a duplicate). Confirmed in AB: exactly 2 transfer legs (not 4), both `cleared=1`, distinct `financial_id`, correctly cross-referenced via `transferred_id`

### Files modified
- `backend/api/csv_import.py` — `cleared=True` on normal transaction path; `financial_id`/`cleared` set on both transfer legs
- `docs/architecture.md` — 2 new critical rules (goal template syntax, Starting Balances artifact) + `imported_id`/`cleared` note under rule 7
- `docs/roadmap.md` — 2 new Backlog sections ("Proactive budget intelligence", "Import & reconciliation robustness") + updated 3 existing rows with issue links
- Second Brain vault `CLAUDE.md` — detailed Roadmap section (48 lines) replaced with a pointer to the repo/issues (vault syncs itself, not committed from here)

### Lessons
- `create_transfer()` in actualpy does NOT accept `imported_id` or `cleared` — unlike `create_transaction()`. Any transfer-creation path needing dedup or reconciliation must set these directly on the returned `Transactions` objects (both legs have real, settable `financial_id`/`cleared` columns).
- Existing seeded test data in the dev AB instance already showed the exact bug pattern (transfer pairs with `cleared=0`, `financial_id=None`) — useful as an unplanned before/after reference.

### Unresolved
- Remaining 26 issues from the vault triage (#104–#131) — not started

## 2026-07-02 — Issue #103: CSV import currency conversion (crypto.com)

### Resolved
- **Issue #103** — turned out not to need real FX conversion: crypto.com's export already provides a "Native Amount"/"Native Currency" column pair, already converted to the account's home currency (EUR). The saved profile (`col_amount: "Amount"`) was pointing at the raw foreign-currency column instead. Real case confirmed: Porkbun.com charged 9.78 EUR, but "Amount" (USD) showed 11.08 — "Native Amount" correctly shows -9.78.
- `CsvProfileDetector`'s LLM prompt (`_DETECT_PROMPT` in `detector.py`) now explicitly instructs: when both a foreign-currency amount column and an already-converted native/home-currency amount column exist, always prefer the converted one for `col_amount` and leave `col_currency` empty.
- Corrected the already-saved crypto.com profile in `memory.db` directly (`col_amount` → `"Native Amount"`) so the fix applies immediately without waiting for LLM re-detection.

### Verified live
- Reimported the real crypto.com export (`tests/CSV/card_transactions_record_20260702_182738.csv`) — Porkbun row now previews at 9.78 EUR (was 11.08)
- Reimported a Revolut export with a RON-denominated transaction settled directly in EUR (`tests/CSV/account-statement_2026-06-01_2026-07-02_en-gb_7c0627.csv`) — unaffected, still correct (Revolut has no native/foreign column pair, single `Amount`+`Currency`, both already EUR)

### Files modified
- `backend/core/csv_importer/detector.py` — `_DETECT_PROMPT` updated with native-currency-column preference rule

### Lessons
- Not every "currency conversion" bug needs an FX rate integration — some bank/card exports already provide a pre-converted amount column, the bug is picking the wrong one. Worth checking the raw CSV headers before assuming conversion logic is needed.
- The already-saved (wrong) CSV profile in SQLite persists independently of the LLM prompt fix — a stale profile needs a direct one-off correction, the prompt fix only prevents the same mistake on brand-new formats.

### Unresolved
- General case (a CSV with only a foreign-currency amount + currency code, no converted equivalent — e.g. a hypothetical Revolut transaction actually settled in USD) still has no conversion path. Would need a live/historical FX rate source. Deferred — no concrete case found yet.
- Remaining 26 issues from the vault triage (#104–#131) — not started

## 2026-07-02 — Issue #118: CSV import account auto-suggestion

### Resolved
- Found while re-testing #103 against the real crypto.com export: the account selector always showed "No account matched" for "crypto.com" vs. account "Crypto card" — existing matching logic (`ImportPage.tsx` + `CsvImportCard.tsx`, duplicated identically in both) was plain substring matching (`a.includes(b) || b.includes(a)`), which fails whenever neither string fully contains the other.
- Replaced with word-token overlap matching (`matchAccountBySource()`, new shared `frontend/src/lib/csvImportUtils.ts`): splits both strings on non-alphanumeric characters, keeps tokens ≥3 chars, matches if any token overlaps. Catches "crypto.com" → "Crypto card" via the shared "crypto" token.
- Extracted into a shared helper since the exact same (buggy) logic was duplicated in both import UI components.

### Verified
- Unit-checked the matching function directly with the real account list (ING current, BUNQ Current, Crypto card, etc.): crypto.com → Crypto card ✅, ING → ING current ✅, BUNQ → BUNQ Current ✅
- `tsc --noEmit` clean, `majordom-web` rebuilt

### Files modified
- `frontend/src/lib/csvImportUtils.ts` — new: `matchAccountBySource()`
- `frontend/src/pages/ImportPage.tsx` — use shared helper
- `frontend/src/components/CsvImportCard.tsx` — use shared helper

### Lessons
- Token overlap can false-positive on generic words (e.g. a hypothetical "Unknown Bank XYZ" source matching an account literally named "Bank Name" via the shared "bank" token) — acceptable since this only preselects a dropdown value the user can still change before confirming, never auto-executes.

### Unresolved
- "Offer to create a new account if none matches" (the second half of the original #118 request) — not implemented, preselection only

## 2026-07-02 — Near-duplicate detection for CSV import (date+merchant match, different amount)

### Resolved
- Root cause of the Porkbun/Revolut-deposit reports above: exact-hash dedup (`SHA256(date+merchant+amount)`) can never catch a transaction re-imported with a *corrected* amount — different amount means different hash, so it silently looks brand new and would create a duplicate on confirm.
- Added a secondary check in `preview_csv`: `_fetch_preview_data()` now also builds a `(date, payee name lower) -> [existing amounts]` index across all AB transactions. For any CSV row that isn't an exact duplicate, if a transaction already exists in AB on the same date with the same merchant but no matching amount, the row is flagged `possible_duplicate=True` with `existing_amount` set — shown in both import UIs as a yellow warning icon + tooltip (ImportPage.tsx also shows an inline text line). Purely advisory — does not block import, still fully user-editable per the confirmation-card rule.

### Verified live
- Created a transaction manually (simulating a historical wrong-amount import: "Porkbun Test 2", 2026-06-25, €11.08), then previewed a synthetic crypto.com-format CSV with the same date+merchant at the corrected €9.78 (Native Amount). Response: `duplicate: false, possible_duplicate: true, existing_amount: 11.08` — exact reproduction of the real Porkbun case reported live.

### Files modified
- `backend/api/csv_import.py` — `_fetch_preview_data()` returns near-dup index; `ImportRowPreview` gains `possible_duplicate`/`existing_amount`; `preview_csv` computes the flag per row
- `frontend/src/lib/api.ts` — `ImportRow` type extended
- `frontend/src/pages/ImportPage.tsx` — warning icon + inline text for possible-duplicate rows
- `frontend/src/components/CsvImportCard.tsx` — warning icon (tooltip only, compact chat card layout)

### Lessons
- Two historical CSV rows reported live (Porkbun @ old wrong amount, 2 Revolut transfer deposits) were not caught by exact dedup because they were created in AB *before* the #101/#102/#103 fixes landed (old/missing `financial_id`) — a one-time data-migration side effect of fixing historical wrong data, not a new bug. This near-duplicate check is what makes that class of situation visible instead of silent, going forward.

### Bug found live in production, same day
- The initial version only flagged `possible_duplicate` when the existing amount *differed* from the CSV's. Tested live against the real Porkbun row in production: not flagged. Root cause — `financial_id` is fixed at creation time; the real Porkbun transaction had already been hand-corrected in AB (amount edited 11.08 → 9.78 directly in the UI), so the *displayed* amount now matched the CSV, but `financial_id` still held the old hash. Fix: flag on any (date, merchant) match regardless of amount equality. Reverified live against both sub-cases (amount matches / amount differs) plus a regression check (20-row exact-duplicate CSV — 0 false positives). Commit `f486786`.

### Unresolved
- Remaining 26 issues from the vault triage (#104–#131) — not started

## 2026-07-03 — Categorization bug batch (#104, #106, #107, #108)

### Resolved
- **#104 (unify categorization tools):** `propose_categorize_by_payee` and `propose_categorize_with_rule` were near-identical, only differing in optional rule creation — the LLM could silently pick the no-rule variant on an ambiguous request. `propose_categorize_with_rule` already supported an optional `create_rule` override, so it's now the *only* categorization tool. Removed `propose_categorize_by_payee` entirely (registry schema + dispatcher, `actual_budget.py` function, `_PROPOSAL_TOOLS` entry, the dead `categorize_by_payee` branch in `category_actions.py`). Added an editable payee input to the remaining card (`CategoryActionCard.tsx`), matching what the removed tool's card offered. Verified live: only one categorize tool registered; `propose_categorize_with_rule` returns the full card (payee, count, category, rule).
- **#107 (inconsistent card vs. direct execution):** reproduced the exact original phrase ("transferurile catre xtb sunt investitii") live, 3 times, 2 phrasings — always returned a proper confirmation card, never direct execution. Root cause was almost certainly the same tool-selection ambiguity as #104; closed as fixed by #104's unification.
- **#108 (setup balance card forces adjustment):** turned out the underlying mechanism already supported deferring — `adjust_account_balance()` no-ops (creates nothing) when the submitted value matches the current balance, so leaving a field untouched was already safe. The real gap was UX clarity: a pre-filled number looks verified even when it isn't, no visible way to say "I don't know, skip this". Added an explicit **Skip** toggle per account in `SetupBalancesCard.tsx` — disables the input and forces the current balance to be sent regardless of what's typed, removing any ambiguity about whether skipping is safe.

### Investigated, left open
- **#106 (card disappears on navigation):** chat `messages` state is lifted to `App.tsx`/`Layout`, above `<Routes>` — in-app `NavLink` navigation (Home → Chat) shouldn't unmount it, and an existing fix (`bb37852`, 2026-06-01) already guards active cards against being overwritten by `loadChatHistory()` on window-focus reload. Architecturally, simple in-app navigation should already be safe. Real gap found: proposal/card-type messages are explicitly excluded from `saveChatHistory()` (only the last user+assistant text exchange is persisted server-side) — if the *page itself* gets reloaded (mobile PWA killed and restarted while backgrounded, not just switched-to within the SPA), the card is genuinely unrecoverable. User wasn't sure which scenario triggered the original report; left open. If it recurs, the PWA-restart case needs server-side persistence of structured card JSON, not just role+content strings — bigger schema change.

### Verification method
No pytest suite for these flows — verified live against the local docker-compose stack (`majordom-api`/`majordom-web`/`actual-budget` on this machine, test data only) by calling `execute_tool()` directly in the running container and hitting `/api/chat` with curl, streaming the raw JSON response.

### Lessons
- Two of three original bug reports (#107, #108) turned out to already be non-issues or side-fixed once actually re-tested against current code — the vault findings were accurate for the day they were written (2026-07-01/02), but code moves fast enough that re-verifying live before implementing is essential, not optional. Don't trust a month/day-old finding without reproducing it first.
- The license-plate regex in `scripts/check-private-data.sh` false-positives on ordinary Tailwind CSS classes when a short letter-only class sits right next to a numeric-width class next to another letter-only class (letter+digits+letters pattern). Fixed by reordering classes so the pattern breaks, not by bypassing the hook — worth knowing if it happens again, don't reach for `--no-verify`.

### Files modified
- `backend/tools/registry.py`, `backend/tools/finance/actual_budget.py`, `backend/api/chat.py`, `backend/api/category_actions.py` — remove `propose_categorize_by_payee`
- `frontend/src/lib/api.ts`, `frontend/src/components/CategoryActionCard.tsx` — editable payee field on the unified card
- `frontend/src/components/SetupBalancesCard.tsx` — Skip toggle per account

### Unresolved
- #105 (categorization ignores notes/description) and #109 (payee fuzzy matching) — not started
- Remaining vault-triage issues (#110–#131) — not started

## 2026-07-03 — #109 (payee fuzzy matching) + #132 (notes-based categorization filter)

### Resolved
- **#109:** two-part fix. Backend: on "no uncategorized transactions for payee X", suggest real close-matching payee names from actual uncategorized data (case-insensitive `difflib` match — was case-sensitive before, silently dropping otherwise-good matches, e.g. 0.48 vs 0.56 ratio just under the 0.5 cutoff) instead of leaving the LLM to invent plausible-looking names. Frontend: `chat.py` forwards any `_PROPOSAL_TOOLS` result straight to the client without checking success vs error, and the frontend had no handler for `{"type": "error"}` — it fell through to being rendered as literal raw-JSON text. Added explicit `error`/`info` handling in `handleChatChunk`. Verified live: typo "gemente" now suggests the real payee "Gemeente Amsterdam".
- **#132 (renumbered from #105 — see below):** categorization only ever matched on payee, ignoring any notes/description condition in the user's instruction. Added an optional `notes_contains` parameter (case-insensitive substring) through the full chain: `ActualBudgetClient` → `FinanceProvider` → the tool → the confirmation card, which now also previews the actual affected transactions (date/amount/notes) instead of only an aggregate count. Verified live with a synthetic Belastingdienst-style car/motorcycle-tax scenario (3 uncategorized transactions, 2 matching the condition, 1 not) — the conditional command correctly matched only the 2 relevant rows and listed them on the card.

### Privacy incident, same session
- While testing #132, the pre-commit hook's license-plate regex correctly caught a real Dutch license plate the user had used as a code example in `registry.py` docstrings — copied there from GitHub issue #105 (created earlier in this session, before we'd re-verified the vault source with the user). The issue body also contained it, unprotected by any hook. Confirmed with the user it was a real plate, not a placeholder.
- Fix: scrubbed the local file (generic wording, no real codes). Checked the whole repo tree and git history (`git log --all -S`) for both plate strings — clean, nothing committed. Checked GitHub via `gh search issues` — only #105 had it. Since the repo is public (`private: false`) and GitHub shows issue-body edit history to anyone who can view the issue (not just to GitHub internally), editing the issue body alone would have left the original text recoverable via the "edited" revision link. Deleted #105 entirely and recreated it clean as **#132** with a generic placeholder in place of the real plate codes.
- Renumbering note: any earlier reference to #105 in this session's issues/docs/commits now means #132.

### Files modified
- `backend/tools/finance/actual_budget.py` — fuzzy payee suggestions on error
- `frontend/src/pages/Chat.tsx` — `error`/`info` type handling in `handleChatChunk`
- `backend/core/actual_client/client.py`, `backend/core/finance/provider.py`, `backend/core/finance/actual_budget_provider.py` — `notes_contains` param + `list_uncategorized_by_payee()`
- `backend/tools/finance/actual_budget.py`, `backend/tools/registry.py`, `backend/api/category_actions.py` — `notes_contains` through the tool + confirm flow
- `frontend/src/lib/api.ts`, `frontend/src/components/CategoryActionCard.tsx` — notes filter + transaction preview list on the card

### Lessons
- Never copy a "real case" example verbatim from a private source (vault) into a public GitHub issue without checking first whether the specific identifiers in it (plate numbers, in this case) are real. The general finding/pattern is fine to publish — the concrete identifying value is not, even when it feels like "just an example."
- `git log --all -S"<string>"` (pickaxe search) is the right tool to confirm something never made it into git history before assuming a leak needs a history rewrite, not just a file edit.
- GitHub issue-body edit history is visible to anyone who can view the issue on a public repo (not a GitHub-internal-only artifact) — editing removes it from the current view but not from the recoverable revision history. Delete + recreate is the only real fix once something sensitive has been posted.

### Unresolved
- Remaining vault-triage issues (#110–#131) — not started

## 2026-07-03 — UX polish batch (#119, #127, #128, #129, #130, #131)

### Resolved
- **#119:** `CsvImportCard.tsx` blocked the Import button on any uncategorized expense row — `ImportPage.tsx` (the other import UI) never had this restriction. Removed the block; uncategorized rows import fine and surface later via the M4.5 digest nudge.
- **#127:** `get_uncategorized_groups()` ordered by transaction count per payee — switched to each group's most recent transaction date (`func.max(Transactions.date).desc()`). Verified live: June transactions now sort ahead of May ones regardless of count.
- **#128:** system prompt now instructs stating the categorize command format before listing groups, not after. Verified live.
- **#129:** digest alert parts get a type-specific emoji prefix, joined with blank lines instead of single newlines.
- **#130:** `/api/home` now returns `uncategorized_count`/`unreconciled_count` (same session, no extra `download_budget()`); new "Needs resolving" widget on Home. Verified live: matches `count_uncategorized()` exactly (19 == 19).
- **#131:** messages carry a `ts` (epoch ms) timestamp, threaded through creation and server-side history loading, rendered as a caption under each bubble.

### Found and fixed while implementing #129
- The chat markdown wrapper used `prose prose-invert prose-sm ... prose-p:my-0 ...` — but `@tailwindcss/typography` was never added to `tailwind.config.js`'s plugin list. Every `prose-*` class had generated zero CSS this whole time (confirmed: `grep -c "prose" dist/assets/*.css` → 0 after a build). My digest blank-line fix would have been invisible without also fixing this. Replaced with plain arbitrary-variant selectors (`[&_p]:my-2 [&_p:first-child]:mt-0 [&_p:last-child]:mb-0 [&_ul]:my-1 [&_li]:my-0 [&_a]:text-accent [&_a]:underline`) that don't need the missing plugin — confirmed real rules in the compiled CSS afterward. Prompted directly by the user asking "does differentiation happen between messages?" after seeing the digest fix — good catch, would have shipped invisible.

### Files modified
- `frontend/src/components/CsvImportCard.tsx` — remove needsActionCount block
- `backend/core/actual_client/client.py` — group sort by date, `uncategorized_count`/`unreconciled_count` in `get_home_data()`
- `backend/api/chat.py` — CTA-first system prompt rule
- `backend/services/notification_service.py` — emoji-prefixed, blank-line-separated digest parts
- `backend/api/home.py` — passthrough (no change needed, already spreads `**data`)
- `frontend/src/lib/api.ts` — `HomeData` fields
- `frontend/src/pages/Home.tsx` — "Needs resolving" widget
- `frontend/src/pages/Chat.tsx` — `ts` field, timestamp caption, prose-class fix
- `frontend/src/App.tsx` — preserve `ts` on history load

### Lessons
- A missing Tailwind plugin can make an entire class family (`prose-*`) silently do nothing for a long time — nothing errors, nothing warns, the classes just don't exist. `grep -c "<prefix>" dist/assets/*.css` after a build is a fast way to confirm a utility class actually generated CSS before trusting it.
- The user's plain question ("is there visible separation?") caught a real gap my own live-testing missed, because my verification was at the data/API layer (confirming the emoji+blank-line text was correct), not the rendered-pixel layer. Worth remembering: text-level verification isn't the same as visual verification for CSS-dependent fixes.

### Unresolved
- Remaining vault-triage issues from the "proactive budget intelligence" and "import robustness" groups (#110–#117, #120–#126) — not started

## 2026-07-03 — #122 redesigned through discussion: notes-based category suggestion + AB rule

### Resolved
- Discussed #122 with the user before implementing (as requested) — the original issue framing ("propose an auto-rename rule after ≥2-3 identical occurrences") didn't match their real case. Their example: sending money to a family member for varying purposes (groceries / gift / allowance) — payee history can't disambiguate a category, but the notes they type for that specific transfer can, since they already use the same notes-based disambiguation pattern for Belastingdienst car/motorcycle tax (built in #132 earlier today).
- Confirmed via `actualpy` source that AB rule conditions natively support `field="notes"` (not just `imported_description`) — so the fix could stay fully AB-native, matching the user's explicit ask that AB remain the single source of truth, not a Majordom-side shadow rule system.
- `propose_transaction` now checks the transaction's `notes` against real AB category names before falling back to payee-history prediction (`SmartCategorizer`). A match becomes the suggested category on the confirmation card — still just a suggestion, never auto-applied.
- When a notes match fires, the card also shows an **unchecked** "Create AB rule" checkbox. If the user opts in, a real AB rule gets created via a new `create_payee_notes_rule()` client method: `imported_description contains <payee>` AND `notes contains <matched category>` → `set category`. Scoped to both payee and notes (not notes alone) so it can't over-match a different payee using the same word.
- New method threaded through the full `FinanceProvider` abstraction (`provider.py`, `actual_budget_provider.py`), matching the existing `create_payee_rule()` pattern from #104.

### Verified live end-to-end
- "sent Stefan 40 euro for groceries" → proposal returned `category_name: "Groceries"`, `notes_category_match: true`
- Confirmed with `create_rule: true` → queried the AB rules table directly: `conditions: [{"field": "imported_description", "op": "contains", "value": "Stefan", ...}, {"field": "notes", "op": "contains", "value": "Groceries", ...}]`, `conditions_op: "and"`, `actions: [{"field": "category", "op": "set", "value": "<Groceries category id>"}]` — a real, native AB rule, not a Majordom-side shadow record.

### Files modified
- `backend/tools/finance/actual_budget.py` — notes-based category match in `propose_transaction`
- `backend/core/actual_client/client.py` — `create_payee_notes_rule()`
- `backend/core/finance/provider.py`, `backend/core/finance/actual_budget_provider.py` — protocol + wrapper
- `backend/tools/proposals.py`, `backend/api/proposals.py` — `notes_category_match` stored on the proposal, `create_rule` accepted at confirm
- `frontend/src/lib/api.ts`, `frontend/src/components/ProposalCard.tsx` — checkbox UI, `create_rule` passed to confirm

### Lessons
- Discussing before implementing changed the actual shape of the feature, not just its framing — the original issue (proactive digest-based rule scanning) would have been the wrong fix entirely for the user's real workflow. Worth the extra round-trip.
- `actualpy`'s `Condition.field` supports `notes` alongside `imported_description`/`category`/`amount`/etc. — worth remembering for any future AB-native-rule work instead of assuming payee/description are the only condition types available.

### Unresolved
- Remaining vault-triage issues from the "proactive budget intelligence" and "import robustness" groups (#110–#117, #120–#121, #123–#126) — not started

## 2026-07-03 — #123: notes-based fallback in uncategorized-groups review

### Resolved
- Discussed with the user: does the #122 notes-matching help when reviewing *existing* uncategorized transactions (CSV import / bank sync), not just new manual ones? It didn't — `get_uncategorized_groups()`'s `suggested_category` only ever came from payee category history, useless for a payee with no reliable history (person's name, varying purpose).
- Extended the same notes-vs-category-name matching into `get_uncategorized_groups()`, as a fallback only when there's no history-based suggestion (never overrides a reliable one). Requires every transaction in the group with usable notes to agree on the same matched category — a mixed-purpose payee stays unsuggested, deferring to manual review / the `notes_contains` filter from #105.

### Verified live
- New payee, one transaction with notes "groceries for family" → `suggested_category: "Groceries"`, `suggested_category_source: "notes"`
- Added a second transaction to the same payee with notes "for his personal use" (matches "Personal") → suggestion correctly dropped back to `null` instead of guessing either category

### Files modified
- `backend/core/actual_client/client.py` — `get_uncategorized_groups()` notes fallback + `suggested_category_source` field

### Unresolved
- Remaining vault-triage issues from the "proactive budget intelligence" and "import robustness" groups (#110–#117, #120–#121, #124–#126) — not started

## 2026-07-03 — #120 paused, #121 implemented

### #120 (own-account transfer detection) — paused
- Discussed matching criteria (same day) and researched the linking mechanism: actualpy has no "link two existing transactions as a transfer" helper, only `create_transfer()` (creates new ones) and the raw `transferred_id` column. Correct implementation would set payee (to the counterpart account's transfer-payee) + `transferred_id` (both directions) + clear category directly on the two existing transactions — must NOT use `set_transaction_payee()`/`create_transaction(process_payee=True)`, which auto-creates a new linked transaction and would duplicate. User wants to think it over before committing to this — flagged as higher-risk than the other fixes today. Comment left on the issue with full notes.

### #121 (receipt scan merge with bank-sync) — resolved
- Discussed tolerances: same account, date within ±1 day, amount within 2%, and confirmed the UX (inline card choice, not silent auto-merge).
- Both receipt confirm flows (`ReceiptCard.tsx` general/grocery, `FuelReceiptCard.tsx` photo mode) now check `find_near_duplicate_transaction()` before creating anything. A match returns as `possible_match` on the response without saving; the card shows "Attach to this" / "Create new anyway". Attaching calls a new `attach_receipt_to_transaction()` that updates category and *appends* to existing notes (doesn't overwrite).
- Text-mode fuel entry (typed manually via chat) deliberately unaffected — no OCR uncertainty there.
- Bug caught live: initial query used `Transactions.account_id`, which doesn't exist on the actualpy model (real column is `acct`) — fixed before it ever reached a build that mattered.
- Verified live end-to-end reproducing the exact original case: CSV-imported an uncategorized "BCK*SHELL AZIELAAN" transaction at €70.09, searched with a synthetic €70.69 receipt amount (0.85% off, same date) — found. Attached "Car Costs" + fuel notes — confirmed directly in the database: same transaction, same €70.09 amount, now categorized with notes appended, no duplicate created.

### Files modified
- `backend/core/actual_client/client.py` — `find_near_duplicate_transaction()`, `attach_receipt_to_transaction()`
- `backend/services/receipt_service.py` — `check_near_duplicate()`, `attach_to_existing()`
- `backend/api/receipts.py` — both confirm endpoints check for a match first; `NearDuplicateMatch`, `force_new`/`attach_to` request fields, `possible_match` response field
- `frontend/src/lib/api.ts`, `frontend/src/components/ReceiptCard.tsx`, `frontend/src/components/FuelReceiptCard.tsx` — inline match-choice UI

### Unresolved
- #120 — paused pending user decision
- Remaining vault-triage issues: #110–#117, #124–#126 — not started

## 2026-07-03 — Old open-issue cleanup: #118 (missed close), #39/#40 (stale), #83 (real fix)

### Resolved
- Audited the full open-issue list for older, small, forgotten items — not just today's vault triage batch.
- **#118** (CSV account auto-suggestion) was already fixed earlier today (`f819da8`) but never closed — closed now with the reference.
- **#39** (income variance alert) and **#40** (goal risk alert) were stale — both fully implemented as M4.3/M4.4 (`_check_income_variance()`/`_check_goal_risk()` in `notification_service.py`, confirmed against `docs/roadmap.md` ✅ status) but left open since completion. Closed as duplicates of already-shipped work.
- **#83** (delete_vehicle_log_entry should also delete the AB transaction) — checked the actual code, not just the roadmap status. M3.6 is marked ✅ done, but this specific sub-fix was never implemented: the confirm handler only ever deleted the SQLite `vehicle_log` row, silently orphaning the linked AB transaction for any refuel logged via photo/text since M3.6 shipped. Genuinely unresolved, not stale — implemented it: `ActualBudgetClient.delete_transaction()` (soft-delete via tombstone, no existing helper for this), `has_ab_transaction` warning on the confirmation card, AB-transaction deletion wired into the confirm handler.

### Verified live
- #83: created a real AB transaction + linked `vehicle_log` row (financial_id pointing at it), deleted through chat — card showed "⚠ Will also remove the Actual Budget transaction" — after confirming: AB transaction has `tombstone=1`, `vehicle_log` row gone.

### Files modified
- `backend/core/actual_client/client.py` — `delete_transaction()`
- `backend/tools/finance/vehicle.py` — `has_ab_transaction` in the delete proposal
- `backend/api/vehicle_log_actions.py` — deletes the AB transaction on confirm when linked
- `frontend/src/lib/api.ts`, `frontend/src/components/VehicleLogActionCard.tsx` — warning UI

### Lessons
- "Roadmap says the milestone is done" and "every issue tied to that milestone is actually implemented" are not the same claim — #83 was filed with a very specific, concrete fix spec against M3.6, and the spec itself was never done even though the milestone got marked ✅ for its broader scope. Worth spot-checking issue bodies against actual code when a milestone is closed, not just trusting the checkbox.
- Old issues accumulate two failure modes: forgotten-but-done (close it, no code needed — #39, #40, #118) and forgotten-and-not-done (#83). Both are worth a periodic sweep; they look identical from the issue list alone (just "open"), the only way to tell them apart is checking the code.

### Unresolved
- #120 — paused pending user decision
- Remaining vault-triage issues: #110–#117, #124–#126 — not started
- Other older open issues not yet audited: #3–#17 (early backlog, likely stale/superseded — not checked), #56–#62 (onboarding-related, M2 onboarding was cancelled — likely stale), #65, #75–#82, #86–#100 — not reviewed this pass

## 2026-07-03 — Full old-issue audit (#4, #7, #8, #9, #11, #32, #33, #56, #57, #59, #62, #100)

### Resolved
- Continued the stale-issue audit across the full open-issue list (not just today's vault triage), checking each against current code/docs rather than assuming.
- **#56, #57, #59** — all specifically about the M2 Onboarding Wizard (cancelled 2026-05-31, replaced by M2-NEW). Verified: no onboarding files in backend/, no /onboarding route in frontend. Closed as stale.
- **#62** (Financial Adapter Layer) — exactly what M5.2's `FinanceProvider` Protocol already delivers (`backend/core/finance/provider.py`). Closed as done, never closed when M5.2 shipped.
- **#32** (transfer detection after historical import) — superseded by #120 (more current design). Closed.
- **#33** (sync merchant→category mappings to AB rules) — exactly what `create_payee_rule()` (#104) and `create_payee_notes_rule()` (#122) already deliver. Closed as done.
- **#4** (Ghostfolio integration) — closed as stale per the documented decision "Sure replaces Ghostfolio" (docs/decisions.md, 2026-06-03) + proposes dead Telegram bot commands. **Reopened same session** — user pushed back, isn't 100% convinced Sure is the right call, wants Ghostfolio to stay under consideration. Left open with a note flagging the tension with the documented decision — needs its own dedicated conversation, not resolved here as a side effect of cleanup.
- **#7** (freelance dashboard), **#8** (transfer detection ING→crypto.com), **#9** (RON support), **#11** (multilingual bot) — all proposed dead Telegram bot commands (bot fully removed: no bot/ directory, no bot service in docker-compose.yml). #7 superseded by #126, #8 superseded by #120, #9 superseded by the current roadmap.md backlog entry, #11's actual need (respond in the user's language) already met more simply by the system prompt instruction, no stored preference needed. All closed.
- **#100** (dead onboarding_state code in database.py) — implemented. Confirmed no external callers, removed the table + 3 methods, verified the app builds and `/api/health` responds normally after the schema change.

### Found, not touched
- **#95** (activate daily backup on server, P0) and **#61** (automate backup during install) — real, unresolved, but #95 specifically is a manual server-side cron task on the user's real production host, not something to verify/execute from this sandboxed session. Left alone.
- **#96** (introduce a `dev` branch, stop deploying from `main` directly) — a git workflow/process change, not a code fix — left for the user to decide, not something to act on unilaterally.
- **#65** (LLM hallucinates account creation), **#76** (offer to add goal amount to budget), **#77** (trend indicator on Home cards), **#79** (vehicle list/deactivate tool), **#87** (copy last month's budget via chat — would unblock the blocked #125), **#89** (transfer card create-account option), **#92** (Android PWA help modal scroll bug), **#97** (one-line deploy workflow fix) — all confirmed genuinely unresolved (not stale) via code checks, flagged as real candidates for future work, not implemented this pass.

### Files modified
- `backend/core/memory/database.py` — remove `onboarding_state` table + methods

### Lessons
- A documented architecture decision (docs/decisions.md) can still be genuinely unsettled in the user's mind even after being written down — "Sure replaces Ghostfolio" was resolved on paper but not resolved for real. Don't treat a decisions.md entry as permanently final when closing related issues; it's a snapshot of a decision at the time it was written, not necessarily still current. Ask before closing something that hinges on it if there's any doubt.
- Auditing ~40 older issues found a genuine mix: ~12 stale (already done or superseded), 1 real bug fixed (#83, previous entry), 1 dead-code cleanup (#100), and a handful of real, still-open, well-specified candidates for next time (#87 in particular unblocks #125).

### Unresolved
- #4 — Ghostfolio, reopened, needs a dedicated decision conversation
- #120 — paused pending user decision
- Real candidates found but not implemented: #65, #76, #77, #79, #89, #92, #97 (needs deploy-workflow approval), #61/#95 (server-side ops, out of scope here)
- Remaining vault-triage issues: #110–#117, #124 (partial), #126 — not started
- Issues #3, #6, #13–#17 still not individually audited (general early backlog / homelab-adjacent, not clearly Majordom-scope)

## 2026-07-03 — #87 (copy last month's budget) + #125 fix + rollover toggle (#124 partial)

### Resolved
- **#87:** `propose_budget_copy(month="")` fetches the previous month's per-category budgeted amounts via a new `get_budget_copy_source()` client method, returns an editable card (grouped by category group, pre-filled amounts) — nothing written until confirmed. Confirm applies each (possibly user-edited) amount via the existing `set_budget_amount()`.
- **#125 (fixed as part of #87):** goal-template categories are excluded from the copy — detected via `"#template"` in the category's Notes entry (the `Notes` table, keyed by category id — categories have no `notes` column directly).
- **Proactive variant, added on request:** `_check_budget_copy_nudge()` in the daily digest — first 5 days of the month, nudges if last month had a real budget but the current month looks empty. New notification rule `budget_copy_nudge` seeded in `main.py`.
- **Rollover overspending, added on request (#124 partial):** `propose_set_budget_carryover(category_name, enabled, month="")` toggles the `carryover` column on ZeroBudgets/ReflectBudgets — the same field AB's own UI writes. Confirmed via `actualpy` source that `create_budget()`'s `carryover` kwarg only applies when creating a brand-new budget row (silently ignored if a budget already exists for that month, the common case) — `set_budget_carryover()` sets the field directly on the existing row instead.

### Two real bugs found and fixed while building this
1. **`Categories.goal_def`/`template_settings` are not reliable "has a goal template" signals.** They looked plausible (a newer structured "Budget Automations" system was mentioned in earlier research) but verified live: `template_settings` returns `{'source': 'notes'}` on literally every category regardless of whether it has a template, and `goal_def` stayed `None` even on a category with a real `#template` note. Removed both checks, kept only the Notes-text check that's actually confirmed to work.
2. **The `zero_budgets`/`reflect_budgets` SQL lookup filtered `WHERE id LIKE '{yyyymm}%'`, assuming every budget row's `id` follows a `"{month}-{category_id}"` convention.** It doesn't — `actualpy`'s `create_budget()` generates a random UUID `id` for brand-new rows. Any budget set through Majordom's own tools (not the real AB app) was silently invisible to this filter. This pattern was copied across **three call sites** — `get_budget_status()`, `get_home_data()` (Home screen), and the new `get_budget_copy_source()` — so it wasn't just breaking the new feature, it's very likely been under-reporting budgeted amounts on the Home dashboard for any category ever budgeted via chat, since M4.12. Fixed all three to filter by the `month` column instead. Found via a direct discrepancy: `set_budget_amount()` reported success (`old_amount: 150.0, new_amount: 150.0`) for Transport, but the copy-source query still showed `0.0` for the same category/month — cross-checked the raw table and found the real row had a random-UUID `id`, invisible to the old filter.

### Verified live end-to-end
- Set June budgets (Groceries €400, Transport €150, Personal €2000 + a `#template` note on Personal simulating a goal category).
- `propose_budget_copy(month="2026-07")` → correctly excluded Personal, included the rest with June's amounts.
- Confirmed via chat → verified July's budget directly via `get_budget_status()`: Transport now correctly shows €150 (only visible after the id-vs-month fix; before the fix it silently showed €0 despite the write having succeeded).
- Repeated July → August to confirm the fix holds on a second cycle.
- Rollover: enabled carryover on Groceries via chat, confirmed `carryover=1` directly in the database, amount unchanged (€400).

### Files modified
- `backend/core/actual_client/client.py` — `get_budget_copy_source()`, `set_budget_carryover()`, id-vs-month query fix (3 call sites)
- `backend/core/finance/provider.py`, `backend/core/finance/actual_budget_provider.py` — protocol + wrapper for both new methods
- `backend/tools/finance/actual_budget.py`, `backend/tools/registry.py`, `backend/api/chat.py` — `propose_budget_copy`, `propose_set_budget_carryover` tools
- `backend/api/category_actions.py` — `budget_copy` and `set_budget_carryover` confirm branches, `category_amounts` override field
- `backend/services/notification_service.py` — `_check_budget_copy_nudge()`; also fixed a double-emoji bug found in passing (income_variance/goal_risk/vehicle alert texts already carry their own embedded emoji, #129's external prefix was doubling up)
- `backend/main.py` — seed `budget_copy_nudge` notification rule
- `frontend/src/lib/api.ts`, `frontend/src/components/CategoryActionCard.tsx` — rollover card branch
- `frontend/src/components/BudgetCopyCard.tsx` — new: grouped, editable budget-copy card
- `frontend/src/pages/Chat.tsx` — wire `BudgetCopyCard` for the `budget_copy` action

### Lessons
- A field that *sounds* like the right signal (`template_settings` for "has a template") needs the same live-verification treatment as everything else this session — it would have silently excluded every single category from every future budget copy, a much worse failure mode than the bug it was meant to prevent.
- The `id LIKE '{month}%'` vs `month = {month}` bug is the kind that only surfaces when data gets written through a *different* code path than the one that originally established the convention — the existing dashboard code had presumably only ever been tested against budgets set through the real AB app (which happens to use the composite-id convention), never against `actualpy`'s own `create_budget()` default (random UUID). Worth remembering: a working-looking existing function isn't proof its query logic is correct for data written a different way.

### Unresolved
- #124 remains open — Budget Automations structured system and goal-template ('by' date) creation/editing via chat not investigated
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — Home dashboard: rollover categories were disappearing (reported on real production data)

### Resolved
- User reported live, on their real (non-demo) budget: a "Holidays" category funded once with rollover enabled, then relied on for spending across the following month with no fresh monthly allocation — vanished from Home entirely, even though real money was still available.
- Root cause: `get_home_data()` only ever read the current month's *fresh* budgeted amount from `zero_budgets`/`reflect_budgets` (0, since nothing new was allocated) — never the *accumulated* rollover balance.
- Fixed using `actualpy`'s `get_accumulated_budgeted_balance()` — the same value AB's own UI shows as a category's Balance. When a category's fresh monthly amount is 0, fall back to the accumulated balance as the effective "budgeted" amount for display. Naturally solves both halves of the request: shows while a carried-over balance exists, disappears once it's fully spent (no separate "hide it" logic needed).

### Two more bugs caught while implementing
1. The accumulated-balance fallback was initially placed *after* the existing `budgeted==0 and spent==0 -> skip` check — a rollover category with a balance but zero spending yet that month got filtered out before ever reaching the fallback. Moved the fallback earlier.
2. `get_category()` (actualpy) resolves by **name**, not by ID — passing the raw `category_id` string returned `None` internally and silently broke the balance lookup. Had to pass the actual `Categories` object instead (already available from the category fetch earlier in the same function).

### Verified live
- Set "Divertisment" budgeted €500 in June with rollover enabled, explicitly zeroed July's fresh amount. Home correctly showed `budgeted: 544.10` for July (June's ending accumulated balance, including some prior test data).
- Spent the full €544.10 in July → re-checked: accumulated balance correctly dropped to €0.00.

### Files modified
- `backend/core/actual_client/client.py` — `get_home_data()` accumulated-balance fallback

### Lessons
- This bug had been live on the user's real budget, not just the test instance — a good reminder that "works in the demo data" isn't the same as "works for how the user actually uses rollover categories" (funded once, spent gradually, no monthly re-allocation is a normal and common pattern for a category like a trip fund).
- Two `actualpy` helper functions found today (`get_accumulated_budgeted_balance`, and earlier `get_category()`'s name-only resolution) both had non-obvious behavior that only surfaced by calling them directly and checking real output — worth continuing to verify actualpy behavior empirically rather than assuming from function names alone.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — Home "Needs resolving" widget redesign + unreconciled/bank-sync logic (#130 follow-up)

### Resolved
- User feedback on the original #130 widget: it only showed uncategorized/unreconciled counts and linked generically to `/chat` — unclear what to actually do about it. Redesigned into an expandable dropdown that lists every pending item individually; clicking one navigates to `/chat` and pre-fills (never auto-sends) a ready-to-edit prompt for that specific item.
- New `get_pending_items()` in `notification_service.py` aggregates everything the daily digest checks (uncategorized, unreconciled, over-budget categories, at-risk goals, vehicle reminders, account staleness) but computed live on each Home load — deliberately NOT gated by the digest's once-a-day anti-spam, since Home should always reflect current state.
- New `/api/home/pending` endpoint backs it; frontend `Home.tsx` widget rewritten with a `pendingExpanded` toggle state.

### Two more real requests folded in during discussion
1. **Unreconciled count was noisy for bank-linked accounts.** They self-reconcile at the next sync, so flagging them was a false positive — the user only wants to see manual/CSV-only accounts (e.g. crypto.com) where staying unreconciled is a real, standing problem. `count_unreconciled()` and `get_home_data()`'s `unreconciled_count` now `JOIN Accounts` and exclude rows where `account_sync_source` is set.
2. **Bank-sync staleness split by account type**, since the two have no shared "freshness" definition: bank-linked accounts flag if `last_sync` is >24h old (something's actually broken, worth a manual nudge); manual/CSV accounts flag if the most recent transaction is >=7 days old (no auto-sync exists for them at all, so the bar is "no fresh import in a week" instead). New `get_account_sync_status()` client method returns per-account `{id, name, sync_source, last_sync, most_recent_transaction_date}` to support both checks.
3. **`propose_bank_resync(account_name)`** — new write tool, confirmed `actualpy` exposes `Actual.run_bank_sync(account=...)` to trigger a live re-sync. Goes through the same confirmation-card flow as every other write tool (a resync pulls new transactions into AB, not a pure read) — wired through `registry.py`, `_PROPOSAL_TOOLS` in `chat.py`, and a new `bank_resync` branch in `category_actions.py`'s confirm handler.

### Verified live
- Simulated a bank-linked account (`account_sync_source="gocardless"`, stale `last_sync`) on the "ING current" test account — `unreconciled_count` correctly dropped (45 → 10) once that account's transactions were excluded, and a `bank_sync_stale` pending item appeared with prompt `"resync ING current"`.
- Confirmed `csv_stale` items appeared with accurate day-counts for genuinely stale manual accounts (ING Savings 33 days, XTB 32 days, Cash 69 days, BUNQ Current 36 days).
- Full chat flow: sent "resync ING current" → got a `category_action`/`bank_resync` card via the real running `/api/chat` endpoint → confirmed via `/api/category-actions/{id}/confirm` → got `"Resynced 'ING current' — 0 new transactions imported."` (0 expected — no real GoCardless connection in the test environment; confirms the code path itself doesn't crash).
- Reverted the simulated `account_sync_source`/`last_sync` on "ING current" back to `None` after testing, to avoid leaving misleading fake data in the shared test environment.
- `ast.parse` on all modified backend files, `npx tsc --noEmit` (exit 0), full docker rebuild of `majordom-api` + `majordom-web`.

### Files modified
- `backend/core/actual_client/client.py` — `get_account_sync_status()`, `run_bank_resync()`, `count_unreconciled()` bank-sync exclusion, `get_home_data()` unreconciled fix
- `backend/core/finance/provider.py`, `backend/core/finance/actual_budget_provider.py` — protocol + wrapper mirrors
- `backend/tools/finance/actual_budget.py`, `backend/tools/registry.py`, `backend/api/chat.py` — `propose_bank_resync` tool
- `backend/api/category_actions.py` — `bank_resync` confirm branch
- `backend/services/notification_service.py` — `get_pending_items()`, `bank_sync_stale`/`csv_stale` checks
- `backend/api/home.py` — `GET /home/pending`
- `frontend/src/lib/api.ts` — `PendingItem`, `getHomePending()`, `CategoryActionData` bank_resync fields
- `frontend/src/components/CategoryActionCard.tsx` — `isBankResync` branch
- `frontend/src/pages/Chat.tsx` — reads `location.state.prefill` to pre-fill (not auto-send) the input box, clears state after
- `frontend/src/pages/Home.tsx` — widget rewritten as expandable dropdown over `getHomePending()`

### Lessons
- "Unreconciled" isn't one concept — it means "needs your attention" for manual accounts but "will resolve itself shortly" for bank-linked ones. Conflating them made the count actively misleading rather than just noisy.
- Prefill-not-autosend (`navigate('/chat', { state: { prefill } })` + a `useEffect` that reads and clears `location.state`) is now the established pattern for "take me to chat with a running start" — reusable for any future notification-to-action flow.

**Correction, later same day:** the `bank_sync_stale`/`csv_stale` staleness rule described above was reverted — see the entry below ("drop bank-sync/CSV staleness noise"). It looked fine against test data but flooded the real widget with 22 items, most of them normal low-activity accounts.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started


## 2026-07-03 — #89: create destination account inline from the transfer card

### Resolved
- Old flow: when `propose_account_transfer` couldn't fuzzy-match the destination name to an existing account, it returned a plain text clarification ("pick an existing account or record as expense") and never showed the transfer card at all. Per #89, replaced with an inline flow directly on the card.
- `propose_account_transfer` now always returns the `account_transfer` card. When no match is found it sets `to_account_missing: true` and passes the raw typed name through as `to_account_name` — no extra round-trip before the card appears.
- `AccountTransferCard`: when the destination is missing, the "To" field swaps to a name input + "off-budget" checkbox (pre-filled with the typed name), plus a link to fall back to picking an existing account (covers the case where it was a fuzzy-match miss, not a genuinely new account). Same link exists in the other direction ("+ Create new account instead") on the normal dropdown.
- `POST /api/accounts/transfer` accepts optional `create_account_name` / `create_account_off_budget` — creates the account via the existing `client.create_account()` (already used by onboarding) before executing the transfer, still a single request either way.

### Bug noticed in passing (not fixed, out of scope)
- The fuzzy-match `cutoff=0.4` in `propose_account_transfer`'s `_resolve()` is aggressive enough to match an unrelated typed name ("Brand New Test Vault") to an existing test account ("Bank Name"). Also, `AccountListItem`/`GET /api/accounts` doesn't surface `off_budget` in the response even though the model declares the field — checked directly via `actualpy`'s `Accounts.offbudget` column instead to confirm the checkbox actually took effect. Neither blocks this feature; worth a small separate fix if it causes a real mismatch later.

### Verified live
- Sent a transfer to a name with no fuzzy match ("Zqxwrmpl Vault") — got `to_account_missing: true` on the card via the real `/api/chat` endpoint.
- Confirmed via `/api/accounts/transfer` with `create_account_name` + `create_account_off_budget=true` — new account created, `offbudget=1` confirmed directly on the `Accounts` row, transfer landed with the correct balance (€100).
- Cleaned up the test account afterward (tombstoned — `actualpy` disallows hard deletes, raises `ActualInvalidOperationError` otherwise).
- `ast.parse` on all modified backend files, `npx tsc --noEmit` (exit 0), full docker rebuild of `majordom-api` + `majordom-web`.

### Files modified
- `backend/tools/finance/actual_budget.py` — `propose_account_transfer()` always returns the card, drops the clarification branch
- `backend/api/chat.py` — system prompt note updated (card offers inline creation, not a clarification)
- `backend/api/accounts.py` — `TransferRequest.create_account_name`/`create_account_off_budget`, creates the account before the transfer when destination is empty
- `frontend/src/lib/api.ts` — `AccountTransferData.to_account_missing`, `confirmAccountTransfer()` takes an optional new-account payload
- `frontend/src/components/AccountTransferCard.tsx` — inline create-account UI, bidirectional toggle with the existing-account dropdown

### Unresolved
- #65 (expose `create_account` as a standalone chat tool, not just inline in the transfer card) — still open, related but separate scope
- Fuzzy-match cutoff and missing `off_budget` in `GET /api/accounts` — noted above, not fixed
- Remaining vault-triage issues: #110–#117, #126 — not started


## 2026-07-03 — Reverted bank-sync/CSV staleness noise from the Home widget

### Resolved
- User tested the redesigned "Needs resolving" widget against real accounts and reported it live: 22 items, most of them low-activity savings/goal accounts (e.g. a sinking-fund account with no new transactions in over a month) that are expected to sit idle, not actually broken. The 24h/CSV-7-day rule couldn't distinguish a stalled sync from a savings bucket touched twice a year.
- Per explicit instruction: `get_pending_items()` should only ever surface what the evening digest actually collects and sends — there is no `_check_bank_sync_stale()`/`_check_csv_stale()` in the digest, this rule only ever existed in the live Home widget and had none of the digest's anti-spam ("already told you, don't repeat"). Removed the account-staleness block entirely; kept uncategorized/unreconciled/over-budget/goal-risk/vehicle-reminder, which do mirror real digest checks.
- `propose_bank_resync` (the manual "resync this account" chat tool, still useful on request) is untouched — just no longer auto-surfaced by a blanket time threshold.

### Verified live
- `GET /api/home/pending` now returns only the 2 real items present in test data (uncategorized, unreconciled) instead of 22.
- `ast.parse` clean, docker rebuild of `majordom-api`. No frontend change needed — `Home.tsx` renders whatever the API returns generically, no hardcoded references to the removed item types.

### Files modified
- `backend/services/notification_service.py` — removed the account-staleness block from `get_pending_items()`, dropped the now-unused `timedelta` import

### Lessons
- A rule that looks reasonable against sparse test data (a handful of accounts, all genuinely active) can be actively wrong against a real account list with many low-traffic buckets — worth remembering that "stale" needs either a per-account baseline or explicit user configuration, not one global threshold, if this is revisited later.
- The instinct to mirror digest logic 1:1 in `get_pending_items()` (rather than inventing new independent checks) turned out to be the right constraint — the two checks that violated it were also the two that caused real noise.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — resync ING bug hunt: actualpy balanceType, chat history persistence, PWA stale cache, goal-notification dedup

### Resolved
- **Root cause of `resync ING` crash:** ING (via GoCardless) returns a `balanceType` code (`"XPCD"`) that actualpy's `BalanceType` enum doesn't recognize (checked against 0.22.3, the latest — same fixed list), crashing `run_bank_sync()` with a pydantic validation error before any transaction was parsed. We never read this field, only the transactions, so `backend/core/actual_client/client.py` now monkey-patches `BalanceType._missing_` at import time to fall back to `INFORMATION` on any unrecognized code. Verified live in production logs: `Unknown bank-sync balanceType 'XPCD' — treating as INFORMATION` followed by a clean `confirm 200 OK`.
- **Home staleness nudges removed entirely** (per explicit request, going further than the earlier watchlist compromise — see architecture.md rule 14): both the bank-sync and CSV staleness checks are gone from `get_pending_items()`. The bank one is what surfaced the crash above via its auto-generated "resync X" prompt.
- **Chat history silently losing entire exchanges on navigation (closes #106, worse than originally filed):** traced to `handleSendText`'s `onComplete` in `Chat.tsx` only ever persisting a plain-text assistant reply — a card-based flow (bank resync, category actions, ...) resolves into a local `status` message with nothing saved server-side, so the *whole* exchange (including the user's own message) was wiped the next time `loadChatHistory()` reloaded from the server (e.g. `visibilitychange` on Home↔Chat navigation). Fixed generically: a new effect watches for newly-resolved `status` messages and persists each one (with its preceding user message) exactly once, via a `WeakSet` + a `_synced` flag on server-loaded messages (see architecture.md rule 17). Verified live by the user — confirmed exchange survived navigation.
- **PWA serving a stale app shell indefinitely:** while debugging why the user saw different chat formatting locally vs. on the real server (LXC), found `frontend/public/sw.js` cached `/index.html` (and navigations) **cache-first** under a `CACHE_NAME` that never changed between deploys — the browser could keep serving an old shell (pointing at old JS/CSS bundle hashes) forever, regardless of how many times the backend redeployed. Fixed to network-first for the shell (cache only as offline fallback); hashed static assets stay cache-first (safe, new content = new URL). Bumped `CACHE_NAME` once to flush what was already stuck (see architecture.md rule 16).
- **Duplicate goal-risk item on Home:** "Needs resolving" listed `Goal 'X' at Y%, N months left` even though the same goal (with its own progress bar, remaining amount, deadline) is already shown directly in the "Financial Goals" section on the same page — pure duplication, removed from `get_pending_items()` (the daily digest's own goal-risk push is untouched).
- **`get_vehicle_log` tool description biased toward suggesting deletion:** its description literally said "or wants to find an entry to delete," which pushed the LLM to always close a refuel-history answer with a delete suggestion — including when the user was just reviewing history/investigating consumption, not looking to delete anything. Reworded to stop defaulting to a deletion suggestion and point toward `get_vehicle_stats` instead when the user seems interested in consumption.

### Investigated, no code change needed
- User suspected a resync had silently dropped a real transaction after a transient `database disk image is malformed` SQLite error appeared in logs on an unrelated `/api/home` call. Verified directly in Actual Budget: the transaction was present — no data loss, the error self-healed on the next request. Logged as a watch-item, [#135](https://github.com/Dorusto/life-os/issues/135), since actualpy downloads a fresh temp SQLite file per operation (no persistent `data_dir`), consistent with a one-off corrupted download rather than a concurrency bug (executor is single-worker).
- User asked to debug a suspected missing fuel-log entry for vehicle "Cora" — turned out `get_vehicle_log`/`get_vehicle_stats` (already-existing chat tools) were sufficient to self-serve the investigation; several real fill-ups had been skipped (confirmed via odometer gaps) but correctly excluded from the consumption average by the existing `fuel_full_tank`/`fuel_missed` flags in this specific case (skipped entry sat at the edge of the date range, not in the middle).
- **Real latent bug found while investigating the above (not fixed, logged only per user request):** `get_vehicle_stats`'s consumption formula (`backend/tools/finance/vehicle.py` ~line 412-438) computes `total_distance` as a single `MAX(odo_km) - MIN(odo_km)` span across all included (full+not-missed) rows. This only produces a correct result when every excluded row sits at the edge of the range, like this case — if a skipped/partial fill sits in the *middle* of a sequence, the distance span would still include the km driven on that skipped fill's fuel, but that fuel's liters are excluded from the sum, silently understating consumption. Correct fix (how Fuelio does it): compute consumption per consecutive interval between two genuine full-tank fills, discarding any interval touching a skipped one. Logged on [#134](https://github.com/Dorusto/life-os/issues/134), not implemented.
- Researched Fuelio's public feature set (fuel.io + app store listings) at the user's request and catalogued it on #134, split into "easy" (chart types where the data already exists in `vehicle_log`/`vehicles` — consumption/cost/price trends, cost-by-category, odometer progression, CSV export) vs. "complex/low value, noted only" (CarPlay, gas-station price map + navigation, route planning, iCloud/Dropbox backup, home-screen widgets) — no implementation done, explicitly deferred per user request.

### Files modified
- `backend/core/actual_client/client.py` — `_patch_bank_sync_balance_type()` monkey-patch
- `backend/services/notification_service.py` — removed bank-sync/CSV staleness block and goal-risk duplicate from `get_pending_items()`
- `frontend/src/pages/Chat.tsx` — card-resolution persistence effect, `_synced` field on `Message`
- `frontend/src/App.tsx` — mark server-loaded messages `_synced: true`
- `frontend/public/sw.js` — network-first app shell, `CACHE_NAME` bump
- `backend/tools/registry.py` — `get_vehicle_log` description no longer biased toward suggesting deletion
- `docs/architecture.md` — rules 15 (actualpy balanceType), 16 (PWA shell caching), 17 (card-resolution persistence pattern); rule 14 updated to reflect full staleness removal

### Lessons
- The GitHub Actions self-hosted runner (`majordom-Majordom`) is a *different machine* from the desktop this session's shell runs on — `docker ps`/`docker exec` here were showing a stale local Docker stack, not production. Always confirm which host you're actually looking at (`gh api repos/.../actions/runners`) before trusting local container state as a stand-in for the deployed one.
- A tool's `description` field doesn't just document behavior to future readers — it actively steers what the LLM chooses to say next, including free-text framing well beyond the tool's literal return value. "Use when X or Y" phrasing can bias the model toward Y even when the user's actual intent was closer to X.
- Existing chat tools (`get_vehicle_log`, `get_vehicle_stats`) already covered a "help me investigate what happened" request the user assumed would need direct database access — worth surfacing what already exists before scoping new work.

### Unresolved
- [#135](https://github.com/Dorusto/life-os/issues/135) — transient SQLite corruption, watch-item only
- [#134](https://github.com/Dorusto/life-os/issues/134) — Fuelio-parity charts + interval-based consumption fix, scoped but not started
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — Vehicle reminders hidden by anti-spam + staleness re-added with a watchlist

### Resolved
- User tested the widget live against real data and reported two problems: (1) a vehicle reminder they expected (car revision/APK update) never showed up, and (2) the CSV-staleness rule they'd asked to remove was actually a real, useful signal for one account (crypto.com) — they just wanted 4 lines total (3 synced banks + crypto), not everything.
- **Vehicle reminders bug (confirmed root cause):** `get_pending_items()` called `_check_vehicle_reminders()` as-is, which has its own digest-only anti-spam ("don't repeat the same alert within 7/30 days"). If the evening digest had already pushed an alert this week, the underlying condition was still true but suppressed from the widget too — the opposite of what a "show current unresolved state" widget should do. This was a known limitation flagged in the original widget's code comment, now confirmed in practice. Added `ignore_anti_spam: bool = False` to `_check_vehicle_reminders()`, threaded through all 5 of its internal spam gates; `get_pending_items()` calls it with `ignore_anti_spam=True`.
- **Staleness re-added, scoped down:** the >24h bank-sync check was already correctly limited to accounts with a live bank link (`account_sync_source` set) — brought back unchanged. The 7-day CSV check is the one that caused the original noise (flagged low-activity savings/goal accounts that are supposed to sit idle) — rather than guess at a heuristic (off_budget flag, naming pattern, etc.) with no way to verify against real account structure, replaced it with an explicit opt-in `_CSV_STALENESS_WATCHLIST` module constant (currently just `{"Bitvavo Crypto"}`) — predictable, one line of code to extend if another account needs it later.

### Verified live
- Simulated a bank-linked, stale account (`account_sync_source` + old `last_sync`) — `bank_sync_stale` appeared correctly, only for that account.
- Renamed a manual test account to match the watchlist entry — `csv_stale` appeared only for that one; the other manual test accounts (which previously all showed up under the old blanket rule) stayed silent.
- Two real vehicle setup-nudge conditions already present in test data (missing APK/insurance dates) now show up in `get_pending_items()` even though their anti-spam window should otherwise suppress them.
- Reverted all simulated account changes afterward.
- `ast.parse` clean, full `majordom-api` rebuild.

### Files modified
- `backend/services/notification_service.py` — `_check_vehicle_reminders(ignore_anti_spam)`, `_CSV_STALENESS_WATCHLIST`, `get_pending_items()` staleness block re-added scoped to the watchlist + bank-linked accounts only

### Lessons
- A widget meant to show "live state, not already-notified state" cannot reuse a digest check's built-in anti-spam as-is — the anti-spam is specifically the thing to bypass, not inherit. Worth checking for this pattern before wiring any future digest check into `get_pending_items()`.
- When a heuristic causes real noise and there's no reliable signal in the data to distinguish the good case from the bad one (and no way to test against the user's real account structure from here), an explicit small opt-in list beats a cleverer-but-unverifiable heuristic — cheaper to reason about, and trivial to extend by hand later.
- Also added to `docs/architecture.md#critical-technical-rules` (rule 14) — this pattern will bite again the next time a digest check gets wired into the Home widget.

**Follow-up, same session:** added `"Crypto card"` to `_CSV_STALENESS_WATCHLIST` — that's the real AB account name for the crypto.com CSV source (established during #118's account auto-suggestion work), alongside `"Bitvavo Crypto"`.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — #95: daily backup cron activated + backup.sh cleanup fix + get_backup_status tool

### Resolved
- **Closed #95 (P0)** — `scripts/backup.sh` and its cron line already existed in DEPLOY.md/docs, but were never actually installed on the production server. Verified live: `crontab -l` initially returned "no crontab for doru"; user added `0 3 * * * .../scripts/backup.sh >> /var/log/majordom-backup.log 2>&1`, reverified active.
- **Real bug found while testing manually:** `backup.sh`'s cleanup (`trap 'rm -rf "$TMPDIR"' EXIT`) failed with `Permission denied` on every run — the alpine container that dumps the Actual Budget volume writes as root, so the tmpdir's contents end up root-owned; the host user can't `rm` them without sudo, silently leaking a root-owned directory in `/tmp` on every single backup (would have accumulated indefinitely). Fixed by running cleanup through a root container too (`docker run --rm -v "$TMPDIR":/backup alpine rm -rf /backup`), same as the dump step. Verified: reran the script, no more permission errors (see architecture.md rule 18 for the general pattern).
- **`get_backup_status`** — new read-only chat tool reporting the latest backup's age, size, and archive count (with a warning if the last one is >2 days old). Requires exposing `./backups/` to the API container (`docker-compose.yml`, new `./backups:/app/backups:ro` mount) and a `settings.backup_dir` (`BACKUP_DIR` env var, defaults `/app/backups`). Not yet deployed/tested live (needs a push + redeploy) — committed, push deferred per the user's "push only when I need to test on real data" policy.
- **Manual offsite copy documented** (`DEPLOY.md`) — local backups in `./backups/` don't survive an LXC-level failure, and full rsync automation to the NAS is deferred (see below). Documented two manual paths: Dolphin's built-in `sftp://` (reusing the LXC's existing SSH server) + the NAS's SMB share for drag-and-drop, or a CLI `scp`+File-Station-upload fallback. Tested live by the user via Dolphin.

### Discussed, deferred by design
- **Full rsync-to-NAS automation** (Option C already scaffolded in `backup.sh`) was scoped out after the user explicitly decided *not* to enable SSH on the Synology NAS for now — reasoned through the security trade-off together (LAN-only exposure, key-only auth, dedicated restricted user, DSM firewall scoping to the LXC's IP would all make it safe, but the user preferred to keep it simple manually for now and revisit later). Manual copy is the interim.
- **A separate "last NAS copy" tracker** (7-day nudge, reusing the `notification_log` table/`log_notification()`/`get_last_notification()` pattern already used for digest anti-spam) was scoped in discussion but the user decided against it — manual copy stays fully manual, no logging tool.
- **User-friendly backup/restore UX** (trigger + restore from chat, not just status) — the user's original motivation for postponing #95 for a long time. Deliberately *not* bundled into this fix; restore is high-risk (overwrites live data, likely needs container stop/start) and deserves its own design pass. Logged as [#136](https://github.com/Dorusto/life-os/issues/136).

### Files modified
- `scripts/backup.sh` — cleanup trap now runs through a root container
- `backend/tools/ops.py` — new: `get_backup_status()`
- `backend/tools/registry.py` — tool registration + dispatch
- `backend/core/config/settings.py` — `backup_dir` setting
- `docker-compose.yml` — `./backups:/app/backups:ro` mount on `majordom-api`
- `DEPLOY.md` — manual offsite-copy steps (Dolphin SFTP/SMB + CLI fallback)
- `docs/architecture.md` — rule 18 (root-owned files from Docker volume dumps)
- `docs/roadmap.md`, `/home/doru/Proiecte-AI/life-os/CLAUDE.md` — removed closed #95 from backlog tables

### Lessons
- A "the script/docs already exist" issue can still be genuinely unresolved in practice — #95 had a complete, well-written script and runbook, but nobody had ever actually run `crontab -e` on the real server. Docs describing a fix and a fix being *active* are not the same claim; `crontab -l` is what actually verifies it.
- A backup script that's never been run for real is exactly where a bug like the root-owned-cleanup issue hides — it would never surface by reading the code, only by actually executing it against the real Docker volume.
- When a user has been avoiding an "easy" ops task for a long time, ask why before just doing the technical thing — here the real blocker turned out to be wanting a fundamentally different (user-friendly, in-app) experience, not the cron line itself. Splitting "close the immediate risk now" from "build the experience you actually want" (as two separate issues, #95 vs #136) unblocked both without conflating them.

### Unresolved
- [#136](https://github.com/Dorusto/life-os/issues/136) — user-friendly backup/restore UX, not scoped yet
- [#16](https://github.com/Dorusto/life-os/issues/16) — broader 3-2-1 backup strategy across all self-hosted services (Immich, Jellyfin, ...), Majordom's offsite piece is manual for now
- Remaining vault-triage issues: #110–#117, #126 — not started

**Follow-up, same session — pushed and verified live:** `get_backup_status` + the docker-compose mount deployed cleanly; asked in chat ("sunt la zi backup-urile?") → correctly reported the latest archive's age/size and count. `backup.sh`'s cleanup fix reverified after the real push+pull cycle (earlier test had been run before the fix was actually pushed) — clean run, no permission errors.

## 2026-07-03 — Backlog sweep: #92, #69, #96, #97

### Resolved
- **#92 (Android PWA help-modal scroll):** user retested live — no longer reproduces. Closed with a note that the real root cause was likely the stale PWA service-worker cache (fixed earlier this session, rule 16) serving an old broken bundle, not the scroll-lock logic itself — reopen if it recurs.
- **#69 (close account from chat):** deprioritized on request — user doesn't plan to use this feature yet. Left open, low priority, no code changed.
- **#96 (dev branch / stop deploying from main):** deprioritized on request — only relevant once a second person uses Majordom; single-user right now. Logged as a proper decision in `docs/decisions.md` (with an explicit trigger to revisit) instead of an ad-hoc memory note — caught myself about to violate this project's own "No auto-memory" rule (majordom-financiar/CLAUDE.md) and corrected before it went further (deleted the wrongly-created memory file, never added to the index).
- **#97 (drop unnecessary actual-budget restart from deploy.yml):** removed `docker compose rm -sf actual-budget || true` — `docker compose up -d` already skips containers with no image/config change. Verified live: pushed, and after the deploy `actual-budget` still showed its pre-existing uptime (22 minutes) while `majordom-api`/`majordom-web` correctly recreated (58 seconds) — confirmed it no longer restarts on every deploy.

### Files modified
- `.github/workflows/deploy.yml` — drop the `actual-budget` removal step
- `docs/decisions.md` — #96 deprioritization decision + trigger

### Lessons
- Caught my own mistake in real time: reached for `~/.claude/projects/.../memory/` out of general habit for a "remind me later" note, even though this exact project's CLAUDE.md explicitly says not to. Worth double-checking project-specific memory rules before writing, not just defaulting to the general auto-memory system.
- "Is this actually fixed, or did it just stop reproducing because something else changed underneath it" (#92) is worth asking before closing a UI bug as fixed — the original scroll-lock code was never touched this session, only the delivery mechanism (service worker) was.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — M5.0: tool domain routing (#98)

### Resolved
- **#98 (tool domain routing):** all 34 tool names in `backend/tools/registry.py` prefixed by domain (`finance__`, `vehicle__`, `system__`) — fixes the confirmed bug where "set Transport la €110" called `rename_category` instead of `propose_set_category_budget`. Added `system__` as a 5th domain (not in the original decisions.md table) for `set_notification_time`/`get_backup_status`, which fit neither `finance` nor `vehicle` — confirmed with the user before implementing. `_build_system_prompt()` in `backend/api/chat.py` restructured into `## Finance tools` / `## Vehicle tools` / `## System tools` sections per `architecture.md#tool-domain-routing`, content preserved 1:1 (renamed only). Frontend required zero changes — confirmation cards route on the `"type"` JSON field, not the tool name (verified via grep before touching anything). Verified live on the local docker-compose stack: the exact bug scenario now calls the right tool, and refuel / plain transaction / real category rename all still route correctly.

### Files modified
- `backend/tools/registry.py` — 34 tool names renamed (schema + dispatch only, Python function names/imports in `actual_budget.py`/`vehicle.py` left unchanged)
- `backend/api/chat.py` — `_PROPOSAL_TOOLS` renamed, system prompt restructured into domain sections
- `docs/decisions.md`, `docs/architecture.md` — `system__` domain documented
- `docs/roadmap.md` — M5.0 marked done, removed from Tier 0 backlog

### Lessons
- A whole-file `\b`-boundary regex rename is dangerous when the same identifier appears in two different roles in one file — here, tool name string literals (safe to rename) and Python `import`/call identifiers (must NOT rename, since the underlying functions in `actual_budget.py`/`vehicle.py` keep their original unprefixed names). Caught it by reading the diff before running the app, not after. Fix: split the file at `async def execute_tool` and only touch the `if name == "...":` literal in the dispatch section, not the `from ... import` / function-call lines.
- Before assuming a live bug report is a regression from the change just made, check the timestamp of the evidence — a blank-response chat bubble the user saw after refresh turned out to be a stale message already sitting in server-side history from roughly nine hours earlier (03:07 vs. 12:09, same day), unrelated to the session's work. Reproducing the same request fresh via direct API call showed the current code works correctly. Filed separately as #137 rather than assuming it needed fixing here.
- Confirmed scope was narrower than the issue text suggested: "check frontend for hardcoded tool names" and "update all confirm endpoints" both turned out to need zero changes once actually grepped — frontend and confirm endpoints (`category_actions.py`, `vehicle_reminder_actions.py`) route on internal `"type"`/`"action"` fields, fully decoupled from LLM tool names. Worth verifying scope claims in an issue against the actual code before planning delegation.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — #137: stop persisting empty assistant responses to chat history

### Resolved
- **#137:** `handleSendText`'s `onComplete` in `frontend/src/pages/Chat.tsx` (~line 346) only checked that an assistant message existed after the user message (`lastAssistantIdx > lastUserIdx`), not that its content was non-empty — so an occasional blank LLM reply (seen as literal `"\n"`) got persisted via `saveChatHistory()` and reappeared as a permanent empty bubble on every reload. Added `&& msgs[lastAssistantIdx].content.trim()` to the save condition. Root cause of *why* the LLM occasionally streams empty content on a follow-up turn wasn't chased — non-deterministic LLM behavior, and the fix at the persistence boundary is what actually stops the permanent-bubble symptom regardless of cause.

### Files modified
- `frontend/src/pages/Chat.tsx` — added non-empty check to the chat-history save condition

### Lessons
- `majordom-web` serves a Docker-built static bundle (Nginx), not a dev server — every frontend change needs `docker compose up -d --build majordom-web` to verify. Confirmed the rebuilt bundle actually contained the fix by grepping the deployed JS for the new code before calling it done, since a stale cached layer would have looked identical from the outside. (Correction, same day: `majordom-api` turns out to have the *same* constraint — see architecture.md rule 19, caught during the #99 audit below after a plain `restart` silently kept serving old code.)
- Already-persisted blank messages from before the fix aren't cleaned up retroactively — explicitly out of scope per the issue, user can clear via the existing trash-icon history-clear action if it bothers them.

### Unresolved
- Remaining vault-triage issues: #110–#117, #126 — not started

## 2026-07-03 — Backlog triage + #99 audit: fix pending_review SQLite violation

### Resolved
- **Backlog triage:** walked the full open-issue list with the user, grouped by theme. Outcomes: #75/#65 deferred (not closed) pending local-first LLM switch-back — decision + trigger recorded in `decisions.md#llm-provider`, comments added to both issues. #110/#111/#112/#113/#114/#124 ("proactive budget intelligence") regrouped into one medium-priority cluster in `roadmap.md`, explicitly below standard-functionality tiers — real but not blocking, pick one at a time once Tier 2/3 work runs dry rather than spreading across sessions.
- **#99 audit (requested: check for other SQLite rule violations before fixing #99 itself):** scanned all 11 tables in `memory.db` against `decisions.md`'s "No financial data in SQLite" whitelist. Found a real violation beyond `merchant_mappings` (#99 itself, which is actually whitelisted by name — #99's real argument is architectural redundancy, not a rule violation): `pending_review` stored `amount`/`merchant`/`date`/`category_name` — genuine duplicated financial data — but grepping every reader confirmed only `financial_id` and row `count` are ever used (`notification_service._check_pending_review`, for the "N transactions need category review" nudge text). Fixed: dropped the four unused columns from the schema (`ALTER TABLE ... DROP COLUMN`, SQLite 3.46 supports it natively), simplified `add_pending_reviews()` to take a plain `list[str]` of financial IDs instead of full row dicts, updated the one caller in `csv_import.py` to match. `category_keywords` is the same spirit as `merchant_mappings` (categorization aid) but isn't literally named in the decisions.md whitelist text — minor doc gap, not a code fix, left as-is.
- **#99 itself (merchant_mappings redundancy) is still open** — the audit only found and fixed the unrelated `pending_review` issue it surfaced. #99's actual scope (replace merchant_mappings with AB rule/history) is untouched, stays in Tier 2.
- **Triage continued (reconciliation, vision, and the vehicle-manager decision):** #116 moved into the proactive-intelligence cluster (Home already surfaces pending items, this is a lower-urgency add-on, not a gap). #115/#117 deferred, not a priority. #120 (own-account transfer linking) reclassified from "paused" to "needs more thought" — AB already does this when set up manually, the real open question is *how* to trigger it from Majordom, not whether it's wanted. #86/#80/#81 (vision/OCR quality) grouped with #75/#65 under the local-first deferral. #41/#42 moved into the proactive-intelligence cluster — same "notice something the user would otherwise miss" shape as #110-114/#124/#116.
- **Vehicle-manager extraction activated** (was "deferred, no active trigger" in `decisions.md`): #79 (vehicle list/deactivate) and #134 (fuel/vehicle charts) both need the same thing — clean API access to vehicle data instead of raw SQLite queries — which is exactly the trigger the original decision was waiting for. Opened [#138](https://github.com/Dorusto/life-os/issues/138) scoped explicitly to *internal* modularity only (own FastAPI service + own DB, REST + MCP-friendly tool layer, reusable pattern for #58): no dedicated UI, no public product, no multi-user auth — that's a separate, undecided future question the user was explicit about not wanting to bundle in now. Sequencing agreed: #93 (code audit) first, then #138, then #79/#134 as thin API consumers. Commented on #79/#134 linking them to #138; updated `decisions.md#vehicle-manager` (trigger activated) and `roadmap.md` (M6.1 now points at a real issue instead of "not scheduled").
- Root `/home/doru/Proiecte-AI/life-os/CLAUDE.md`'s "Current priorities" section had drifted badly (still listed #92/#96/#97 as open, both closed weeks ago; #110-117/#120/#126 as one undifferentiated blob now organized properly in `roadmap.md`). Rewrote it as a short pointer to `roadmap.md#next-session--start-here` plus a compact "what's actually next" summary, instead of a second full list that goes stale independently — the drift itself was the evidence this format doesn't hold up.

### Files modified
- `backend/core/memory/database.py` — `pending_review` schema (4 columns dropped), migration for existing DBs, `add_pending_reviews()` signature simplified
- `backend/api/csv_import.py` — updated the one caller of `add_pending_reviews()`
- `docs/architecture.md` — new critical rule 19 (`majordom-api` needs `--build`, not `restart`, for Python changes)
- `docs/decisions.md` — local-first target + revisit trigger added to the LLM-provider decision; vehicle-manager extraction trigger activated
- `docs/roadmap.md` — proactive-intelligence cluster regrouped (now includes #41/#42/#116), Tier 2/3 reorganized around #93 → #138, deferred sections split into "local-first switch-back" vs "opportunistic"
- `/home/doru/Proiecte-AI/life-os/CLAUDE.md` — "Current priorities" rewritten as a pointer instead of a duplicated, drifting list

### Lessons
- **`majordom-api` has the exact same build-vs-restart trap as `majordom-web`** (see the correction above in the #137 entry) — only `./data` and `./backups` are bind-mounted in `docker-compose.yml`, backend source is `COPY`'d into the image at build time. `docker compose restart majordom-api` restarts the *old* image with zero error or warning; the container looks perfectly healthy the whole time. Caught it here because a schema migration I'd just written didn't appear to run — `docker exec majordom-api grep ... /app/backend/...` against the live container is the fast way to confirm what code is *actually* running before trusting a restart. Genuinely worrying in retrospect: earlier today's #98 verification happened to work only because an unrelated `--build majordom-web` run also recreated `majordom-api` as a side effect — if that coincidence hadn't happened, #98 could have been "verified" against stale code without any signal something was wrong.
- When asked to audit for rule violations, checking who actually *reads* a suspicious field (not just who writes it) turned a "how do we migrate this data" question into a much simpler "these columns are provably dead, just drop them" — `pending_review`'s amount/merchant/date/category_name were written on every import and never read anywhere.
- Grouping related-but-not-identical backlog issues (the 6 "proactive intelligence" ones) as one explicit cluster with a shared priority rationale, instead of independently ranking each by size, made a fast triage decision possible — the user's framing ("these matter, but after standard functionality, and we'll keep discovering the split as we go") is a better fit for genuinely fuzzy-scope work than trying to force a precise per-issue ranking now.

### Unresolved
- #99 (merchant_mappings → AB rule history) — audit done, fix not started, stays Tier 2
- Full backlog organized into Tier 2/3 + proactive-intelligence cluster + deferred sections — see `docs/backlog.md` (split out from `roadmap.md` later this same day, see below) for what's next, not a separate list here

## 2026-07-03 — Doc cleanup: split backlog.md out of roadmap.md, track root CLAUDE.md

### Resolved
- **Split `docs/roadmap.md` into two files:** the user pointed out the backlog (issue-level, churns every session) didn't conceptually fit alongside milestones (big, stable pillars) in the same file. Moved the whole "## Backlog" section — Tier 0-3, proactive-intelligence cluster, deferred lists, feature-ideas table — into a new `docs/backlog.md`. `roadmap.md` now holds only milestones (M0-M6) + the hardware recommendation, with a one-line pointer to `backlog.md`. Also found and removed real duplication while moving it: three old per-session appendix tables ("Proactive budget intelligence (2026-07-02 session)", "Import & reconciliation robustness", "Other findings") repeated issues already listed in the Tier tables above them, one with a stale "blocked by #101" note (#101 was fixed earlier the same week) — folded their useful extra detail into the main table rows instead of carrying two copies forward.
- **Updated every cross-reference** rather than leaving the split half-done: `docs/INDEX.md` (2 rows), `majordom-financiar/CLAUDE.md` (4 spots — task-type table, end-of-task checklist, key references), root `CLAUDE.md` (2 spots), `docs/learn/13-starting-a-task.md` (2 spots — task-picking step now mentions backlog.md as the practical day-to-day source, closing-checklist step). Historical `docs/sessions/*.md` entries left untouched — they're an append-only log of what was true when written, not a living doc.
- **Root `CLAUDE.md` now tracked in git** (was gitignored since April, when it held financial data directly — that moved to `PRIVATE_context.md` long ago, same precedent already applied to `majordom-financiar/CLAUDE.md` in June). Re-verified current content had zero sensitive data before un-ignoring. Also fixed a stale "(gitignored)" note about `majordom-financiar/CLAUDE.md` in `architecture.md`'s project-structure diagram — it was already wrong before today, just never noticed.

### Files modified
- `docs/roadmap.md` — trimmed to milestones + hardware, backlog section removed
- `docs/backlog.md` — new file, consolidated backlog (no more duplicate appendix tables)
- `docs/INDEX.md`, `CLAUDE.md`, `docs/learn/13-starting-a-task.md`, `docs/architecture.md` — cross-references updated
- `/home/doru/Proiecte-AI/life-os/CLAUDE.md`, `/home/doru/Proiecte-AI/life-os/.gitignore` — root CLAUDE.md un-ignored and tracked, references updated

### Lessons
- Doc restructuring is exactly the kind of change where "update the one file the user pointed at" and "actually done" are different bars — a grep across the whole `docs/` tree for the old filename before considering it finished caught four files that would have quietly gone stale otherwise (INDEX.md, both CLAUDE.md files, the starting-a-task guide).
- Moving content is also a good moment to notice duplication that's been sitting there unnoticed — the three redundant appendix tables in `roadmap.md` had existed for a while (one referencing an issue, #101, that was already fixed days ago) but nobody had reason to touch that part of the file until now.

### Unresolved
- None from this entry.
