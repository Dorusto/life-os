## 2026-09-23/24 — unified UI across Finance, Transport, Invest (#283, #238) + market data (#292)

### Resolved
- **Design direction** agreed on a mockup (claude.ai design canvas): Wealthfolio reference, graphite palette, neutral brand (light pill), per-app accent, IBM Plex Mono labels/figures, Instrument Serif for headline statements and the serif "M" logo. Decision entry: `decisions.md#unified-ui-shared-frontend`.
- **Shared frontend (option B, generated copies):** `packages/frontend-shared` now holds tokens, BrandMark (+ generated favicon/PWA icons), the shell (AppShell rail + 5-button mobile bar with chat in the middle, More sheet, Page wide/narrow, PageHeader, DomainTabs, AppearanceSettings), the content kit (Card, HeroValue, StatStrip, ListRow, ProgressBar, Segmented, AreaChart/BarChart/GroupedBarChart/StackedBar/DonutChart/BarList) and privacy mode. One Python sync script + pre-commit drift check.
- **Apps migrated:** all three on the shared shell/header; Finance dashboard rebuilt (hero, stat strip, 12-column grid, Spending widget), Finance `Chart.tsx` renders through the kit (dashboard, analytics, chat chart cards), new `/net-worth` page; Invest and Transport pages on the kit; Transport vehicle value section rewritten (#210).
- **#308** hide-amounts toggle (eye icon in rail and mobile top bar); one number format (nl-NL) in all apps.
- **#292** Invest market data: Yahoo (keyless) primary, Twelve Data optional fallback, no blocking sleeps on the request path, httpx no longer logs the API key, XTB tickers mapped to Yahoo symbols, pence and foreign listing currencies converted.
- **#305** Settings → Connections shows a real Invest health status. **#304** runner watchdog workflow (every 30 min on a GitHub-hosted runner).
- Private-data checker: the licence-plate regex no longer matches Tailwind class runs.

### Gotchas
- Aider reviewer (glm-5.3-flash) often returns UNPARSED on large diffs; gates + visual check still ran. Review diffs yourself before merging.
- Local Actual Budget intermittently answered 5xx during the night (another session was reconciling #303); visual checks flagged those as page failures — they are environment, not UI.
- `/api/transactions` caps `limit` at 200; the Spending widget pages through it.

### Open
- Transport local `Card`/`ChartSection` still imported by a few pages; Finance chat action cards and review pages not yet on the kit.
- Deferred features: retirement Monte Carlo / what-if planner, region/sector insights (need a data source), Ctrl K search (#307).
- Rotate the Twelve Data API key (was logged in plain text).

## 2026-09-23 — first autonomous-pipeline batch: 8 branches merged, overlay fixed

### Resolved
- **#302** "Ask Majordom" icon on transaction rows → chat prefilled, not sent.
- **#301** own-account handling, split in two specs:
  - 301a: `POST /income/sources` transfer mode retro-converts existing uncategorized transactions for the payee (`convert_uncategorized_by_payee_to_transfer`). A candidate with a likely counterpart leg already in the target account (negated amount, ±3 days) is skipped and reported, never converted, to avoid the payee-editing duplicate pitfall noted on #120.
  - 301b: uncategorized groups carry `own_account` (existing transfer rule > account name as a whole word in the payee > generic savings keyword) and `interest_category`. The Inbox card shows an OwnAccountPanel with Convert to transfer / Interest income, and the chat tool presents it.
- **#299** Settings → FIRE excluded accounts (GET/PUT `/api/fire/excluded-accounts`, validated).
- **#296 step 1** the Add → Photo flow was dead (sessionStorage handoff never set). ReceiptFlow is now a portal popup opened from AddButton (photo and manual), and the `/receipt` route is gone. Steps 2 (chat) and 3 (vehicle-manager) are still open.
- **#293** Invest Settings states the real key source (a saved key wins over the env var) and lists failing symbols (`market_data_errors`). The reviewer also found and fixed an API-key leak: httpx error strings contain the request URL with `apikey=`.
- **#291** Transport Settings → Data: per-vehicle CSV export + Import from Fuelio.
- **#283 slice 1** 11 chat action cards moved onto `ui/Card variant="bubble"` (no visual change).
- **#297** docs now describe the shared single-worker executor.
- **#300** `scripts/ab_compat_check.py` + weekly workflow `ab-compat-check.yml`. Local result: actualpy 0.22.3 round-trips fine against actual-server 26.9.0, including a sync write on an existing file. That contradicts the 400 reported on 2026-09-15; that failure may need a budget created by the AB web client to reproduce.
- **#298** needs no code: the hook window already matches, and the pipeline backdates commits into it.

### Files modified
Merged branches: backend/core/actual_client/client.py, backend/core/finance/{provider,actual_budget_provider,fire}.py, backend/api/{income_sources,home,chat,fire_settings}.py, backend/main.py, frontend/src/{lib/api.ts,pages/{Transactions,ReceiptFlow,Settings}.tsx,App.tsx,components/{AddButton,IncomeSourceCard,CategoryActionCard,OwnAccountPanel,+11 cards}.tsx}; tools/vehicle-manager/{app/{main,database}.py,frontend/src/{lib/api.ts,pages/Settings.tsx}}; tools/investment-manager/{app/{main,market_data}.py,frontend/src/{lib/api.ts,pages/Settings.tsx},tests/test_settings.py}.
Direct: .dockerignore (new), docs/{architecture.md,learn/01-async.md}, scripts/ab_compat_check.py (new), ../.github/workflows/ab-compat-check.yml (new).

### Lessons
- **The first backend task through the pipeline exposed that the overlay never worked.** `docker compose build majordom-api` failed on a root-only file in `data/`, and `run.py` ignored the exit code, so gates and the visual check tested the old container and produced a false FAIL. Fixed in the pipeline (build/up/health failures are fatal) and at the source (`.dockerignore`).
- **`Dockerfile.backend` does `COPY . .`.** Without `.dockerignore`, the image on the LXC carried `.env`, `data/` and gitignored private notes. The next deploy rebuild drops them.
- **The private-data scanner's credential regex has false positives** on `x = _func()` and `os.environ.get(...)` values (it exempts only `os.getenv(`). Worked around both times; the regex itself is unchanged.
- **Twelve Data cannot serve this portfolio on the free plan.** `.DE`/`.US` suffixes are invalid symbols, and XETRA ETFs require the Grow plan. Findings are posted on #292; the source choice belongs to #42.

### Unresolved
- #292 market-data source (product decision, #42); #296 steps 2–3; #303 (needs LXC data); #283 remaining slices; #210, #260.
- The branches were merged and pushed on the user's explicit request, before their own in-app check.

## 2026-09-23 — deploy runner outage fixed; #296 step 2 (chat receipt → shared popup)

### Resolved
- **Deploy runner offline for 4 days.** On 2026-09-19 a mistyped `chmod 600 / root/.ssh/authorized_keys` (extra space) set `/` to 0600 on the deploy LXC. Every non-root service died (runner, `systemd-networkd`). Docker runs as root, so the app kept working until the 2026-09-23 reboot, which left eth0 without an IPv4 address. Fixed with `chmod 755 /` and restarts, and the queued deploy then ran. #304 opened: alert when the runner is offline.
- **#296 step 2** a receipt photo in chat opens the shared `ReceiptFlow` popup. The popup gained fuel mode (FuelReceiptCard inside, with a Fuel/Grocery tab both ways) and the "Save as rule" checkbox. After a save, chat keeps one status line, or the refuel stats bubble for fuel. `ReceiptCard.tsx` and the `'receipt'` chat role are gone. The Add button now handles fuel receipts too; before this they were saved as plain transactions. The text-mode `fuel_log` card stays inline and lost its Grocery tab.

### Files modified
frontend/src/pages/{ReceiptFlow,Chat}.tsx, frontend/src/components/ReceiptCard.tsx (deleted).

### Lessons
- Pipeline: the reviewer reported "FIXED: deleted ReceiptCard.tsx", but the deletion never reached a commit, and the report flagged it only as "coder left untouched". Check `git diff --stat` against the report's claims.
- `pre-commit-review` was skipped because the whole diff was read in-session and the user confirmed it works.

### Unresolved
- #296 step 3 (vehicle-manager). The session index row is pending: `INDEX.md` has unrelated uncommitted edits in the working tree.

## 2026-09-23 (late) — #303 diagnosed live on the LXC: root cause is an upstream actualpy bug, not Majordom's code

### Resolved
- **#303 root cause found and confirmed, not guessed.** Read-only investigation against real LXC
  data (`scripts/ab_audit.py`, plus a direct read-only call to `Actual.bank_sync_transactions()`)
  found `ING current` was missing 53 real transactions (a full week, Sept 1-6, entirely absent).
  Root cause: every `run_bank_sync()` call for this account throws `pydantic.ValidationError` on
  Enable Banking's `XPCD` balance-type code, which `actualpy`'s `BalanceType` enum doesn't
  recognize — silently swallowed by `run_bank_resync_all()`'s per-account `try/except`, so sync
  "runs" with no visible error but imports nothing. Confirmed upstream and unfixed even in the
  latest release (0.22.3): [bvanelli/actualpy#208](https://github.com/bvanelli/actualpy/issues/208).
  Decision: track upstream, don't build a local workaround now (see `docs/decisions.md#303-actualpy-enable-banking-incompatible`
  for the full reasoning) — the user's own bank-sync button in Actual Budget's native UI works as
  a fallback (different code path, native sync). After the user ran it, `ING current`'s AB-vs-real
  gap dropped from ~€1982 to ~€96.50.
- **The remaining ~€96.50 gap fully explained:** one manual-vs-synced duplicate (`Tankstelle
  M.Weber` €96.50 vs. `SHELL 2508` €96.50, same day) — the app's own existing duplicate-review
  machinery (`#181`/`#229`) correctly found it, but it was buried in 132 false positives.
- **Fixed the false-positive source:** `_find_duplicate_candidates()` had no date window at all
  (an explicitly-flagged, accepted trade-off from #229 — "revisit only if this turns out noisy in
  practice"). Added a 7-day window, same convention already used by `ab_audit.py`'s `dupes()`.
  Reduced 134 candidates → the 2 real ones on live data. Commit `63521dc`.
- **#306 filed** (not fixed): Connections page guesses the wrong port (`${origin}:5006`) on this
  Tailscale-`serve` deployment, where the correct port is different (found live while the user was
  trying to reach the Actual Budget UI directly — unrelated to #303's cause).

### Files modified
`backend/core/actual_client/client.py` (`_find_duplicate_candidates`), `docs/decisions.md`.

### Lessons
- **A fork reported "done" after 0 tool calls and ~4 seconds** (the exact #254-documented failure
  mode from `plan-feature`'s own gotcha list) — caught immediately via `git worktree list`/`git
  status` before trusting the report, and the fix was implemented directly instead.
- **`_find_duplicate_candidates()`'s "no date window" trade-off note in `docs/decisions.md#229`
  was a prediction that came true** — worth remembering that "revisit if X" notes in this file are
  live tripwires, not just historical color; this session hit exactly the condition it named.
- Diagnosing a "balance doesn't match" bug by reading `_run_bank_sync_account`'s and
  `bank_sync_transactions`'s actualpy source directly (not just Majordom's wrapper code) was what
  actually found the root cause — the local wrapper's own error handling hid it.

### Unresolved
- **#303 stays open**: the ~53 missing September transactions (now mostly recovered via the user's
  native-AB sync) still need a final user-confirmed reconciliation pass; August's ~€280 discrepancy
  found earlier in the session was never verified line-by-line; the upstream actualpy issue is
  unfixed (tracked via #208, not by us).
- #306 (wrong Connections-page port) not yet fixed.
- Commit `63521dc` not yet pushed to the LXC — needs the user's explicit push confirmation.

## 2026-09-24 (past midnight) — #303 fully reconciled; #309 shipped in two iterations, ended as a button

### Resolved
- **#303 closed for real:** the user ran Actual Budget's own native bank-sync button directly
  (bypassing the broken `actualpy` code path). It correctly pulled in the full missing week plus
  the rest of September — `ING current` now matches the real bank balance exactly (€19.59). The
  one remaining duplicate (Tankstelle M.Weber vs. Shell 2508) was merged via the review screen. No
  CSV import was needed — the user correctly pushed back on that suggestion as not a repeatable
  fix; the native sync working was the actual answer.
- **#309 shipped, then corrected once live:** first iteration added a 7-day date window to
  `_find_duplicate_candidates()` — fixed the far-apart false positives, but live data on the real
  account still showed 17 false positives, all `kind: transfer` (already-resolved, bank-confirmed
  transfer legs matched against unrelated same-amount transactions within the window). Fixed with
  the trade-off `docs/decisions.md#229` had already predicted: gate transfer-kind candidates on
  lacking their own `financial_id`.
- **#309 also added an LLM category-suggestion fallback — then redesigned into a button.** First
  version ran automatically (up to 20 concurrent LLM calls per page load) when history + notes
  both found nothing. Live-verified working (correct guesses: Easy Park→Transport, Ninos
  Gelato→Restaurants, Knaus Campingparks→Lodging) but also live-verified *silently flaky* — one
  real call returned empty LLM content, invisible to the user, no retry. The user asked for a
  button instead ("vreau sa am control, nu sa faca singur"); redesigned same session:
  `get_uncategorized_groups()` no longer calls the LLM at all, a new
  `POST /home/uncategorized/suggest-category` endpoint is called only by an explicit "Suggest
  category" button, with a real loading/error/retry state.
- **Editable AB-rule-text field (#309's original bug) shipped**: the "Create AB rule" checkbox now
  has an editable input, defaulting to the same truncated suggestion as before but overridable —
  confirmed the override reaches `create_payee_rule()`.

### Files modified
`backend/core/actual_client/client.py`, `backend/api/{category_actions,home}.py`,
`backend/core/finance/{actual_budget_provider,provider}.py`,
`frontend/src/components/CategoryActionCard.tsx`, `frontend/src/lib/api.ts`, `docs/decisions.md`.

### Lessons
- **Both delegated tasks tonight had their automated review pass return the identical hallucinated
  response** — glm-5.3-flash answered "I need you to add the file containing `get_factorial()`,"
  a completely unrelated fake request, verbatim across three separate review calls (two different
  tasks). The actual prompt file had no such text anywhere in it — confirmed by grep. Not random
  flakiness (temperature-0 determinism reproducing the same wrong answer on the same prompt shape
  is more likely than three independent hallucinations landing on identical wording) — a real,
  reproducible weakness of this reviewer model on this prompt shape, worth a `delegate-by-complexity`
  note if it recurs a third time. Manual review caught what the automated pass didn't, both times.
- **A private-data scanner false positive from ordinary HTTP-error docstring prose** — a one-letter
  word immediately followed by an HTTP status code immediately followed by a three-letter word
  case-insensitively matched the license-plate regex's `[A-Z]{1,2}\s\d{2,3}\s[A-Z]{2,3}` alternative.
  Reworded per the scanner's own documented guidance rather than `--no-verify`.
- **A stray untracked `frontend/node_modules` symlink (left by the delegation pipeline's visual-check
  setup, target path containing the user's real username) tripped the same scanner on `git commit
  --amend`** — not part of the actual diff, just local clutter confusing what the hook scans.
  Removing the untracked symlink resolved it.
- **A burst of ~15 OpenRouter calls the user spotted in their usage dashboard, right around this
  session's own testing window, was correctly identified as Claude's own verification calls** (curl
  tests against `_suggest_category_with_llm` plus the now-removed automatic bulk-suggestion path's
  earlier testing) — confirmed clean by live-tailing the LXC's real traffic for 20s and seeing
  exactly one `/suggest-category` call, matching the user's own button click in real time. Worth
  remembering: local/dev testing against a shared LLM API key shows up in the same production usage
  dashboard the user watches for cost — flag this proactively next time before running several test
  calls in a row, rather than letting the user notice and ask.

### Unresolved
- #306 (wrong Connections-page port) still open, still unfixed.
- August's ~€280 discrepancy (flagged earlier this session) never verified line-by-line.
- The upstream `actualpy`/Enable Banking bug (actualpy#208) remains unfixed — tracked, not ours to
  fix locally; revisit if it stalls upstream or the native-sync workaround becomes painful.

## 2026-09-25/26 — Mobile UI batch: receipt popup scroll, Add camera, chat composer, shared kit Button, Settings accordion

### Resolved
- #311 receipt popup had two nested scroll containers (outer `h-dvh` + form `flex-1 overflow-y-auto`
  under a fixed 45vh photo) — on phones the form shrank to a strip. Now one scroll container, photo 35dvh.
- #312 Add sheet: "Take photo" (`capture="environment"`) + "Choose from gallery", one shared handler.
- Content hidden under the phone tab bar on every Finance page: `App.tsx` gave `<Page>` `h-full` for all
  routes (needed only by Chat), capping long pages so `<main>`'s `pb-28` landed before the content end.
  Chat-only now. Chat: bubbles wrap long unbroken strings (was 1045px wide), composer sits on the tab
  bar, Send icon `text-on-brand` (was `text-ink` = same color as the brand fill → invisible arrow).
- #283 (partial): `Button` promoted to `packages/frontend-shared/kit` (was duplicated in Transport and
  Invest), pill-shaped per the unified-UI decision; Finance buttons migrated; chart palette →
  shared `SERIES_COLORS` (follows the accent); Settings: duplicate Appearance removed, toggle knob
  visible in dark mode, short sections open inline (accordion, one at a time), long lists stay pages,
  inert "Disconnect Actual Budget" removed. DomainTabs/PageHeader: same compact row on every home
  page, header actions get a full row on phones, Add icon-only on phones; Transport rail bell aligned.
- Issues opened: #310 (unified shell + single login, from the user's proposal; plugin-style
  add/remove modules added as a comment), #313 (Payees page read-only), #314 (build commit/date
  instead of hand-written "2.0.0").

### Files modified
frontend/src/{App.tsx, pages/{ReceiptFlow,Chat,Settings,ImportPage,Login,AbSetupWizard,Dashboard,
NetWorth,Transactions}.tsx, components/{AddButton,CsvImportCard,FuelReceiptCard,IncomeSourceCard,
SetupBalancesCard}.tsx, lib/chartColors.ts}; ../packages/frontend-shared/src/{kit/Button.tsx,
shell/{DomainTabs,PageHeader}.tsx} + generated copies; ../tools/{vehicle-manager,investment-manager}
Button imports; ../scripts/sync_shared_frontend.py; CLAUDE.md (visual checks via the pipeline).

### Lessons
- Delegation pipeline: the visual verdict call hung 15+ min (urllib timeout is per read, not total) and
  the glm review returned UNPARSED on 3 of 5 runs despite having the files — fixed in the skill
  (`visual.py` wall-clock deadline + retry; review prompt states files/diff are present; review
  context trimmed). Diffs were reviewed manually + a pre-commit-review subagent pass.
- Testing Transport/Invest from the phone against the local stack needs them on the LAN — the tracked
  compose binds them to 127.0.0.1; a gitignored `docker-compose.override.yml` (`ports: !override`) does it.

### Unresolved
- #283 remaining: other Finance components not yet on the shared kit (CategoryActionCard, NotificationBell,
  GoalsSection, BudgetDashboard, Chart.tsx, ReceiptFlow Confirm button).
- Settings keeps 10 now-unreachable `SUBPAGE_TITLES` entries (harmless). Invest "Performance" not
  verified by Claude (needs the user's Invest login).
