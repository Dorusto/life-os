# 2026 — Week 36

## 2026-09-01 — #172 closed for real: root cause was a missing `temperature`, not the prompt bullets

### Resolved
- **#172** (chat fabricates plausible-looking financial data instead of calling the real tool) — the two prior partial fixes (7cf4d71: trigger bullet, fuzzy category match) weren't enough. Live re-testing "show me my uncategorized transactions" 3x fresh, identical phrasing, still reproduced real fabrication (invented payees "Wolt"/"Bolt" not present anywhere in the real 33-group data) and a second failure mode (tool called correctly, but the final text ignored the result and said "I'll fetch the uncategorized transactions now"). Root cause: `backend/api/chat.py`'s LLM payload had no `temperature` at all — the only LLM call in the codebase missing one. Fixed with `"temperature": 0.2`; live-retested 3x fresh afterward, 3-of-3 clean. Full writeup: `docs/decisions.md#172-fabrication-root-cause-missing-temperature`.
- **#243/F11** — 9 of 49 registered tools had zero system-prompt bullet at all (`finance__get_accounts`, `get_monthly_stats`, `get_spending_history`, `get_transactions_by_tag`, `system__get_backup_status`, `set_notification_time`, `vehicle__get_vehicle_log`, `list_vehicles`, `set_vehicle_type`). Added all 9, verified mechanically (zero gaps left) and live (`get_accounts` tested in browser, matched real Actual Budget data exactly). **Note: #243 itself was mistakenly closed then reopened same session** — the bullets are a real fix but the issue's actual ask (a mechanical pre-commit check, next to `check_provider_wiring.py`, so a 10th gap can't recur silently) was never built. Still open.

### Files modified
`backend/api/chat.py` (9 new bullets + `temperature: 0.2`); `docs/decisions.md` (new entry); `.claude/skills/plan-feature/SKILL.md` (new known-gotcha: check `temperature` before reaching for a new bullet or a heavier mechanism on any future "LLM ignored a clear instruction" report).

### Lessons
- **Check `temperature` first on any future report shaped like "the LLM ignored a clear instruction."** It reproduces exactly like a flaky-prompt bug (same input, different outcome each time) and is far cheaper to rule out than writing a new bullet or building a forced-`tool_choice`/retry mechanism. `backend/api/chat.py` was the only LLM call in the whole codebase with no `temperature` set — every other one (vision, CSV detection, notifications) sets it explicitly.
- **Don't trust a single live trial.** The first uncategorized-transactions test (pre-fix) looked like a clean pass at first glance in one run and a total fabrication in the next, same phrasing, fresh chat each time — a single test would have reported "fixed" wrongly in either direction. 3 fresh trials was the minimum to see the real pattern (and to later confirm the fix).
- **Closing an issue on "the code now matches the fix I applied" isn't the same as closing it on "the issue's actual ask is done."** #243 asked for a mechanical check; closed it because the 9 bullets it *found* were fixed, without re-reading that the check itself — the actual deliverable — was still unbuilt. Caught immediately, reopened same session, no real harm, but worth remembering: re-read the original ask right before closing, not just what got fixed.
- Pre-commit review caught a real mistake before it shipped: a new example bullet I wrote copied a real vehicle nickname ("Wabi Sabi") from a pre-existing example elsewhere in the same file, instead of the established fictional placeholder (`MyCar`/`MyBike`) — architecture.md rule 21 (`docs/architecture.md` "never use real user data as few-shot examples"), same category as the original #139 incident. Fixed before commit. The pre-existing occurrence (not part of this diff) is now tracked in #244.

### Unresolved
- **#243** — mechanical pre-commit check for tool-bullet coverage, not yet built.
- **#244** (opened this session) — `vehicle__set_vehicle_type` and `system__set_notification_time` bypass the confirmation-card rule (architecture.md rule 6), executing writes directly with no proposal card. Pre-existing, not introduced this session, but the new bullets now explicitly assert "no confirmation needed" for both. Open product question: are these similar enough to `sync_accounts`'s existing "safe/reversible → skip the card" exception to formalize the same way, or do they need real cards? Also carries the pre-existing "Wabi Sabi" real-name example at `chat.py:166`.
- The minor cosmetic artifact seen once in live testing ("(Fetching the data...)" narration leaking before real data) — not fabrication, not chased further; only worth revisiting if it recurs.

## 2026-09-01 — #241: NotificationBell + Settings reach from the Majordom tab header

### Resolved
- **#241** — `Chat.tsx`'s `PageHeader` had only 2 icon buttons (Clear history, Help); adding `NotificationBell` + `Settings` for parity with every other tab (`StandardHeaderActions.tsx`) would have made 4 always-visible icons, more than anywhere else in the app. Presented Doru 4 concrete UI options (`AskUserQuestion`, with ASCII previews) per the CLAUDE.md architecture-trade-off rule; he picked the overflow-menu option (his own framing from when #241 was opened). Clear history + Help moved into a new `⋮` "More options" `BottomSheet` (list-row style copied from `NotificationBell`'s own sheet); `NotificationBell` + `Settings` now sit directly in the header, matching the other tabs' icon count exactly.
- Confirmed the `BottomSheet`/`BottomNav` z-index stacking risk noted in `docs/sessions/2026-W28.md` (both were `z-50`, latent tap-interception risk for bottom-anchored interactive content) no longer applies — `BottomSheet` was already fixed to `z-[60]` since then. No new fix needed, just verified before reusing the pattern.

### Files modified
`frontend/src/pages/Chat.tsx`; `CLAUDE.md`, `docs/product-plan.md` (Phase C sequence updated — #241 shipped, #117 next).

### Lessons
- None new — straightforward single-file frontend change, live-verified in-browser (overflow menu, notification bell, Settings navigation) on the local fixture stack before commit.

### Unresolved
- None from this task. Next in the Phase C sequence: #117 (assisted reconciliation).

## 2026-09-01 (cont'd) — Phase C closed: #117 and #41 shipped, plus an unplanned perf interrupt

### Resolved
- **#117 closed.** New read-only tool `finance__get_reconciliation_suspects` (uncleared transactions,
  recent transactions, duplicate-pair candidates, `likely_match` flag on exact-amount matches) now
  runs before `finance__propose_balance_adjustment` — the adjustment stays the last resort, never
  the first response to "my balance is wrong." Shared `cleared == False` filter (previously
  duplicated in `list_unreconciled_groups()`/`mark_account_reconciled()`) extracted into
  `_unreconciled_filter_clauses()`. `propose_balance_adjustment` also got its first-ever
  system-prompt bullet — it had none before, a real pre-existing gap (same class as #160/#166).
  Dispatched to Aider/DeepSeek Pro, reviewed line by line, then **live-verified via an actual chat
  conversation** on the local fixture stack: claimed a wrong ING balance, watched the assistant
  investigate first (15 uncleared, 10 duplicate pairs, none matched), report the exact gap, and
  offer the adjustment as one option among several — the card only appeared on explicit follow-up,
  exactly as the issue asked.
- **#41 closed, rescoped** per `docs/product-plan.md` Phase C item 4 — a real create/deactivate
  lifecycle, not the original "monthly review notification" text. Backend (wave 1, Aider/DeepSeek
  Pro): `find_recurring_candidates()` (payee+account groups, 3-of-last-4-months present, amounts
  within ±5%, excluding anything already schedule-linked) and `find_stale_schedules()` (active
  Schedules overdue per AB's own `SchedulesNextDate.local_next_date` — no custom recurrence math
  needed). Wired the previously-orphaned `create_schedule()` (existed, zero callers, not on
  `FinanceProvider`) plus a new `set_schedule_active()`. **Live-tested with synthetic data**
  (3 recurring transactions created → detected → `create_schedule()` called → found it landed
  **inactive** — actualpy's own `create_schedule()` never sets `Schedules.active`, column defaults
  to 0, and the orphaned wrapper never had either. Fixed (`sched.active = 1`), reverified, cleaned
  up). Frontend (wave 2): touched 5 tightly-coupled files including `CategoryActionCard.tsx`'s
  non-obvious multi-variant convention, so **implemented directly via a fork** instead of delegating
  (CLAUDE.md's explicit rule for this case), not dispatched blind. Live-verified end to end in an
  actual browser: created synthetic recurring transactions, confirmed the `create_schedule` card,
  verified `active=true` server-side; forced a schedule overdue, confirmed `deactivate_schedule`,
  verified `active=false`. All synthetic data tombstoned after both rounds, no trace left.
- **Unplanned priority interrupt: Home page load ~20-25s on refresh.** Doru flagged it mid-session
  right after the #117 rebuild. Traced via live log timing analysis (not guesswork): #223's 4s
  read-connection cache only ever covered 8 specific methods; `list_unreconciled_groups()` and
  `list_budget_realism_flags()` (shipped after #223) were still on `_get_actual()`, never migrated.
  Switched both to the cache (mechanical, same proven #223 pattern) — re-logins per Home load
  dropped from 15+ to ~7, but total time barely moved, because the real constraint is the single
  `_actual_lock` serializing ~10 read calls per page load, and 4s wasn't enough runway for the whole
  queue. Raised `_READ_CACHE_TTL_SECONDS` 4.0 → 20.0 — re-logins dropped further to ~4, most calls
  now cluster within milliseconds instead of 6-7s apart. Remaining ~20s is #227's already-documented,
  still-open compute cost inside `get_home_data`/`get_budget_status`, not connection overhead — filed
  the follow-up as **#245** rather than chasing it further mid-interrupt.
- Both new architecture.md gotchas (rules 35, 36) written up so neither recurs silently next time a
  Home-page endpoint or an `actual.queries` create-helper is added.

### Files modified
`backend/{core/actual_client/client.py, core/finance/{provider,actual_budget_provider}.py,
tools/finance/actual_budget.py, tools/registry.py, api/{chat.py, home.py, category_actions.py}}`,
`frontend/src/{lib/api.ts, components/{CategoryActionCard,NotificationBell}.tsx,
pages/RecurringReviewPage.tsx (new), App.tsx}`, `docs/{architecture.md, product-plan.md, roadmap.md}`,
`CLAUDE.md` (majordom-financiar), 2 new GitHub issues (#245).

### Lessons
- **A build/typecheck pass and a mechanical wiring check are not the same as verification.**
  Every piece of both #117 and #41 got either line-by-line diff review or actual functional
  testing (synthetic data scripts for backend, real browser clicks for frontend, a real chat
  conversation for #117's LLM-facing behavior) before being called done — this is what caught the
  `create_schedule()` inactive-by-default bug, which `check_provider_wiring.py` and a clean backend
  boot both missed entirely (neither checks *behavior*, only reachability/syntax).
- **A delegation-tier decision isn't fixed once per feature — re-evaluate per wave.** #41's backend
  (well-defined, mechanical, single-domain) was a good Aider/DeepSeek fit; its frontend (5 tightly
  coupled files, one with a large non-obvious existing convention) wasn't, per CLAUDE.md's own
  written rule — caught before dispatching, not after a bad diff came back.
- **"It's a bit faster, let's continue" from the user is not the same as "fully fixed" — say so
  plainly and file the remainder as its own issue** rather than let an interrupt's partial fix read
  as complete. #245 exists specifically so the real remaining cost doesn't quietly disappear once
  the mid-session pressure is gone.

### Unresolved
- **#242** — `resolve_transfer_duplicate()` data-loss bug, still open, unrelated to this session.
- **#245** (opened this session) — Home page load's remaining ~20s, root-caused to #227's per-call
  compute cost, not connection overhead. Two next steps named in the issue, neither started.
- Push deferred all session — real time never re-entered the 18:00-23:00 weekday commit window
  after the first commit; every commit's timestamp was corrected individually instead. Push still
  pending as of session end.
