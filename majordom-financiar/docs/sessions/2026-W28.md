# Week 2026-W28 (July 6 – July 12)

---

## 2026-07-06 — #69: close account from chat (partial — tool + card shipped, AB-parity card deferred)

### Resolved
- Implemented `propose_close_account` end-to-end following the standard `propose_*` confirmation-card pattern: new tool + registry schema/dispatch, in-memory proposal store (`backend/tools/close_account.py`), confirm/cancel endpoints (`backend/api/close_account.py`), `client.close_account()` + `ActualBudgetProvider`/`FinanceProvider` pass-through (all 3 layers per #126), `_PROPOSAL_TOOLS` + system-prompt bullet, frontend `CloseAccountCard.tsx` (danger variant, balance-≠-0 warning) wired into `Chat.tsx`.
- Done in an isolated git worktree/branch, then fast-forward merged to `main` (didn't touch the in-progress `roadmap.md` edits).
- Backend tested live in the local test stack: exact/partial/not-found matching, full propose→close→verify-disappears flow, registry + `_PROPOSAL_TOOLS` presence. Frontend compiled clean in the Docker build.
- **Bug found in UI testing + fixed:** LLM extracted `"test account"` from "close my test account" but the account is named `"test"`, so matching failed. Extracted shared `_match_account()` (also refactored `propose_balance_adjustment` onto it — same latent bug) that strips a trailing " account" word and retries. Added a system-prompt example.

### Files modified
- backend/core/actual_client/client.py, backend/core/finance/actual_budget_provider.py, backend/core/finance/provider.py, backend/tools/close_account.py (new), backend/api/close_account.py (new), backend/tools/finance/actual_budget.py, backend/tools/registry.py, backend/api/chat.py, backend/main.py
- frontend/src/lib/api.ts, frontend/src/components/CloseAccountCard.tsx (new), frontend/src/pages/Chat.tsx
- docs/architecture.md (rule 24), scripts/prompts/deepseek/69_close-account-from-chat.md

### Lessons
- **`close_account` doesn't move money — see architecture.md rule 24.** Setting `Accounts.closed = True` on an account with balance ≠ 0 hides the money from the budget (verified: €1175 vanished from visible totals, restored on reopen) instead of forcing a transfer the way AB's native UI does. Current implementation is only safe for empty accounts.
- The confirmation-card mirror pattern (balance_adjustments as reference) is mechanical and reliable across ~7 layers; the only non-obvious layer the DeepSeek prompt missed was the `ActualBudgetProvider`/`FinanceProvider` pass-through (#126) — caught via CLAUDE.md's known-gotchas.

### Unresolved
- **#69 left OPEN by choice.** Next iteration: card that, when balance ≠ 0, requires a destination account and does transfer + close atomically on confirm — reusing the existing `propose_account_transfer` / `create_transfer` (money-movement already exists, no new mechanism needed). Note added to the issue.
- Test-data side effect: `Bank Name` (balance 0) was closed during testing and left closed; `test` was closed+reopened (back to original state). Local test stack only, not real data.

---

## 2026-07-06 — #69: transfer+close atomic follow-up (closes the issue)

### Resolved
- Implemented the deferred AB-parity flow from the note above: `client.close_account_with_transfer(account_id, destination_account_id)` — single `_get_actual()`/`commit()` doing transfer + close atomically (no split-failure window). Direction depends on balance sign: positive drains out of the closing account, negative (a debt) gets paid off from the destination. Reuses `_get_or_create_transfer_payee` (shared with `create_transfer()`) — no new money-movement code.
- Passed through all 3 layers (`client.py` → `actual_budget_provider.py` → `provider.py` Protocol, per #126 gotcha).
- `propose_close_account` now returns the full accounts list (mirrors `propose_account_transfer`); confirm endpoint requires `destination_account_id` when balance ≠ 0 (400 + proposal kept alive if missing, so the user can retry instead of restarting the whole flow); `CloseAccountCard.tsx` grew a destination selector (pattern borrowed from `AccountTransferCard.tsx`), confirm disabled until picked.
- Implemented directly (not via DeepSeek) — touches provider pass-through + card conventions that aren't obvious from a prose spec alone.
- Tested live end-to-end via the real chat endpoint (LLM tool call, not bypassed) on the local test stack: positive-balance case (`test`, €1175.78 → `BUNQ Savings`) and negative-balance case (`Cash`, -€47.48 → `ING current`) both zeroed the closing account and moved the balance correctly; missing-destination confirm correctly 400s without discarding the proposal. Test data restored to original balances afterward (reopened both accounts, tombstoned the test transfer transactions).

### Files modified
- backend/core/actual_client/client.py, backend/core/finance/actual_budget_provider.py, backend/core/finance/provider.py
- backend/tools/finance/actual_budget.py, backend/api/close_account.py
- frontend/src/components/CloseAccountCard.tsx, frontend/src/lib/api.ts
- docs/architecture.md (rule 24, updated from partial → resolved)

### Lessons
- None new — this followed the plan already documented in the prior entry's "Unresolved" note and architecture.md rule 24 almost exactly as scoped.

### Unresolved
- None. #69 closed.

---

## 2026-07-07 — Home redesign: #163 design tokens + Card, #164 Portfolio Independence, #166 FIRE model rewrite

### Resolved
- **#163** — shared `<Card>` (`hero`/`list-item`/`accordion` variants, `accentSide` top/left) and `<InfoIcon>` (reuses the existing bottom-sheet pattern from `Chat.tsx`'s help modal) components. Semantic tokens (`positive`/`attention`/`info`/`interactive` + `-dim` variants) added to `tailwind.config.js` alongside the pre-existing `accent`/`success` (intentionally not mass-renamed — see decisions.md). Applied to `GoalCard`, `BudgetDashboard`'s outer card, and the new Portfolio Independence card.
- **#164** — FIRE widget retired, folded into Financial Goals as a proper "Portfolio Independence" card (left-accent, same 4-row structure as other goal cards). Cashflow metric card dropped entirely (matches the mockup, confirmed with Doru via `AskUserQuestion`). Backend gained `estimated_year` (numeric solve, replacing the fixed `FIRE_YEAR` check) and `trend_months` (1-month comparison — a true 3-6mo moving average needs new persistence, deferred).
- **#166** — replaced the single-phase hardcoded FIRE model (`FIRE_TARGET`/`MONTHLY_CONTRIBUTION`/`ANNUAL_RETURN`/`FIRE_YEAR`) with a 2-phase accumulation/decumulation model: target is now a *derived output* (present value of a depleting annuity that funds the user's desired retirement spend), not a flat number. All 6 assumptions (years to transition, years in retirement, monthly contribution, accumulation/decumulation return, desired monthly spend) live in `user_preferences.fire_model` (JSON blob, reused the existing generic KV mechanism — no new table) and are editable via a new `finance__propose_set_fire_model` chat tool with a full confirmation card (extended `CategoryActionCard.tsx`, not a new component — that file is the app's generic "pending confirmable action" mechanism despite its name).
- Delegated #166 to DeepSeek (`scripts/prompts/deepseek/done/166_fire-model-chat-editable.md`) — Claude wrote the full spec (exact annuity/FV formulas, storage decision, end-to-end file trace copied from `propose_set_category_budget`), DeepSeek implemented, Claude audited.
- **Audit found 3 issues, all fixed before commit:**
  1. `fire_pct` used a forward-projected value (`fv_at_transition`) while `fire_pct_prev` and the card's own "€X saved" row used today's raw balance — inconsistent basis, traced back to Claude's own spec (not a DeepSeek deviation). Fixed to use today's balance consistently, matching every other goal card's percentage semantics.
  2. A stray "→" rendered on the new confirmation card — the pre-existing "rename" fallback block wasn't excluded for the new `set_fire_model` action type.
  3. **The new tool had no system-prompt bullet.** Live chat testing (by Doru) showed the model leaking its raw internal function-call tokens (`function< | tool_sep | >finance__propose_set_fire_model {...}`) as literal chat text instead of executing a structured tool call — reproducible on a fresh conversation. Root cause: same lesson already documented for #160 (new tools need an explicit system-prompt bullet + example, not just schema registration) that Claude's own #166 spec forgot to apply, despite using #156's prompt (which *does* include this step) as the explicit template. Fixed by adding the bullet and trimming the tool's own overly-long description (4 quoted examples crammed into one field). Added as a permanent gotcha in `CLAUDE.md` so it's not a third strike.
- Verified live throughout with real browser screenshots (Playwright + a cached Chromium binary, no `chromium-cli`/Playwright available as a project skill — improvised a driver script in the scratchpad) and by Doru testing in his own browser.
- Fixed a real privacy leak mid-session: a personal domain name (referenced only generically here, see the CLAUDE.md rule) ended up in `docs/decisions.md`'s public-demo entry. `scripts/check-private-data.sh` already had a regex for it but only ran at the commit-time git hook — nothing had been committed yet, so it never fired before Doru caught it by reading the doc directly. Extended the script to also work as an ad-hoc self-check (falls back to the unstaged working-tree diff), added a CLAUDE.md rule to run it proactively after editing any tracked doc, not just wait for the commit gate.
- Created 3 forward-looking GitHub issues from the same design discussion (not implemented this session): #165 (Home polish — empty states, sync icon, budget period nav, Needs Resolving popup), #167 (Expense Coverage / Coast FIRE, blocked on an income-classification decision — recorded in decisions.md), #168 (public demo on a VPS, deliberately blocked on Ghostfolio + this whole Home redesign batch landing first).

### Files modified
- `frontend/src/components/Card.tsx` (new), `frontend/src/components/InfoIcon.tsx` (new), `frontend/src/components/FireWidget.tsx` (deleted)
- `frontend/src/pages/Home.tsx`, `frontend/src/components/BudgetDashboard.tsx`, `frontend/src/components/CategoryActionCard.tsx`, `frontend/src/lib/api.ts`, `frontend/tailwind.config.js`
- `backend/core/actual_client/client.py` (`_calc_fire` rewrite, `_load_fire_model`, `_fire_months_to_amount`), `backend/tools/finance/actual_budget.py` (`propose_set_fire_model`, `get_fire_chart` updated), `backend/tools/registry.py`, `backend/api/chat.py` (`_PROPOSAL_TOOLS` + system-prompt bullet), `backend/api/category_actions.py` (`GoalOverride` + `set_fire_model` branch)
- `docs/decisions.md` (5 new entries), `docs/roadmap.md`, `CLAUDE.md` (root + majordom-financiar), `scripts/check-private-data.sh`
- `scripts/prompts/deepseek/done/166_fire-model-chat-editable.md` (new), `scripts/prompts/deepseek/done/69_close-account-from-chat.md` (moved from `deepseek/`, already closed, just hadn't been filed)

### Lessons
- See the audit findings above — the system-prompt-bullet gotcha is the one worth remembering (now in `CLAUDE.md`, not just this log).
- A DeepSeek prompt's own accuracy matters as much as the implementation — the `fire_pct` bug came from Claude's spec, not DeepSeek's execution. Auditing a DeepSeek diff means re-deriving whether the *spec* was right, not just whether the diff matches the spec.
- Rendering the actual mockup HTML (via a headless Chromium screenshot) instead of reasoning from its CSS as text caught real mismatches (left-border accent, no accent on the Budget card) that reading the stylesheet alone missed.

### Unresolved
- #165, #167, #168 — open, not started this session.
- True 3-6 month moving average for the trend arrow — needs new persistence (monthly snapshots), noted but deferred, not a blocker.

---

## 2026-07-07 — #165 sub-item 1: Financial Goals empty state

### Resolved
- Dashed-border card ("+ Create your first goal" + 3 tappable chips — Expense Coverage / FIRE / Custom goal) shown when `goals.length === 0`; disappears entirely once ≥1 goal exists, replaced by a thin dashed "+ Add another goal" row at the end of the goal list. Reused existing `<Card>` (#163) and the already-established `navigate('/chat', { state: { prefill } })` pattern (same one "Needs Resolving" items use) — no new components beyond the two small ones needed for this card/row. Chip colors reuse the existing `positive`/`positive-dim`/`attention`/`attention-dim` tokens, nothing new added to `tailwind.config.js`.
- Dropped the outer `(fireData || goals.length > 0)` guard that used to hide the whole "Financial Goals" section — it now always renders in the has-accounts branch, since the empty state itself is the thing to show when neither exists.
- Verified live: rebuilt `majordom-web`, logged in via a scratchpad Playwright script (no `chromium-cli`/browser tool available in this environment — cached Chromium binary from a prior `npx playwright` fetch was reused, no project skill existed for this yet), screenshotted the empty card, clicked "Custom goal", confirmed the prefill text lands correctly in the Chat input, then sent it — model asked for the missing account/target/deadline details with a worked example, i.e. degrades to a normal clarifying question rather than failing silently. No console errors.
- Checked whether `finance__set_account_goal` (the tool this chip eventually needs) has a system-prompt bullet, per the now-standing gotcha from #160/#166 — it doesn't, but the live test showed the model handling the missing-args case gracefully anyway (asks for details, gives a correct worked example), so this wasn't treated as a blocker for this sub-item; left as-is, not fixed here.

### Files modified
- frontend/src/pages/Home.tsx

### Lessons
- No project skill exists yet for driving this app's UI in a browser inside this environment; improvised via a cached Playwright/Chromium binary. Worth turning into a proper project skill (`/run-skill-generator`) next time this comes up, so it isn't rediscovered from scratch again.

### Unresolved
- #165 remaining 4 sub-items (sync icon, budget period nav, Needs Resolving popup, tap-category → Chat) — not started.
- `finance__set_account_goal` still has no system-prompt bullet (unlike `get_goals_chart`/`propose_set_fire_model`) — not currently causing a visible failure, but worth a bullet next time this tool is touched.
