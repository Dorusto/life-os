# Week 2026-W25 (June 15–21)

---

## 2026-06-15 — propose_categorize_by_payee tool

### Resolved
- **propose_categorize_by_payee tool:** bulk-categorize all uncategorized transactions for a payee via chat; returns confirmation card showing count affected
- **client.py fix:** `update_uncategorized_by_payee` used `get_or_create_category(group_name="Income")` hardcoded — replaced with direct category_id lookup; added `count_uncategorized_by_payee()` for pre-confirm count
- **Editable confirmation card:** payee (text input) + category (select dropdown from `available_categories[]`); user can correct both fields before confirming
- **GoalOverride:** added `payee` field; confirm endpoint resolves override category_name → category_id via stored `categories_map`
- **CLAUDE.md rule:** confirmation card fields must always be editable (input/select), never static text

### Files modified
- `backend/core/actual_client/client.py` — count + fix update method
- `backend/tools/finance/actual_budget.py` — propose_categorize_by_payee()
- `backend/api/category_actions.py` — categorize_by_payee handler + GoalOverride.payee
- `backend/tools/registry.py` — tool definition + dispatch
- `backend/api/chat.py` — added to _PROPOSAL_TOOLS
- `frontend/src/components/CategoryActionCard.tsx` — editable fields for categorize_by_payee
- `frontend/src/lib/api.ts` — CategoryActionData extended
- `CLAUDE.md` — rule 5 extended

### Lessons
- Payee field rămâne text liber (nu dropdown) — matching e substring, nu exact match; dropdown ar forța selectarea numelui complet

### Unresolved
- Issue #94 (uncategorized alert proactiv) rămâne deschis — asta a fost doar flow-ul manual
