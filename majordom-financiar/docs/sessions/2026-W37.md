# 2026 — Week 37

## 2026-09-12/13 — vehicle-manager Fuelio-nav closed out; investment-manager scoped, planned, and dispatched to opencode

### Resolved
- **vehicle-manager Fuelio-style nav** (Dashboard/Timeline/Stats/Reminders + BottomNav/
  VehicleSwitcher/LogoutButton, three new read-only endpoints backed by a new `app/stats.py`)
  built by Doru directly via DeepSeek Flash in opencode (0.5€, ~18 min) — verified by a subagent
  (typecheck, backend import, full live browser walkthrough incl. a real add/delete log-entry
  round-trip) and by `pre-commit-review` (two minor duplications fixed: `reminderHorizon` shared
  into `lib/reminders.ts`, `ChartSection` shared into its own component). Fixes #265 (Chart.tsx
  axis-label rounding, both copies) and #266 (no logout button). Committed and pushed.
- **Real credential leak found and contained**: `tools/vehicle-manager/docs/review-checklist.md`
  (committed, pushed to the public repo) had the real `VEHICLE_MANAGER_USER1_USERNAME`/`PASSWORD`
  in plain prose — missed by `check-private-data.sh` (built for `KEY=value` shapes, not prose).
  Local password rotated immediately; Doru chose to leave the git history as-is (accepted the
  exposure for this specific shared dev password) rather than rewrite public history. Checklist
  file redacted to a placeholder in a normal commit.
- **Real port-8888/8889 outage root-caused and fixed on the production LXC**: `tailscale serve`'s
  own listener binds the *specific* Tailscale interface address for a port; Docker's `"8888:8080"`
  style publish binds the *wildcard* `0.0.0.0` — Linux won't let a wildcard bind coexist with an
  already-existing specific-address bind on the same port, so recreating `majordom-sqlite-web`
  after that binding order got established failed every time, silently leaving the container
  running with zero actual port bindings (`docker inspect` showed `{}`) even when `docker compose
  up` reported success. Fixed by binding those services to `127.0.0.1:<port>` instead of `0.0.0.0`
  (matches what `tailscale serve` already proxies to) — applied live on the LXC and in
  `docker-compose.yml`. Same evening: found and explained (not a bug) that Actual Budget's own
  "Fatal Error: SharedArrayBuffer" requires a real HTTPS context — added a new `tailscale serve
  --https=5443` mapping to `127.0.0.1:5006` rather than touching the working plain-HTTP setup.
  `DEPLOY.md`'s documented update procedure was also missing `docker compose pull` (only rebuilds
  this repo's own images, never fetches newer third-party ones like `actual-server`) — fixed.
- **investment-manager (#262) scoped and planned**: confirmed staying with the separate-service
  architecture after a real reconsideration (bundling into majordom-financiar, Wealthfolio-style,
  was seriously discussed and rejected — pulls the opposite direction from #263's agreed
  sequencing). Market-data source resolved: Twelve Data. A real XTB `.xlsx` export was inspected
  once to document its actual structure (`Cash Operations`/`Closed Positions`/`Open Positions`
  sheets) then deleted, never committed — anonymized CSV samples written instead, and
  `tools/*/docs/samples/` added to `.gitignore` so the mistake is structurally impossible for any
  future app. Full spec written (`tools/investment-manager/docs/standalone-app-plan.md`, 13
  sections): the essential Wealthfolio-shaped toolset (holdings/transactions/XIRR+TWR+benchmark/
  allocation/income/rebalancing/goal-projection), explicit non-scope (no net worth, no in-app AI
  assistant, no US-specific contribution limits, no Monte Carlo), auth/nginx/Docker files written
  directly (never delegated, per `standalone-app-playbook.md`), reusable-design-token requirement
  for a possible later platform-wide visual pass. Confirmed XTB's own public API was discontinued
  2025-03-14 — the `.xlsx` report-import path is the only option, not a stopgap.
- **Dispatched the plan to `opencode-61`** (Doru's existing DeepSeek/OpenRouter agent container,
  separate LXC) — its prior "Cortor" orchestration layer (planner/coordinator, Telegram, cron) was
  abandoned as non-functional in practice; flow simplified to a direct `opencode run --agent flash`
  dispatch. First run stalled silently on an uncovered `external_directory` permission ask (no one
  to answer it, headless) after finishing the backend files cleanly — fixed
  (`external_directory: "allow"`, `edit` scoped to `tools/investment-manager/**`, `bash` already
  denying `rm -rf`/`ssh`/`sudo`/`git push`) and resumed. Full process written up for reuse:
  `20_AREAS/guides/technical-guides/Ghid_Build_App_Cap-Coada_opencode_DeepSeek.md` (Second Brain
  vault) — plan-doc structure, opencode permission gotchas, tooling (Context7 recommended, Ralph
  Loop/`opencode-plan-manager` noted as escalation options), post-build verification flow (pull
  back to a machine with real tooling, never trust the done-report, `.61` itself is too small to
  run the Docker stack for verification anyway).

### Files modified
`tools/vehicle-manager/{app/{main.py,stats.py (new)},docs/review-checklist.md,frontend/src/{App.tsx,components/{BottomNav,ChartSection,LogEntryForm,LogoutButton,VehicleSwitcher}.tsx (new),lib/{api.ts,entryTypes.ts (new),reminders.ts (new),useSelectedVehicle.ts (new)},pages/{Dashboard,RemindersPage,StatsPage,TimelinePage}.tsx (new),pages/{FuelioImport,VehicleDetail,VehicleList}.tsx}}`,
`majordom-financiar/{frontend/src/components/Chart.tsx,docker-compose.yml,DEPLOY.md}`,
`tools/investment-manager/` (new: `app/auth.py`, `requirements.txt`, both `Dockerfile`s, `frontend/{nginx.conf,src/lib/auth.ts}`, `.env.example`, `README.md`, `docs/standalone-app-plan.md`, `docs/samples/xtb_*_sample.csv`, `.claude/skills/frontend-design/SKILL.md`),
root `.gitignore`,
LXC production (`~/life-os/majordom-financiar/{.env,docker-compose.yml}`, not tracked here),
`opencode-61` (`~/.config/opencode/opencode.jsonc`, not tracked here).

### Lessons
- A private-data scanner built around `KEY=value` patterns has a real, different blind spot:
  a real secret restated in plain prose ("Login with X/Y at ..."). Worth a `check-private-data.sh`
  enhancement later (diff the actual configured `.env` values against the staged diff, not just
  pattern-match likely-looking secrets) — not done this session, flagged for later.
- `docker inspect`'s `NetworkSettings.Ports` being `{}` on a container reporting `Up`/`running` is
  the tell that a port-bind failure happened at *creation* time and a later plain `up -d` just
  started the same broken container object rather than retrying the bind — `--force-recreate` (or
  removing the container first) is required to actually retry, not just `up -d`.
- Wealthfolio (the visual/feature reference for investment-manager) is AGPL-3.0 — fine as
  inspiration, not as a code source, and specifically relevant to keep in mind once/if the
  "package for others" phase (root `CLAUDE.md`'s sequencing, step 4) is ever reached.
- opencode's permission model has more independently-defaulted categories than just `edit`/`bash`
  (`external_directory` defaults to "ask" and isn't covered by restricting those two) — a short
  test run before a long unsupervised one would have caught this before it silently stalled.

### Unresolved
- investment-manager build still in progress on `opencode-61` as of this entry — not yet reviewed,
  not yet merged into `main`. Next session: verify per the vault guide's §8 flow before any commit.
- `check-private-data.sh` prose-secret blind spot (see Lessons) — not fixed, no issue filed yet.
- Hermes Agent install (already tracked, `Radar.md` + this repo's own `DECISIONS.md` for the
  opencode-agent-system) still not done — separate dedicated session, per Doru.

---

## 2026-09-11 — #253 closed: text-mode refuel confirm gets the #121 near-duplicate parity, dedup debt cleared

### Resolved
- **Context**: picked up an uncommitted diff from 2026-09-03 (no session log, no GitHub issue — built and Docker-rebuilt back then but never finished) that gave `confirm_vehicle_proposal` (chat-triggered refuel) the same attach/near-duplicate/`force_new` dispatch `confirm_fuel_receipt` (photo-mode) already had from #121, plus a fix for `ActualBudgetClient.delete_transaction()` only matching on `Transactions.financial_id` when `add_transaction()` actually returns `Transactions.id` (architecture.md rule 39).
- **`pre-commit-review` caught a real rule violation before commit**: the attach/near-duplicate/create dispatch was now duplicated a *third* time (`confirm_receipt`, `confirm_fuel_receipt`, `confirm_vehicle_proposal`) — exactly the pattern `duplication-prevention.md`'s "extract at second occurrence" rule exists to catch. Extracted into `ReceiptService.resolve_transaction()`, all three endpoints now call it; each endpoint's own response-shaping (fuel stats vs. plain confirm) stays local, only the shared branching moved.
- Also flagged (documented, not fixed — the review correctly called it theoretical, not a bug): `Transactions.id`/`financial_id` are both unconstrained `Text` columns, so the new `delete_transaction()` `OR` match isn't schema-guaranteed collision-free, only conventionally so for every ID this codebase itself generates. Noted directly in `delete_transaction()`'s docstring.
- Filed and closed **#253** for the parity bug itself (GitHub as the record, since none existed for this work).
- Live-tested via FastAPI `TestClient` against the real fixture Actual Budget instance (not just `tsc`/mechanized scripts) for **all three** confirm endpoints: near-duplicate detected → `possible_match`, `force_new` bypass, `attach_to` with a real `financial_id`, `attach_to` on a bogus id → 404, and `delete_transaction()` resolving an `id`-shaped value. All synthetic transactions cleaned up (deleted) after.

### Files modified
`backend/{api/receipts.py, api/vehicle_proposals.py, core/actual_client/client.py, services/receipt_service.py, tools/finance/actual_budget.py}`, `docs/architecture.md` (rule 39), `frontend/src/components/FuelReceiptCard.tsx`.

### Lessons
- **Uncommitted work from a prior session is not "done" just because it was built and Docker-rebuilt** — no session log or issue meant the duplication debt it introduced went unreviewed for 8 days. Running `pre-commit-review` on it (even though every line had already been read manually) is what caught the violation; the skill's "skip only when already reviewed" bar was met on paper, but running it anyway here was the right call given the code's unclear provenance.
- **A three-way copy is not always visible from one diff alone.** The 2026-09-03 diff only *added* the second copy of the dispatch (vehicle_proposals.py); the first copy (confirm_receipt, confirm_fuel_receipt already duplicating it inside receipts.py) predated it and was never itself flagged. Worth checking for existing near-identical control flow in the *same file* being touched, not just the diff's own new code.

### Unresolved
- None from this task. Still open elsewhere: #245 (Home page ~20s), #249 (5th action-store copy), #242 (unrelated transfer-duplicate bug).

## 2026-09-11 — parallel-delegation session: 9 issues closed, mid-session directive to redirect Pro-tier work to a Claude subagent

### Resolved
- **#242** closed: `resolve_transfer_duplicate()` was discarding the bank-synced side's payee/category/date onto the surviving transfer leg. Fixed (payee/category "only if missing", date always taken from the bank side — the transfer leg's own date is the unreliable one) plus inline-edit (payee/category/notes/date) on the Duplicates review card. Dispatched to Aider/DeepSeek Pro (before the mid-session redirect below) — review caught **2 real bugs in the delegated diff before merge**: `notes` had no truthy-guard unlike payee/category, so an untouched confirm silently overwrote the just-auto-copied bank-side notes back to empty; the `date` field was seeded from the surviving side (the transfer leg — exactly the unreliable value being fixed) instead of the bank-synced side, so an untouched confirm undid the fix's entire point on every single use. Both were gaps in my own delegation spec, not something Aider invented.
- **#249, #230, #228** closed via 3 parallel Aider/DeepSeek Flash dispatches (mechanical, well-scoped): shared `_action_store.py` helper (5 verbatim copies → 1), `ab_audit.py` errors on an unrecognized subcommand instead of silently running the default report, `check_provider_wiring.py` now also compares parameter signatures across all 3 layers (not just method existence) — live-verified by injecting a real signature mismatch and confirming it's caught.
- **#219** closed (3 sub-items): F25 (dev-language copy in the Analytics placeholder) + F27 (Transactions list chip truncation/repeat + checkboxes hidden behind a new Select toggle) via Aider Flash. F26 (chat re-formats the same `get_transactions` result differently turn to turn) resolved by adding a dedicated `finance__list_transactions` tool + `TransactionListCard`, mirroring the existing chart-card pattern (`_PROPOSAL_TOOLS` short-circuit) instead of a system-prompt tweak — Doru's explicit choice between the two options presented. Implemented via a Claude subagent specifically because blindly adding the existing `get_transactions` tool to `_PROPOSAL_TOOLS` would have broken its other use (finding a transaction id to feed into `propose_transfer_conversion`/`propose_tag_transaction` in the same turn) — caught by reading the system prompt before delegating, not by the subagent.
- **Mid-session directive from Doru**: any task that would normally go to DeepSeek Pro (per `delegate-by-complexity`'s file-count/complexity rubric) goes to a Claude subagent instead, for now. Applied to the rest of the session's larger tasks:
  - **#216** closed: `_calc_fire`/`_load_fire_model`/`rule_match_prefix`/`_financial_id` relocated out of `client.py` into `backend/core/finance/{fire,transaction_utils}.py` (pure move+rename, `_financial_id` also dropped from `actual_client/__init__.py`'s `__all__` — a private name had no business being there). F13 (the issue's other half, `_get_client()` duplication) was already resolved by #222, stale in the issue body.
  - **#226** closed: new `docs/learn/14-ledger-reconciliation.md` — the ledger-model explanation Doru asked for in his own words ("I don't really understand how the whole thing is supposed to work"), not just an integration doc. Every code-backed claim in it (`get_reconciliation_suspects()`, the `[Balance Adjustment]` tag, the split-`cleared` gotcha) checked against real code before merge, one self-caught correction (a Majordom-specific compensation described as if it were general AB UI behavior).
  - **#190** closed: AB setup wizard — live "Test connection" (lists available budgets via `list_user_files()` when the file isn't known, distinct error messages for bad host/password/file), encrypted-at-rest storage (Fernet, key auto-generated once in the gitignored `data/` volume, no user passphrase), and `Settings.actual.reload_from_db()` so a saved connection takes effect immediately with no container restart. Two real architecture/security decisions surfaced to Doru *before* dispatch, not decided silently: how credentials take effect without a restart (extend `Settings` to check DB-saved overrides + an explicit reload call), and how the encryption key is derived (random, generated once, disk-stored, gitignored — not a user passphrase, not hardcoded). "AB connection lost — reconnect" fallback banner cut from scope (no existing app-wide mechanism to detect a later disconnect) and filed separately as **#254**.
  - **#187** closed: "+ Create new category" in the Add/Review Transaction sheet, reusing the exact pattern CSV import already shipped (same sentinel value, same group `<datalist>`, same "Majordom" default) rather than inventing a new one. Live-verified end to end against the local AB fixture: created a real category + transaction through the actual endpoint, confirmed the `category_id` linkage, cleaned up after. Caught 2 real `tsc` errors before merge (`addLine()` not updated for the two new `Line` fields, an untyped `Record<string, unknown>` silently defeating type-checking at both call sites) — diff review alone wouldn't have caught either, only actually compiling did.
- **Also fixed in passing**: `scripts/check-private-data.sh`'s "Real credential value" check false-positived on `password=body.password`/`self.password = saved["password"]` (legitimate FastAPI/dict references, not literal secrets) — extended the existing negative-lookahead exclusion list (already had `settings.`/`cfg.` for the same reason) rather than writing a separate check, per the script's own stated convention. Verified the fix still catches an actual literal value.
- **Live verification pattern used throughout**: rather than trusting a delegated diff from static review alone, overlaid each branch onto the main checkout's working tree (`git checkout <branch> -- <files>`, `node_modules` symlinked for `tsc` when the worktree had none) to run the real mechanized checks and, for #242/#190/#187, rebuilt `majordom-api` and exercised the actual code path against the local Actual Budget fixture container with real (if synthetic) data — created, verified, then cleaned up every time.

### Commit-timestamp correction (process gap, not a code fix)
All of today's local commits — every Aider auto-commit and every merge — landed at their real system time (~00:30-02:00), outside `CLAUDE.local.md`'s 18:00-23:00 weekday window, because `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` weren't exported *before* each `aider` launch as `delegate-by-complexity`'s own step 7 already instructs. Caught by Doru after the fact, not self-caught. Fixed retroactively for the whole session's range in one pass: `git filter-branch --env-filter` rewriting author+committer dates to a sane evening sequence, content-diffed against the pre-rewrite tip to confirm zero change, backup ref (`refs/original/...`) cleaned up after. See "Lessons" below — this is the second time in recent sessions this exact gotcha has bitten despite being documented (see the 2026-09-02 W36 entry above, a different Aider/OpenRouter gotcha, same skill).

### Files modified
`backend/{api/{setup,category_actions,home,transactions,receipts,chat}.py, core/actual_client/client.py, core/finance/{provider,actual_budget_provider,fire(new),transaction_utils(new)}.py, core/config/{settings,ab_credential_store(new)}.py, tools/{registry,category_actions,vehicle_log_actions,vehicle_reminder_actions,vehicle_status_actions,notification_actions,_action_store(new)}.py, tools/finance/actual_budget.py}`, `frontend/src/{lib/api.ts, App.tsx, pages/{Analytics,Transactions,DuplicatesReviewPage,AbSetupWizard(new),ReceiptFlow}.tsx, components/{CsvImportCard.tsx(read only),TransactionListCard(new)}.tsx}`, `scripts/{ab_audit.py, check_provider_wiring.py, check-private-data.sh}`, `docs/{learn/14-ledger-reconciliation.md(new), INDEX.md}`.

### Lessons
- **A delegation spec's own gaps produce real bugs just as easily as the model executing it** — #242's two pre-merge bugs (unguarded `notes`, wrongly-seeded `date`) both traced back to a seeding assumption in the task I wrote (default form values coming from the "surviving" side instead of the authoritative one), not to anything Aider did wrong. Reviewing a delegated diff against the *actual runtime behavior* it produces (what does an untouched confirm actually submit?) catches this; reviewing it only against "does this match what I asked for" does not, because the ask itself was the gap.
- **Live end-to-end testing against the local fixture stack (not just `tsc`/mechanized scripts/diff review) found real bugs three separate times this session** (#242's default-submission bugs, #187's two compile errors, the private-data-scanner false positive) that no earlier check surfaced. For anything touching a live external connection (actualpy) or a compile-checked language boundary, budget for an actual run, not just a read.
- **The same documented process gap recurred** (Aider commits use real system time; the fix is exporting `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` *before* launch, already written down in `delegate-by-complexity`'s own step 7) across all 5 Aider dispatches this session, caught only after the fact by Doru. Documentation that says "do X before Y" doesn't reliably get followed just because it exists in a skill file read at the start of the flow — worth treating as a checklist item to actively verify post-dispatch (`git log --format=%ad` on the new commit) rather than trusting it was applied, until it stops recurring.
- **Surfacing real architecture/security decisions to Doru *before* dispatching, not after** (settings-reload mechanism and key-derivation for #190, structured-card-vs-prompt-tweak for F26) kept both from becoming rework — a subagent or Aider filling either gap silently would have been a plausible-looking but uncoordinated guess on a security-relevant or product-visible choice.

### Unresolved
- **#254** filed and open: "AB connection lost — reconnect" banner, deliberately cut from #190's scope.
- Nothing pushed to GitHub this session (all closes/opens above are issue-tracker actions via `gh`, independent of `git push`) — `main` is 33+ commits ahead of `origin/main` as of session end, entirely local.

## 2026-09-11 — #254 implemented and live-verified: AB reconnect banner

### Resolved
- **#254** ("AB connection lost — reconnect" banner, cut from #190) implemented: `ActualBudgetClient._run()` — the single choke point every read/write method already awaits through — now catches `httpx.HTTPError`/`AuthorizationError` and re-raises as a new `ActualBudgetUnavailableError`; `backend/main.py` gets this codebase's first `@app.exception_handler`, turning it into a 503; `authFetch()` recognizes that 503 shape and flips a small pub/sub flag a new `AbConnectionBanner` subscribes to. Reactive, not polling — see `docs/decisions.md#254-reactive-not-polling` for the two options presented to Doru and why, and `architecture.md` rule 40 for the choke-point pattern itself.
- Two architecture questions surfaced to Doru *before* dispatch, not decided silently: reactive-vs-polling detection (above), and where the pacing-style config question for a *different*, not-yet-started issue (#112) would live — deferred, #112 itself not started this session.
- First fork attempt at implementing #254 returned a "done" report after 0 tool calls / ~6s with zero file changes — caught via `git status`/`git diff --stat` before trusting it, retried with an explicit instruction to actually execute; second attempt did the real work (56 tool calls, ~4.7 min). Added as a standing check to `plan-feature`'s fork guidance (step 7a) so this isn't re-learned per session.
- Diff reviewed in full in-conversation (file by file, not just skimmed) plus `tsc --noEmit`, `check_silent_exceptions.py`, `check_provider_wiring.py`, all clean — `pre-commit-review` subagent skipped per the skill's own stated exception (diff fully read + live-verified, not just a compile check).
- **Live-verified in Chrome**, not just the fork's own network-level checks: logged into the local fixture stack, stopped `actual-budget`, confirmed the banner appears app-wide and `Reconnect` navigates to `/setup/ab` (banner hidden there), restarted `actual-budget`, confirmed the banner clears on the next in-app navigation with no page reload. Stack restored to all-healthy after.
- Not committed yet — Doru raised a real concern mid-session (a screenshot of #254 itself having been opened at ~02:20 last night, outside the timestamp window) that turned out to be the incident `CLAUDE.local.md`'s GitHub-side-actions rule already documents, not a new one. Clarified the distinction explicitly: local `git commit` never touches GitHub regardless of time; only `gh issue`/`gh pr` write actions are time-gated. Doru confirmed: commit locally now, hold `gh issue close 254` for this evening's window.

### Files modified
`backend/{main.py, api/setup.py, core/actual_client/client.py}`, `frontend/src/{lib/{auth.ts, abConnectionStatus.ts(new)}, components/AbConnectionBanner.tsx(new), App.tsx}`, `docs/{architecture.md (rule 40), decisions.md, specs/ab-setup-wizard.md}`, `.claude/skills/plan-feature/SKILL.md`, `CLAUDE.md`.

### Lessons
- **A fork's completion report is not evidence of completion** — plausible prose describing the intended plan back is exactly what a no-op run produces too. `git status`/`git diff --stat` is a 2-second check against that, cheap enough to do every time rather than only when something feels off.
- **"Commit locally" and "act on GitHub" are two different gates with two different rules**, and conflating them (as the first pass through this conversation did) produces unnecessarily cautious answers — worth stating the distinction explicitly whenever a GitHub-visibility concern comes up, not just internally tracking it correctly.

### Unresolved
- #254 committed locally (`5b80ad8`, synthetic evening timestamp per `CLAUDE.local.md`), `gh issue close 254` still deferred to this evening's 18:00-23:00 window.

## 2026-09-11 — #112 implemented and live-verified: annual budget pacing

### Resolved
- **#112** (annual budget pacing) implemented: new "Annual budget pacing" Settings page (annual income + two mutually-exclusive category-tagging lists — fixed expenses / sinking funds), a new `ActualBudgetClient.get_budget_pacing_totals()` (one AB session for the whole Jan-to-now range, looped inside it, following `list_budget_realism_flags()`'s existing pattern to avoid tripping AB's login rate limit), a shared `backend/core/finance/budget_pacing.py` formula module (config storage + the pool/expected/actual arithmetic — one source of truth for 3 callers: the Settings status endpoint, `get_pending_items()`, and a new `finance__get_budget_pacing_status` chat tool), and a bell alert that only fires when configured AND over pace. Full design rationale in `docs/decisions.md#112-annual-budget-pacing`.
- **File-collision awareness**: dispatched only after confirming the task touched none of the files #254's fork was still working in (`client.py` was a near-miss — #245/#227 were deliberately held back instead for exactly this reason, see the "what we skipped" note below).
- **Two real findings caught during diff review, before commit** (see `decisions.md` entry for full detail): (1) the two category-tagging lists allowed the same category to be toggled into both at once, silently mis-prioritized by the backend — fixed with mutual exclusion enforced in the UI itself, sent back to the same fork as a scoped follow-up rather than fixed inline, live-verified in both directions afterward; (2) `scripts/check_provider_wiring.py` had a real blind spot — it only traced `client = get_provider()` assignments in the same file, so it never saw `get_budget_pacing_totals()` called one function-hop away through a `FinanceProvider`-typed parameter in `budget_pacing.py`. Fixed directly (not delegated — small, contained, script-only): the checker now also tracks any parameter annotated `FinanceProvider`, file-wide. Verified the fix actually closes the gap (method count 70 → 71 after adding the new detection, not just an unrelated change).
- **What we deliberately skipped this session**: #245/#227 (Home-load perf) were the natural next pick after #112 but live in the same `client.py` #112's fork was actively editing — held off to avoid an uncoordinated concurrent edit in the same working tree (no worktree isolation was used for either fork). #157 and #218 were also considered and explicitly set aside: both are later-phase work per `product-plan.md`'s stated sequencing (C2 → UI-polish → D → E) — picking them up now would jump the queue for no reason other than not wanting to wait, which `CLAUDE.md`'s own product-plan discipline exists to catch. Chose to just wait for #112 rather than force a task into the gap.
- Diff reviewed in full in-conversation (every changed file, both the initial implementation and the mutual-exclusion follow-up), plus independently re-ran (not just trusted the fork's own report of) `tsc --noEmit`, `check_provider_wiring.py`, `check_silent_exceptions.py`, and `check-private-data.sh` — all clean.
- **Live-verified twice** by the fork itself this time (unlike #254, it had live browser access): initial implementation (config save/load round-trip, chat tool, bell item appearing/disappearing based on pace) and the mutual-exclusion follow-up (toggling in both directions in Chrome, plus a `curl` check confirming zero overlap can be persisted).

### Files modified
`backend/{main.py, api/budget_pacing.py(new), core/actual_client/client.py, core/finance/{provider,actual_budget_provider,budget_pacing(new)}.py, services/notification_service.py, tools/{registry,finance/actual_budget}.py, api/chat.py}`, `frontend/src/{pages/Settings.tsx, lib/api.ts}`, `scripts/check_provider_wiring.py`, `docs/decisions.md`.

### Lessons
- **A mechanical safety-net check can have its own blind spots, and finding one is worth fixing on the spot when it's small and contained** — `check_provider_wiring.py` passing "clean" at an unchanged method count right after a new method was added was the tell; trusting the green check without noticing the count didn't move would have shipped a real gap in the exact mechanism #126/#148 exist to prevent.
- **Holding off on a task purely because of a file-collision risk (not a blocker in the task itself) is a legitimate reason to wait**, distinct from "nothing to do" — #245/#227 were fully actionable, just badly timed against #112's still-running fork in the same non-isolated working tree.

### Unresolved
- #111/#42 — still blocked on the same gaps identified earlier this session (no live bank-sync capability for #111's "native target" premise; #42 needs a Phase-D-scoped market-data-source decision not yet made).
- #157/#218 — deliberately not picked up, later-phase per `product-plan.md`'s sequencing; #218 is a decision for Doru to make, not a build task, whenever it's actually next.
- `gh issue close 112` deferred to the evening window, same as #254.

## 2026-09-11 — #227's compute-cost half fixed and measured; #245's duplicate-fetch half found to be bigger than scoped, deferred

### Resolved
- **#227's real root cause found by profiling live, not guessed**: instrumented `get_home_data()`/`get_budget_status()` directly against the local fixture stack (`docker compose exec majordom-api python3 -c "..."`, timing each internal step). The per-account transaction loop and every other step combined cost <0.5s — the entire ~8s came from `_compute_budget_vs_spent()` alone, specifically `actual.queries.get_accumulated_budgeted_balance()` being called once per zero-budgeted category (17 calls × ~470ms). Traced into `actualpy`'s own source: that function internally calls `get_budget_history(session, month)`, which rebuilds the full budget history from the first available month every single time — identical work for every category since `month` is constant within one `_compute_budget_vs_spent()` call.
- **Fixed** (`backend/core/actual_client/client.py`, `docs/architecture.md` rule 41): hoisted one `get_budget_history()` call above the per-category loop, sliced per category via `history[-1].from_category(cat)` instead of re-deriving history each time. Measured: 7.86s → 0.50s for identical output. Correctness independently verified — reran the *old* per-category `get_accumulated_budgeted_balance()` calls as ground truth against the new code's output for all 17 fixture categories: zero mismatches.
- **Real-world impact confirmed live in Chrome**, not just backend timing: Dashboard went from ~40+ seconds fully blank (screenshotted at 0s/13s/23s/33s, still blank each time on the unfixed build) to fully loaded in ~5s after the fix and a `majordom-api` rebuild.
- Benefits every other caller of `_compute_budget_vs_spent()` too, including #112's `get_budget_pacing_totals()` (loops it across up to 9 months) — measured 9-month pacing calc: would have been ~9×7.9s≈71s before, now 4.4s, with the exact same numbers (`discretionary_spent_elapsed: 43089.17`, matching the fork's earlier report from before this fix existed).
- **#245's duplicate-fetch half turned out bigger than its own text assumed**: live network trace (Chrome DevTools via `read_network_requests`) showed it isn't just `budget-period` firing twice — *every* Dashboard query fires twice as a matched set (`home`, `accounts`, `transactions`, all the `home/*` sub-endpoints), meaning the whole Dashboard component tree likely mounts twice, not one query re-firing. Confirmed the production Vite build is genuinely a production build (`vite build`, no mode override) — StrictMode's double-invoke behavior is stripped in real React production bundles, so it's very unlikely to be the cause despite `<StrictMode>` wrapping `main.tsx`. Also confirmed this predates today's session (#227 was filed 2026-08-30, well before #254/#112 existed) — not a regression introduced today, though not yet ruled out as *also* affected by today's changes. Doru chose to ship the measured, high-confidence compute-cost fix now and treat the remount root cause as needing its own dedicated investigation rather than continuing to dig same-session.
- Also found, incidentally: a stale/stuck Service Worker registration on the local dev browser profile caused an entirely separate blank-page symptom (zero network requests firing at all) unrelated to either perf issue — `navigator.serviceWorker.getRegistrations()` + `.unregister()` cleared it. Worth remembering as a first troubleshooting step if the local dev Dashboard ever appears fully stuck (not just slow) after multiple same-session `majordom-web` rebuilds.

### Files modified
`backend/core/actual_client/client.py`, `docs/architecture.md` (rule 41).

### Lessons
- **"Measure before deciding" (the issue's own instruction) found a completely different root cause than the issue text's own guess** ("likely inefficient per-transaction iteration") — it was a third-party library function's undocumented O(full history) cost hidden behind an innocuous-looking per-category call, not anything in this codebase's own transaction-iteration code. Guessing from the symptom description alone would have looked in the wrong place entirely.
- **A live browser reproduction surfaced a bigger problem than the backend timing investigation alone would have** — the compute-cost fix would have shipped as "#227 done, #245 next" without the Chrome network trace revealing that #245's own issue text undersold the scope (one query vs. the whole page). Worth doing the live check even when a backend-only fix already looks sufficient on paper.
- **A local-dev-only symptom (stuck Service Worker) can look identical to a real backend perf bug (blank page, no error) until you check the network tab and see literally zero requests** — the fix (unregister + reload) cost under a minute once diagnosed, but would have wasted much longer if attributed to the wrong root cause.

### Unresolved
- **#227**: only the compute-cost half is fixed. Not closing the issue yet — the "fetched twice" half (item 1 in the issue body) is unresolved and, per the finding above, bigger in scope than that item's own text describes (whole-page remount, not one query).
- **#245**: superseded in substance by this session's finding — its own root-cause hypothesis (per-call compute cost) is now fixed and measured, but its "not in scope" framing (treating the two problems as cleanly separable) doesn't hold anymore now that the remaining problem looks like a full remount, not an isolated fetch. Needs a decision next session: keep #245 open scoped to just the remount question, or file a new issue specifically for "Dashboard mounts twice" and close #245 as the compute-cost part is done. Not decided — flagging, not choosing silently.
- The full-remount root cause itself: not investigated (React Router / `AbConnectionBanner`'s new `useLocation()`/`useNavigate()` hooks from #254 as a possible *contributing*, not sole, factor was raised but not tested either way).
- Not yet committed — same evening-timestamp mechanism as #254/#112, real time was Friday ~10:40, outside the window.

## 2026-09-11 — Correction: the "whole Dashboard remounts" finding above was a testing artifact, not a real bug

### Resolved
- **Retested #245's duplicate-fetch claim properly and it did not reproduce.** The earlier finding in this same session ("*whole* Dashboard query set double-fires... likely a full component remount") came from reading `read_network_requests` across two separate `navigate` calls without clearing the log in between — every request from the first (stuck, pre-perf-fix) page load was still present when the second navigation's requests were read, double-counting the entire set. A clean single navigation with `clear: true` showed exactly one request per endpoint, `budget-period` included.
- **Independently confirmed via a mount/unmount probe**, not just network timing: a temporary `useEffect(() => { console.log('MOUNT'); return () => console.log('UNMOUNT') }, [])` added to `Dashboard`'s top level (local-only, reverted before commit, never shipped) fired `MOUNT` exactly once per page load, with 5 ordinary re-renders after it (normal — one per `useQuery` resolving independently) and no `UNMOUNT` at all. Dashboard does not remount.
- Corrected `CLAUDE.md` and the `docs/sessions/INDEX.md` row for the earlier entry today to remove the wrong "likely full component remount" claim, rather than silently rewriting the original entry above — the mistake and its correction both stay visible.

### Lessons
- **`read_network_requests` accumulates across calls on the same tab until explicitly cleared** — always pass `clear: true` (or open a fresh tab) before trusting a request count as "what this one page load actually did," especially after any earlier navigation on the same tab within the same investigation. Not doing this produced a confident-sounding but entirely wrong conclusion that nearly became this session's next open investigation.
- **A live browser finding that contradicts the issue's own original text (#245 assumed one query, this session's first pass claimed the whole page) deserved a second, independent check (the mount/unmount probe) before being reported as settled** — one measurement technique giving a surprising result is a reason to cross-check with a different technique, not to report immediately.

### Unresolved
- **#245's original claim ("`budget-period` fetched twice") still not confirmed one way or the other** — this session only disproved its own over-broad restatement of it ("everything doubles"), not the original, narrower report from 2026-08-30. Needs re-verification against the same kind of evidence #245 originally used (a real backend log showing `get_budget_status` called twice for identical params), not just a frontend network trace, before deciding whether it's still real, already incidentally fixed by later refactors, or was itself a similar measurement artifact.
- #227/#245 both stay open — the session's actual, confirmed contribution to them is the compute-cost fix (previous entry) only.

## 2026-09-11 — #245's original claim checked against real backend logs: does not reproduce

### Resolved
- **Checked against the same kind of evidence #245's original 2026-08-30 report used** (a real backend access log, not a frontend trace): `docker compose logs majordom-api` around a single, clean Dashboard page load. First attempt used `--since <absolute-timestamp-without-timezone>` and silently returned the container's *entire* log history instead of filtering — looked like 4 full repeated sets of every Dashboard endpoint, which would have been an even bigger false alarm than the one just corrected above. Caught by checking the actual `--timestamps` on each entry: they were spread across ~15 minutes (my own separate test navigations throughout this investigation), not one page load. Redone with `--since 5m` (a relative duration, which Docker parses reliably) — exactly one `budget-period` request, matching the single navigation.
- **Three independent checks now agree**: backend access log (this entry), frontend network trace with `clear: true` (previous entry), and a React mount/unmount probe (previous entry) — all show exactly one request per endpoint, one mount, no duplication, on the current code.
- **Working theory for why #245's original report doesn't reproduce anymore** (not proven, flagged as a theory): #245 was filed against code roughly 20s+ slow per page load (pre-#227-fix). A plausible, mundane explanation for "get_budget_status called twice, back to back" in that era's backend log is a real user reloading out of impatience during a page that felt stuck — two genuine page loads close together, not a code bug at all. Today's compute-cost fix (previous session entry) makes that scenario far less likely to occur regardless of whether it was ever a real duplicate-fetch bug.

### Lessons
- **`docker compose logs --since` silently ignores a malformed absolute timestamp instead of erroring** — it fell back to showing the whole log, which looked exactly like a real (and worse) finding until the per-line `--timestamps` were actually read. Use a relative duration (`--since 5m`) for this kind of live check — it can't be silently misparsed the same way.
- **This session made the same category of mistake twice in a row** (uncleared network log, then an unfiltered "since" flag) — both looked like confident, reproducible findings until checked against a second, independent signal (timestamps in both cases). Worth treating any single-method "surprising" finding as provisional until cross-checked, not just for this specific bug but as a general habit going forward.

### Unresolved
- Recommend to Doru: close #245 (compute-cost fixed and measured; "fetched twice" doesn't reproduce against fresh evidence) and close #227's remaining item alongside it, both once the evening `gh issue close` window opens — not done yet, holding per `CLAUDE.local.md`.

## 2026-09-11 — #111 implemented and live-verified: unprotected savings-goal detection, delegated via Aider (process incident caught and corrected)

### Resolved
- **#111** re-scoped from "needs live bank sync" (a wrong assessment made earlier this session) to reusing the existing `TARGET:`/`DEADLINE:` account-note goal mechanism (`get_goals()`) — no new infrastructure needed. Full reasoning in `docs/decisions.md#111-unprotected-goal-detection`. Scope cut to the account-goal half of the issue only (not the generic "suggest common sinking-fund categories" half), conservative no-name-matching trigger (fires only when a `TARGET:` goal exists AND zero sinking-fund categories are configured anywhere, reusing #112's config from earlier today) — both confirmed with Doru before dispatch.
- New `get_unprotected_goals()` chat tool (`finance__get_unprotected_goals`) + a `get_pending_items()` bell check, delegated to Aider/DeepSeek Flash (mechanical, matches the proven #41/#116/#112 shape exactly) — 4 files, ~48 lines, $0.0069.
- **Process incident during dispatch, caught and corrected same session**: the launch command's `cd` to the isolated worktree used a wrong assumed path — `git worktree add ../X` from the monorepo root resolves `../` to the monorepo's parent, not a sibling inside it (unlike this repo's own existing `life-os-<slug>` worktree convention, which the generic skill example `repo-<slug>` doesn't match). The `cd` failed silently, Aider ran on the main checkout and committed (`--yes-always`) straight to `main`, bypassing the whole review-then-confirm flow. Caught immediately via `git worktree list`/`git log` on both the checkout and the worktree. Content reviewed after the fact and found correct (4 files, exact requested scope, mechanized checks clean, live-tested both the positive case — temporarily cleared sinking-fund config, 2 items fired, config restored exactly — and the negative case against real fixture data). Doru chose to keep the already-verified commit on `main` rather than reset and redo. Fixed at the skill level (`~/.claude/skills/delegate-by-complexity/SKILL.md` + `references/decisions.md`): verify the real worktree path mechanically before any `cd`, guard pre-launch `cd`s with `|| exit 1` — protects every future delegation using this skill, not just this repo.
- **A second, smaller process mistake in the same dispatch**: launched the wait with both `run_in_background: true` AND a manual `&` inside the command — double-backgrounded it, so the "completed" notification fired for the wrapper script exiting immediately after `echo "PID: $!"`, not for Aider actually finishing (still running, detached, ~99% CPU). Caught via `ps aux`, corrected by tracking the real PID in a proper `while kill -0 <pid>; do sleep 5; done` background command instead. Also mistakenly called `ScheduleWakeup` (a `/loop`-dynamic-mode-only tool) outside of a loop context while waiting — self-caught and cancelled (`stop: true`) before it could cause confusion, no user-visible effect.
- Stale worktree (zero unique commits, Aider never wrote to it) removed after: `git worktree remove` + `git branch -d`.
- Rebuilt `majordom-api`, live-tested both branches against the real fixture stack directly (not through the UI — no frontend changes for this feature): positive case (config temporarily forced to zero sinking-fund categories) correctly produced 2 `unprotected_goal` bell items with accurate names/amounts; negative case (real saved config, one sinking-fund category already tagged) correctly produced zero items. Config restored to its exact original value afterward, confirmed via equality check.

### Files modified
`backend/{services/notification_service.py, tools/{finance/actual_budget.py, registry.py}, api/chat.py}` (delegated), `docs/decisions.md`, `~/.claude/skills/delegate-by-complexity/{SKILL.md, references/decisions.md}` (outside this repo — a shared skill fix).

### Lessons
- **A generic skill's example command (`repo-<slug>`) silently diverging from a specific repo's own established convention (`life-os-<slug>`) is a real trap** — worth pattern-matching against what already exists in the repo (`git worktree list` first) before typing a path from memory of the skill's docs, not just trusting the skill's own generic example.
- **An unguarded `cd` before a background-launched command is a silent single point of failure** — bash doesn't stop on an uncaught error, so a failed `cd` doesn't abort the script, it just runs everything after it from the wrong directory with no visible signal until the *result* looks wrong (here: a commit on the wrong branch, found only by checking afterward).
- **Double-backgrounding (`run_in_background: true` plus a manual `&`) produces a misleading "completed" notification** for the wrapper exiting, not the real work finishing — worth checking `ps aux` for the actual process whenever a background dispatch's reported duration looks implausibly short for the task size.
- **Content correctness doesn't retroactively justify skipping a process step** — the diff being right was fortunate, not a reason the isolation-then-review flow doesn't matter; the fix target was the mechanism (verify the path, guard the `cd`), not "it worked out this time."

### Unresolved
- `gh issue close 111` deferred to the evening window, same as #254/#112/#227's compute-cost note.

## 2026-09-11 — Chart.tsx multi-year x-axis label bug fixed (partial #231)

### Resolved
- Found a real, confirmed bug while preparing translation of `docs/glm-5.3/ui-audit-2026-08-30.md` (§1.2): `Chart.tsx`'s `formatDateShort()` deliberately omits the year (by design, for short-span crowded axes) — but a multi-year series (the Vehicle Detail value-over-time projection, 2024→2036) hits the exact same code path, rendering consecutive ~1-year-apart labels as e.g. "29 Aug · 28 Aug · 27 Aug · 26 Aug", indistinguishable from consecutive days.
- Fixed directly (single file, fully specified from the audit's own finding, no delegation needed): compute the series' total span in days once per series; if it exceeds 365 days, use the existing `formatDateFull()` (which already includes the year) for x-axis labels instead of `formatDateShort()`. Short-span charts (the common case) are completely unaffected.
- **Verified the exact logic with synthetic data matching the audit's own example** (12-year span, `2024-08-29` → `2036-08-17`, day-of-month stepping down by 1 per year) — labels now read `29 Aug '24 · 25 Aug '28 · 21 Aug '32 · 17 Aug '36`, no longer ambiguous. Verified the short-span control case (30 days) still selects `formatDateShort` with no regression.
- **Could not live-test in the actual Vehicle Detail page** — the local fixture stack currently has no vehicle account (`Accounts` page: "No vehicles yet"). Live-verified instead against the real Dashboard Balance Trend chart (30-day span, real fixture data) — unchanged, correct, no year shown, confirming no regression on the common short-span path.

### Files modified
`frontend/src/components/Chart.tsx`.

### Unresolved
- Live pixel-level verification against a real multi-year vehicle chart is still pending — needs a fixture vehicle account to exist. Worth doing whenever vehicle fixture data is next set up for something else.
- Addresses one finding from #231 ("chart clarity — sizing, axis labels, gridlines, color registry") — not the whole issue; #231 stays open.

## 2026-09-11 — UI audit report translated to English and committed (source for #231-240)

### Resolved
- `docs/glm-5.3/ui-audit-2026-08-30.md` had sat untracked for 12 days — written in Romanian, blocked from being committed by `check-private-data.sh`'s Romanian-language check (root `CLAUDE.md`'s English-only rule). Confirmed it's still actively used before spending effort on it: #235 quotes it almost verbatim by section number, and the same holds for the rest of #231-240 — the audit's own §5 prioritized list already maps onto those 10 already-open issues, so this was translation only, no new issues needed.
- Delegated to Aider/DeepSeek Flash in a properly isolated worktree this time (`life-os-ui-audit-translation`, matching the repo's own sibling-worktree convention, verified via `git worktree list` before any `cd` — the fix from #111's incident earlier today held). Reviewed the full 239-line output: zero Romanian text remaining (checked for diacritics directly), all markdown/table/code-span structure preserved, technical identifiers untouched, numbers reformatted to English convention. Cross-checked one finding (§1.2's X-axis year bug) against the fix already shipped this session (`f1d0a86`) — matches exactly, independent confirmation the fix addressed the real audited issue.
- Two more instances of the same license-plate-pattern false positive (an ordinary CSS class next to a word, and "a" + a 3-digit HTTP status directly followed by a short word) found and rephrased before commit — same known class already documented in `check-private-data.sh` itself.
- Worktree copied into (not merged via git history into) the main checkout — the file was untracked so a normal merge/cherry-pick had nothing to reconcile against; copied the final reviewed content directly and committed fresh on `main`. Stale worktree + branch removed after.

### Files modified
`docs/glm-5.3/ui-audit-2026-08-30.md` (new).

### Unresolved
- Not pushed — stays local until the evening `git push` request, same as everything else today.

## 2026-09-11 — VehicleDetail's vehicleQuery missing retry:false fixed (quick win #5 from the audit)

### Resolved
- Audit §4.2/quick-win #5: `getVehicle` (`vehicleQuery` in `VehicleDetail.tsx`) had no `retry: false`/`refetchInterval: false`, unlike the sibling `projectionQuery` right below it, which already guards against exactly this — a permanent 404 (stale vehicle link/bookmark) hitting the app-wide `QueryClient` default (retries ~15s, then polls every 5s forever).
- Fixed by copying the exact same pattern already used on `projectionQuery`. Live-verified with a real browser request to a nonexistent vehicle id (`/accounts/vehicle/999999`) — network trace confirms exactly one `GET /api/vehicle/999999` → `404`, no retries, "Vehicle not found" renders immediately instead of after a ~15s black screen.

### Files modified
`frontend/src/pages/VehicleDetail.tsx`.

### Unresolved
- Other quick-wins from the audit's §5 list (progress bar heights, tap targets, label sizes, ISO date formatting, raw Chat errors) not yet picked up.

## 2026-09-11 — Remaining audit quick-wins #2/#3/#4/#7 shipped

### Resolved
- **#2** (direct fix): 4 progress bars using `h-px` (literally 1px, invisible) in `GoalsSection.tsx`/`BudgetDashboard.tsx` bumped to `h-1.5` with a visible `bg-border` background.
- **#7** (direct fix): Transactions table view rendered raw ISO `{tx.date}` instead of the `formatDate()` helper already used two lines away in the list view.
- **#3/#4** (delegated to Aider/DeepSeek Flash, silently — no Pro-tier ask needed): enlarged 6 undersized tap targets (InfoIcon, New goal, Edit groups pencil, month nav ‹›, period preset buttons, row checkboxes) via padding only, no icon-size changes; bumped Chart.tsx's 9px axis labels to 11px.
- **Real merge complexity handled correctly**: the Aider worktree branched before the #2/#7 direct fixes were committed, so its diff didn't know about them. Two shared files (`GoalsSection.tsx`, `Transactions.tsx`) needed both change sets combined — used `git apply --3way` (verified clean with `--check` first) instead of a wholesale file copy (which would have silently discarded one side), since the two change sets touch non-overlapping lines.
- **Live-verified the one real risk**, not just trusted the diff: `InfoIcon.tsx` combines a fixed `w-4 h-4` with newly-added `p-1.5` padding — flagged in the delegation prompt as needing visual verification since border-box sizing could in principle squish the "i" glyph into a shrunk content box. Checked in Chrome with a zoomed screenshot: the circle renders correctly, no squishing. Also checked Transactions (list/table/select views — checkboxes, formatted dates) and Dashboard (goal card progress bars, now clearly visible).

### Files modified
`frontend/src/components/{GoalsSection.tsx, BudgetDashboard.tsx, Chart.tsx, InfoIcon.tsx}`, `frontend/src/pages/{Dashboard.tsx, Transactions.tsx}`.

### Unresolved
- Audit quick-win #6 (raw Python tool-error text like `Tool error: set_account_goal() missing 1 required positional argument: 'target'` leaking verbatim into Chat) not yet started — needs a small design choice (soften the text fed to the LLM vs. a structured frontend error-message component with a technical-detail disclosure) before implementing, flagged to Doru rather than picked silently since it touches LLM-facing error text in a codebase that's already had a real prompt-behavior bug this area (#172).

## 2026-09-11 — Last audit quick-win shipped: raw tool-error text no longer relayed verbatim to Chat

### Resolved
- Audit §4.3/quick-win #6: `backend/api/chat.py`'s tool-execution exception handler fed a bare `f"Tool error: {exc}"` string to the LLM as the tool result — real chat history showed this getting relayed verbatim to the user (`Tool error: set_account_goal() missing 1 required positional argument: 'target'`).
- Doru chose the lighter of two options presented (soften the text fed to the LLM vs. a new structured frontend error component with a details disclosure) — implemented by wrapping the exception in an explicit bracketed instruction the model reads alongside the real technical detail (`[Internal note, do not quote this verbatim to the user — explain in plain language...] {name} failed: {exc}`), rather than adding a general system-prompt bullet — a per-occurrence instruction travels with every single failure, so it can't be one more rule buried among many to forget (this codebase's own documented "#160/#166 bit twice now" pattern).
- **Live-verified against the app's real chat LLM** (not just a code read): replayed the exact real-world scenario (a tool call missing a required argument) through a one-shot completion call with the new wrapped result — model responded "Oops! It seems I missed specifying the target amount for your savings goal... How much would you like to save?" — friendly, no raw text, and correctly used the real cause (missing `target`) to ask a genuinely useful follow-up, not just a generic "something went wrong."
- **Honest finding, not just a clean win**: also replayed the *old* unwrapped format once for comparison — the model handled it fine that one time too (same non-deterministic pattern already documented for #172 — `temperature: 0.2` is already set, but "0.2" isn't "0"). The real-world audit evidence shows the bad outcome (verbatim relay) does happen with the old format; this fix makes the good outcome reliable rather than probabilistic, not "unlocks" it from a guaranteed-bad state — worth being precise about, since a single A/B sample can't fully prove a probabilistic improvement either way.

### Files modified
`backend/api/chat.py`.

### Unresolved
- All of the audit's own §5 "quick wins" list (#1-#7) are now shipped. Medium-effort items (§5's second tier: chart height, tooltips/drill-down, loading skeletons, entry animations, color-palette registry, sticky headers, date grouping, account-type icons, PWA icons) not started — next natural pickup for continued UI-polish work, whenever that's next in sequence.

## 2026-09-11 — Evening window opened: push + 5 issue closes, then chart-height/Y-gridlines shipped (partial #231)

### Resolved
- **Evening housekeeping, once real time entered the 18:00-23:00 weekday / Friday-17:00+ weekend window**: `git push` (`84273b3..23edf75`, all of today's earlier work). Closed the 5 issues left deferred through the day purely on the timestamp rule, each with a comment recapping what shipped: **#254** (AB reconnect banner), **#112** (annual budget pacing), **#111** (unprotected-goal detection), **#227** (compute-cost half only — closed with an explicit note that the "fetched twice" half isn't confirmed fixed, just doesn't reproduce), **#245** (closed on the doesn't-reproduce finding from earlier today). Commented on **#231** (not closed — see below) noting the day's partial progress on it.
- **Chart height + Y gridlines shipped** (audit §5 medium-effort item #8, partial #231): single file, `frontend/src/components/Chart.tsx` — donut 72→120px (radius/stroke scaled proportionally), line chart 100→160px plus 3 intermediate Y gridlines alongside the existing min/max labels, bar chart 80→140px plus 5 Y gridlines with `formatCurrency`-formatted value labels (bars/x-axis-label row/value row all given a shared `pl-8` offset so the new left-edge gridline labels don't overlap the first bar). Pure SVG/div, no new library — consistent with the #134 decision (Recharts threshold still not hit).
- **`/plan-feature` run first**: confirmed single-file scope, checked `decisions.md`/`architecture.md` for chart-related rules (none blocking — #134's SVG/div-only decision was the relevant one), grepped all backend call sites for `chart_type: "bar"` to confirm the one caller is finance-only (safe to reuse `formatCurrency` for bar gridline labels, no non-currency bar-chart consumer exists).
- Implemented directly (not delegated) — visual SVG/CSS work needing iteration and live judgment, not a clean spec for Aider. `tsc --noEmit` clean.
- **Live-verified all three affected chart_type variants in the browser** (not just typecheck), after a `majordom-web` rebuild against the local fixture stack: the Dashboard's `line` "Balance trend" widget (gridlines + min/max labels visible, taller chart), a `pie` spending-breakdown chart via chat (donut visibly larger, fits its card), and a `bar` spending-vs-income chart via chat (5 gridlines with correct `€0`/`€14.903`/.../`€59.613` labels, bars aligned under their x-axis labels).
- **Found live, while getting the bar chart to render for verification**: `finance__get_spending_trend` has no bullet + example in `_build_system_prompt()`'s tool-guide section, unlike the other four chart tools — two different phrasings either called the wrong tool (`get_spending_chart`) or produced no reply at all (nothing saved to `chat_history` either — not investigated further, flagged in the new issue instead of scope-creeping into a second fix this session). Matches the documented #160/#166 pattern exactly. Filed as **#255** rather than fixed inline, per Doru's explicit "create an issue for what you found" instruction mid-session — kept separate from #231's own work.
- Commented on #231 (not closed): item #8 done, but #231 also covers axis-label detail, gridline-styling refinement, and the color-registry unification, none of which this session touched.

### Files modified
`frontend/src/components/Chart.tsx`, `CLAUDE.md`.

### Lessons
- **A dashboard widget with a plausible matching name isn't necessarily the generic component being changed** — `Dashboard.tsx`'s "Balance trend"/"Expenses Structure" cards looked like natural places to verify the `LineChart`/`PieChart` edits, but "Expenses Structure" turned out to be a separate bespoke `ExpensesStructureWidget`, not the generic `Chart.tsx`. Grepping for the actual import (`from '../components/Chart'`) and the actual `<Chart chart_type=...>` call sites was the reliable way to find real verification targets, not matching on visual similarity or card title.
- **Not every generic-chart variant has a UI entry point outside chat** — `chart_type: "bar"` is only ever reached through one backend tool (`get_spending_trend`), itself only reachable via chat tool-calling, which turned out to be unreliable for the exact reason discovered (#255). Verifying it took phrasing the chat question three different ways before the right tool fired — worth remembering that a chart type's live-verification path may itself depend on an unrelated, separately-broken mechanism.

### Unresolved
- #231 stays open — item #8 done, rest of the medium-effort tier (tooltips/drill-down, skeletons, animations, color registry, sticky headers, date grouping, PWA icons) not started.
- #255 filed, not investigated further this session (root cause identified — missing system-prompt bullet — but not fixed).
- The silent-reply incident noted in #255 (a chat query that produced no text, no chart, and nothing persisted to `chat_history`) is flagged inside that issue but not itself diagnosed — may be a separate, smaller bug worth a dedicated look if it recurs.

## 2026-09-11 — Chart tap-tooltip shipped (audit §5 item #9, tooltip half only, partial #232)

### Resolved
- **Scoped down before implementing**: item #9 bundles tooltip-on-tap with drill-down-to-Transactions. Investigating drill-down first surfaced two real reasons to split it out rather than do both: `Transactions.tsx`'s `getInitialFilters()` (line 78-79) ignores `dateFrom`/`dateTo` entirely whenever `categoryIds` is falsy — a date-only drill-down (what the bar chart needs, filtering by month, not category) would silently apply no filter at all unless `categoryIds: []` is passed explicitly to dodge the early-return. Separately, `BarPoint.x` only carries a pre-formatted label (`"Aug-26"`), not the raw month/year `get_spending_trend` already computes server-side — drill-down would need either a fragile label-reparse or a backend contract change. Presented both findings to Doru before writing any code; chose tooltip-only now, drill-down as its own later pass.
- **Tooltip shipped**: `Chart.tsx` — line chart points and bar chart columns now toggle a small tap tooltip (exact date + value per line point; all series' values per bar column, reusing the existing `formatCurrency`/`formatNumber` helpers). Line-chart points got a larger invisible hit circle (`r=10`) layered over the visible `r=2.5` dot — the visible dot alone is far below the 44px tap-target guideline the audit's own §2.1 already flagged elsewhere.
- **Found and fixed live, not just in code review**: the tooltip's first implementation always centered itself on the tapped point (`-translate-x-1/2`) — for the bar chart's last column and (in principle) the line chart's last point, the tooltip's `whitespace-nowrap` content overflowed past the card's right edge into the surrounding page. Fixed by reusing the exact same first/last clamping idea the line chart's x-axis labels already use elsewhere in this file (anchor to the column/point's own edge instead of centering, for the first and last position only). Verified both the bug and the fix live in Chrome (bar chart's last column, before and after).
- Implemented directly, no delegation (small, visual, needed live iteration). `tsc --noEmit` clean throughout. Commented on **#232** (not closed) — drill-down and entrance-animation, the issue's other two parts, untouched.

### Files modified
`frontend/src/components/Chart.tsx`.

### Lessons
- **Investigating the harder half of a bundled audit item before writing code is what caught the real scoping reason to split it** — the `Transactions.tsx` filter bug and the missing raw-date-per-point contract weren't visible from the audit text itself, only from actually reading the code the drill-down would need to touch. Splitting first, then confirming with Doru, avoided building half a feature only to discover it needed a second, bigger piece to actually work.
- **A tooltip/overlay positioned by centering on a data point needs explicit edge-clamping the same way axis labels do** — this file already had the first/last clamping pattern for x-axis labels before this session; the new tooltip didn't inherit it automatically just by being in the same component, and the overflow only became visible by actually clicking the edge-most point/bar in the browser, not from reading the JSX.

### Unresolved
- #232 stays open — tooltip (this entry) done, drill-down and entrance animation not started.
- Drill-down's two blockers (Transactions.tsx's date-only-filter gap, get_spending_trend's missing per-point date) are documented in the #232 comment but not fixed — next pass whenever drill-down is picked up.

## 2026-09-11 — Detail-page loading skeleton shipped (audit §5 item #10, worst-offender half only, partial #234)

### Resolved
- **Scoped down before implementing, same pattern as #9**: item #10 covers skeleton loading everywhere; the audit's own §2.4 already narrows it — `WidgetLoading` (Dashboard spinner+text) is called "good," only `VehicleDetail.tsx`/`AccountDetail.tsx`'s literal blank `<div className="min-h-dvh bg-background" />` on first load (audit's own "most visible before/after in the entire report") is the real problem. Scoped this pass to just those two.
- New shared `frontend/src/components/DetailPageSkeleton.tsx` — both pages have the same back-button/label/title/value header shape, so one `animate-pulse` skeleton (extracted immediately, not copy-pasted twice, per `duplication-prevention.md`) covers both. Swapped in for the blank-div return in each page.
- Implemented directly (3 files, mechanical, no delegation needed). `tsc --noEmit` clean. Commented on **#234** (not closed) — rest of item #10 (per-widget Dashboard skeletons) untouched, `WidgetLoading` judged sufficient there per the audit's own read.
- **Could not visually capture the skeleton frame itself** — the local fixture stack resolves the underlying queries too fast to catch mid-load in a screenshot, the same reason this bug was invisible locally and only real on the slower LXC. Verified instead: no regression (both real pages render correctly after the swap, screenshotted), `tsc --noEmit` clean, markup follows existing Tailwind conventions already used in the same files.
- **Incidental confirmation**: a real vehicle fixture (`Duster`) now exists on the local stack (didn't before, per earlier session notes) — used it to also visually confirm the multi-year x-axis fix from earlier in #231's history is correctly showing years (`29 Aug '24` / `28 Aug '28` / `27 Aug '32` / `26 Aug '36`) on real 12-year vehicle-projection data, closing out that item's own "pending" note from a prior session.

### Files modified
`frontend/src/components/DetailPageSkeleton.tsx` (new), `frontend/src/pages/{VehicleDetail.tsx, AccountDetail.tsx}`.

### Lessons
- **The audit document itself, read carefully, often already contains the scoping decision** — §2.4 explicitly said `WidgetLoading` was "good" and named the two worst pages by name; the scoping-down here didn't require new investigation the way #9's drill-down split did, just reading the audit's own text more closely before assuming the whole item needed doing at once.
- **A visual fix that can't be verified against local fixture speed still deserves a stated reason why, not silence** — noting "couldn't catch the loading frame, here's what was checked instead" keeps the verification honest instead of implying something was seen that wasn't.

### Unresolved
- #234 stays open — worst-offender half (this entry) done, per-widget Dashboard skeletons not started (deferred to `WidgetLoading` already being deemed adequate there).
- Live pixel-check of the skeleton's actual appearance (not just its code) still pending — needs either a throttled network or a slower environment (the LXC) to actually see it render before it resolves.

## 2026-09-11 — Category color registry shipped (audit §5 item #12, partial #231), real duplication found and filed separately

### Resolved
- **Investigated before implementing**: audit §1.6 named 5 constants across 4 files as "fragmented." Reading each usage site found only 2 (`Chart.tsx`'s `SEGMENT_COLORS`, `Dashboard.tsx`'s `EXPENSE_COLORS`) are true category-color duplicates competing for the same identity space; `GROUP_COLORS` (budget groups) and `GOAL_COLORS` (goals/accounts) are different domains entirely, and `getBudgetColor`'s percentage-based green→red gradient is a semantic state color the audit itself says should stay separate. Also found, investigating the real fix: `Dashboard.tsx`'s `ExpensesStructureWidget` is a second, independent pie-chart implementation (CSS `conic-gradient`) duplicating `Chart.tsx`'s `PieChart` — the exact pattern #134 already solved once for other chart components, reintroduced here after that migration.
- **Surfaced the scope choice to Doru before coding**: fix just the color registry (all 4 arrays → one shared hash-based `colorForKey()`, `ExpensesStructureWidget` stays as its own implementation but sources colors from the same registry), or also collapse `ExpensesStructureWidget` into `<Chart chart_type="pie">` and kill the duplication outright. Doru chose color-registry-only; the duplication finding filed as **#256** rather than folded in, since it's a real second task (bigger footprint, real visual-regression risk swapping a CSS conic-gradient for an SVG donut on a widely-used card).
- **Shipped**: new `frontend/src/lib/chartColors.ts` — one shared `CHART_PALETTE` + a deterministic `colorForKey(name)` string hash, so the same key always resolves to the same color regardless of a widget's own sort order (the actual root cause — all 4 old arrays picked colors by array *position*, not by item identity). `SEGMENT_COLORS`/`EXPENSE_COLORS`/`GROUP_COLORS`/`GOAL_COLORS` all removed, every call site switched from `PALETTE[i % n]` to `colorForKey(<stable name/id>)`. `INCOME_COLOR` moved to the same file as a fixed constant (not hashed — a deliberate semantic color, not competing for a palette slot).
- Implemented directly (4 small frontend files + 1 new file, mechanical once the investigation was done). `tsc --noEmit` clean.
- **Live-verified the actual bug, not just the refactor**: before the fix, the same August 2026 data showed "Home"/"Groceries"/"Health"/"Car Costs" in different colors on the chat's pie-chart card vs. Dashboard's Expenses Structure widget (confirmed via a real screenshot comparison, colors noted explicitly). After the fix, rebuilt `majordom-web` and re-screenshotted both — all four category names now show identical colors in both places. Also spot-checked `BudgetDashboard` (Categories Watchlist group dots) and `GoalsSection` (Financial Goals progress bars) still render correctly with the new hash-based colors, no missing/undefined colors.
- Commented on **#231** (item #12 done, issue stays open for the rest of §1) with the before/after finding and a pointer to #256.

### Files modified
`frontend/src/lib/chartColors.ts` (new), `frontend/src/components/{Chart.tsx, BudgetDashboard.tsx, GoalsSection.tsx}`, `frontend/src/pages/Dashboard.tsx`.

### Lessons
- **The audit's own grouping of "4 fragmented palettes" wasn't quite right at the domain level** — reading the actual usage sites (not just the audit's list of constant names) found only 2 of the 4 were genuinely the same kind of thing (category colors); the other 2 are different domains that happen to share the "hardcoded array of hex colors" shape. Worth checking what a flagged pattern *actually* represents before assuming the audit's framing maps 1:1 onto the real fix.
- **Index-based color assignment (`PALETTE[i % n]`) is the actual root cause of "same item, different color across widgets"** — not the fact that palettes were duplicated across files. A single shared array alone (without switching to identity-based lookup) would not have fixed the audit's actual complaint; the fix had to change *how* a color is chosen, not just *where* the color values live.
- **A duplication finding surfaced while investigating a color bug is still a separate task**, even though it's directly adjacent to the fix being made — filing it as its own issue (#256) rather than silently expanding scope matched the session's own "one feature at a time" rule, and gave Doru a real choice (bigger fix now vs. smaller fix now + tracked debt) instead of deciding it silently.

### Unresolved
- #256 filed, not investigated further — a real architecture choice (collapse `ExpensesStructureWidget` into the generic `Chart` component, or leave it duplicated but color-synced) still needs deciding.
- #231 stays open — axis-label refinement and the rest of §1 (gridline styling detail beyond what #8 already shipped) not covered by any of this session's #8/#9/#10/#12 work.

## 2026-09-11 — Sticky page header shipped (audit §5 item #13, page-header half), real min-h-dvh/h-dvh root cause found and fixed app-wide

### Resolved
- **Started narrow, found something bigger mid-implementation**: added `sticky top-0 bg-background/95 backdrop-blur z-40` to the shared `PageHeader.tsx` (used by all 13 pages — one-file fix for the whole app, confirmed via grep before starting). The class applied correctly (verified via computed style: `position: sticky`, `top: 0px`) but had zero visible effect when scrolling Transactions.
- **Root-caused it live, not guessed**: every page's outer wrapper used `min-h-dvh bg-background flex flex-col overflow-y-auto`. `min-h-dvh` is a *minimum* height only, so the div grows taller than the viewport to fit its content instead of clipping — its own `overflow-y-auto` never actually engages (`divScrollHeight === divClientHeight`, `divScrollTop` stuck at 0 the whole time), and the real scroll happens on `document`/`body` instead (`document.scrollingElement.scrollTop` was the value actually changing on scroll, confirmed via a live JS check in the browser). `position: sticky` was relative to a container that structurally never scrolls, so it had no effect despite being correctly applied.
- **Surfaced the expanded scope to Doru before touching more files**: the same exact `min-h-dvh bg-background flex flex-col overflow-y-auto` wrapper string appears in 15 places across 13 pages (grepped first, all byte-identical). Doru chose to fix all of them now — one mechanical `sed` replace (`min-h-dvh` → `h-dvh`, matched on the full class string so the unrelated `min-h-dvh` usage in Vehicle/AccountDetail's "not found" states — no `overflow-y-auto`, not affected by this bug — was correctly left alone).
- Documented as **`architecture.md` rule 42** — a new full-page wrapper combining `overflow-y-auto` with `min-h-dvh` (instead of `h-dvh`) will silently reintroduce this exact bug, with no error and no obvious symptom beyond "sticky doesn't work."
- `tsc --noEmit` clean. Live-verified on 3 pages after rebuild (Transactions, Dashboard, Settings) — header now stays pinned with the backdrop-blur visible against scrolling content underneath, bottom nav/fixed bars unaffected.
- Commented on **#237** (this issue's first bullet, §2.5) — its other bullets (date-grouping/sticky date headers, ISO-vs-formatted date inconsistency, AccountDetail's flat list, mixed locale strings) stay open.
- **Caught and fixed a private-data-scanner false positive before commit**: the new header className put the z-index utility directly ahead of the background-opacity utility, tripping the license-plate regex (same `[A-Z]{1,2}-\d{2,3}-[A-Z]{2,3}` Tailwind-adjacency false positive documented before) — moved the z-index class later in the string so it isn't immediately followed by a short class, verified the scanner passes clean, no visual change (Tailwind doesn't care about class order).

### Files modified
`frontend/src/components/PageHeader.tsx`, `frontend/src/pages/{RecurringReviewPage,AccountDetail,Dashboard,Accounts,DuplicatesReviewPage,Transactions,UnreconciledReviewPage,Planned,VehicleDetail,UncategorizedReviewPage,BudgetRealismReviewPage,Analytics,Settings}.tsx`, `docs/architecture.md` (rule 42).

### Lessons
- **A CSS class applying correctly (confirmed via computed style) is not the same as it having an effect** — `position: sticky` with `top: 0px` looked completely correct in DevTools-equivalent checks; the bug was entirely in the *container's* scroll behavior, several layers away from the property that looked broken. Checking the sticky element's own computed style first (quick, ruled out "did the class even apply") before checking `scrollTop` on the actual scrolling elements (found the real cause) was the right order — cheap check first, not the other way around.
- **`min-h-*` and `h-*` read as nearly interchangeable at a glance but have opposite implications for a container meant to scroll internally** — `min-h` guarantees a floor and lets the box grow past it; `h` caps it, which is what `overflow-y-auto` needs to ever actually trigger. Worth treating any `overflow-y-auto` + `min-h-*` combination as suspect on sight going forward, not just when a bug report names it.
- **The same license-plate-pattern false positive (a short Tailwind class immediately followed by a numeric class immediately followed by another short class) keeps recurring across unrelated sessions** — third documented instance now. The fix (reorder, don't touch the regex) stays cheap each time, but it might be worth a quick look at whether a slightly narrower regex could dodge Tailwind's own naming convention without losing real license-plate detection, next time this comes up a fourth time.

### Unresolved
- #237 stays open for its other 4 bullets (date grouping/sticky date headers, date-format consistency, AccountDetail structure, locale mixing).
- The "sticky date headers (Transactions)" half of audit item #13 still needs date-grouping to exist first (item #14, not started) — can't make non-existent headers sticky.

## 2026-09-11 — Date grouping + monthly summary shipped (audit §5 item #14, partial #237)

### Resolved
- New shared `frontend/src/lib/groupByMonth.ts` — takes an already date-sorted list plus a `getDate`/`getAmount` accessor pair, returns month buckets (label + items + net total). Written once and used by both `Transactions.tsx` (list and table views) and `AccountDetail.tsx`'s transaction list, since both needed the identical grouping shape — checked `DuplicatesReviewPage.tsx`'s existing month-based UI first (`decisions.md`'s "month-drill-down for duplicates vs. a flat list" note) but it's backend-driven month cards, a different shape entirely, not reusable here.
- **Transactions**: both views now render a month header (e.g. "SEPTEMBER 2026") with a net income-minus-expenses total above each month's rows, instead of one continuous flat list, for both the list view (`space-y-4` of grouped sections) and the table view (a `colSpan`-ed header `<tr>` per group via `Fragment`).
- **Real small correctness fix found while restructuring**: the list view's category-chip dedup (hides a repeated category chip on consecutive same-category rows) was computed once globally across the whole flat array. Grouping by month meant a chip could now be hidden right under a new month header if the last row of the previous month happened to share a category with the first row of the next — visually wrong once a header sits between them. Recomputed per-group instead (resets at each month boundary), replacing the old global `chipHidden` array entirely.
- **Account Detail**: same month header + net total above its transaction list — previously a flat 50-row list with zero grouping and zero visible dates per row (unchanged: still no per-row date shown, out of scope for this pass, the month header now supplies that context instead).
- `tsc --noEmit` clean. Live-verified all three surfaces (Transactions list, Transactions table, Account Detail) against real fixture data spanning September/August 2026 — correct grouping, correct per-month net totals, chip dedup behaving correctly at month boundaries, no regressions.
- Commented on **#237** (two more of its bullets addressed) — date-format consistency, AccountDetail's other structure gaps, and locale-string mixing stay open; "sticky date headers" (the other half of #13) is now buildable since the headers exist, but not made sticky this pass.

### Files modified
`frontend/src/lib/groupByMonth.ts` (new), `frontend/src/pages/{Transactions.tsx, AccountDetail.tsx}`.

### Lessons
- **A dedup/adjacency computation that worked correctly against a flat list can silently produce a wrong-looking result once that list gets visually segmented** — the global `chipHidden` array wasn't *incorrect* in the sense of a bug in isolation, but adding month headers changed what "adjacent" should mean for the user's eye, and the old computation didn't know that. Worth checking any per-row "same as previous" logic whenever a flat list gains new visual grouping, not just assuming it still applies unchanged.
- **Checking an existing similar-sounding pattern before writing a new helper paid off, even though the answer was "no, don't reuse it"** — `DuplicatesReviewPage.tsx`'s month UI looked like a candidate at a glance, but reading it confirmed the shapes are genuinely different (backend-paginated drill-down vs. client-side grouping of an already-fetched list), so writing a fresh small helper was correct, not a duplication-prevention violation.

### Unresolved
- #237 stays open — date-format consistency (ISO vs formatted, mixed locales) and Account Detail's other structure gaps not addressed.
- Making the new month headers sticky (stacking under the already-sticky page header, `architecture.md` rule 42) not done this pass — a natural, now-unblocked follow-up for #13's remaining half.

## 2026-09-11 — Priority review + 7 issues closed in one batch, delegated mostly to DeepSeek Flash

Started as a "what's actually essential" priority review (Phase C/C2 status confirmed, market-data-source decision for Phase D flagged as the next big fork) — turned into a full batch of fixes once Doru confirmed working through the list. Mid-session, Doru explicitly authorized a more autonomous mode for the rest of the session: proceed on recommended answers without stopping to ask, open a GitHub issue instead of blocking on architecture/product questions, push after each merge, prefer DeepSeek Flash over Pro/subagent by default, and note everything done here.

### Decision recorded (not a fix)
- **vehicle-manager becomes a fully standalone app with its own frontend** — Majordom connects to it over API only, never owns/embeds its UI. Resolves the "explicitly undecided" question left open by the 2026-07-05 opt-in-profile stopgap. Full reasoning: `decisions.md#vehicle-manager-standalone-frontend`. Referenced from #150 and #221.

### Resolved
- **#252 + partial #236 — vehicle-account self-service linking.** A disabled/unlinked vehicle row in Accounts.tsx previously did nothing when tapped. New dedicated link path (separate from the existing price/date-gated `sync_vehicle_account()`, which always created a new AB account and risked duplicates): a tag-only internal endpoint, a vehicle-manager `link-account` endpoint storing `ab_account_id` directly, and a frontend sheet to pick an unlinked vehicle-manager profile or create a bare new one and link it in one step. Delegated to a Claude subagent fork (Doru's explicit choice over DeepSeek Pro for this one, per the skill's per-Senior-task question) given 9 files across 3 services. Live-verified against the local fixture stack (a disabled XTB row → sheet → linked to the existing "kia" profile → clickable, no new AB account created, no balance/name change).
- **#202 — private-data scanner blind spot.** `check-private-data.sh`'s unstaged fallback only saw tracked files; untracked new files went unscanned while the tool reported "clean". Fixed via a temporary `git add -N` (intent-to-add) for untracked files, reset afterward via the script's existing `trap`. Delegated to DeepSeek Flash, live-verified (fake IBAN in a new untracked file now caught, file stays untracked after the run).
- **#233 — tap targets, chart label, contrast.** Table-view checkboxes now have a cell-filling tap target (list view was already fine, just mismeasured by the audit); `InfoIcon` grew 16px→32px; Dashboard's period chevrons grew 28px→36px; one outlier 9px chart label matched to the file's other 10px labels; `muted-2` contrast against the app background computed and fixed from ~2.5:1 to ~5:1 (WCAG AA). Delegated to DeepSeek Flash.
- **#235 — Settings' two fake toggles.** "Include credit limits"/"Show archived accounts" were local-`useState`-only, looking identical to real wired toggles. Swapped to the same `InertRow` "Not built" convention already used elsewhere in the same file — no new persistence built (that's a separate feature decision). Delegated to DeepSeek Flash.
- **#238, partial — desktop max-width.** Wrapped the routed content, bottom nav, and AB-reconnect banner in a `md:max-w-[480px] md:mx-auto` centered column with a border, so desktop no longer stretches edge to edge. Pull-to-refresh, infinite scroll, haptics (same issue, different concern) explicitly left out of scope. Delegated to DeepSeek Flash, live-verified in browser.
- **#240 — PWA manifest + dead code.** Generated two properly safe-zone-padded maskable icon variants (existing icons run edge-to-edge and would crop badly on Android if marked maskable as-is — generated via Pillow, 70% scale centered on the app's background color), added a "Scan receipt" shortcut pointing at the real `/receipt` route, removed the forced-portrait constraint, deleted the confirmed-dead `TransactionItem.tsx` (zero references, broken category lookup). Delegated to DeepSeek Flash — caught and fixed one real dispatch mistake: the `--file` scope didn't include `TransactionItem.tsx`, so its "remove dead component" commit message oversold what actually happened (only the manifest changed); re-verified zero references and deleted it in a follow-up commit before merging.

### Investigated, deliberately not shipped
- **#124's remaining part — "Budget Automations" (repeat/copy last month).** Actual Budget's own docs mark this experimental/unfinished (nightly builds only); the installed `actualpy` has zero fields or methods related to it. Same failure shape as the goal-template syntax bug from 2026-07-02 (silently wrong numbers from a guessed schema). Documented the finding as a comment on #124 rather than guessing at an undocumented schema for a financial write; parked until it gets the same kind of direct live-investigation the goal-template syntax got.
- **#239 — Analytics tab placeholder.** This issue's own text already flags the right gate (check against `product-plan.md` — what does a reports page make Majordom notice, not just fill a tab). Left a scoping comment rather than guessing; real product-direction question for a dedicated session.

### Files modified
`docs/decisions.md` (new entry), `backend/api/{vehicle_accounts_internal.py, vehicle_value.py}`, `backend/core/vehicle_client/client.py`, `tools/vehicle-manager/app/{database.py, main.py, majordom_client.py, models.py}`, `frontend/src/lib/vehicleValueApi.ts`, `frontend/src/pages/{Accounts.tsx, Dashboard.tsx, Settings.tsx, Transactions.tsx}`, `frontend/src/components/vehicles/LinkVehicleSheet.tsx` (new), `frontend/src/components/{InfoIcon.tsx, Chart.tsx, BottomNav.tsx, AbConnectionBanner.tsx}`, `frontend/src/App.tsx`, `frontend/tailwind.config.js`, `frontend/public/manifest.json`, `frontend/public/icon-{192,512}-maskable.png` (new), `scripts/check-private-data.sh`. `frontend/src/components/TransactionItem.tsx` deleted.

### Lessons
- **Always trust-but-verify a delegated commit's own message against its actual diff, not just against the spec.** #240's Aider commit claimed "remove dead component" but the diff showed only the manifest changed — caught by diffing the commit itself, not by re-reading the prompt. Root cause was mine, not Aider's: the dispatch's `--file` list never granted write access to `TransactionItem.tsx`.
- **A worktree branched before other parallel branches merge will show a misleading `git diff main`** once main moves ahead — #238's branch (based on the pre-batch commit) looked like it "reverted" #233/#235/#240's changes when diffed against the *current* main, purely because it never had them, not because anything was undone. Diffing the isolated commit itself (`git show HEAD` / `git log -p -1`) is the reliable check, not `git diff main` once several branches are landing in sequence.
- **Live UI testing needs real fixture data, and the local stack sometimes needs a beat after a rebuild before Actual Budget is ready** — the Accounts page showed `€0`/"No vehicles yet" right after `majordom-api` restarted, looking like a real regression, until a few seconds' wait + reload showed the real fixture data (9 accounts, `Duster` linked). Worth checking freshness before concluding a page is broken.
- **Doru's mid-session move to a more autonomous mode** (recommended-answer default, open-an-issue-and-move-on for architecture questions, push after each merge, Flash-first) was a direct, explicit instruction for the rest of this session — not a standing change to `delegate-by-complexity`'s "always ask before Pro" rule, which stays as written for future sessions unless he says otherwise.

### Unresolved
- #238 stays open for pull-to-refresh / infinite scroll / haptics.
- #239 stays open, gated on a product-plan-aligned scoping session.
- #124 stays open on its Budget Automations half, gated on a live-investigation session against the real AB schema.
- Phase D's market-data-source decision (the dependency unblocking #42 and the rest of the portfolio work) is still open — flagged at the start of this session as the highest-leverage next decision, not picked up today.

## 2026-09-11 — Second wave: market-data-source research issue, 3 more small fixes closed, #256 scoped and left open

Continued the same batch after Doru said "hai sa continuam" — same autonomous mode as above (recommended-answer default, issue-and-move-on for architecture questions, push after each merge).

### Resolved
- **#257 opened** (not implemented — an issue, not a fix) — researched viable market-price data providers for Phase D (Alpha Vantage, Twelve Data, Finnhub; IEX Cloud confirmed dead, Yahoo Finance confirmed unreliable per current sources) and wrote up the real open questions (refresh cadence, ticker coverage for actually-held ETFs, provider-outage tolerance) as facts only Doru has — not decided, deliberately, since this is exactly the kind of call the new autonomous-mode instruction still routes to an issue rather than a guess.
- **#251 — think:false on the 5th Ollama call site** (`csv_import.py::_suggest_categories_llm`). Two-line fix, implemented directly rather than delegated — too small to be worth a worktree round-trip.
- **#255 — missing system-prompt guide bullet for `finance__get_spending_trend`.** Investigated rather than just adding a bullet: found the real relationship is the same text-vs-chart-card "twin" pattern already used for `get_transactions`/`list_transactions`, since `get_spending_history` (which already has a bullet) is this tool's plain-text counterpart. Framed the new bullet that way instead of a standalone mention. Also found the same gap likely applies to `finance__get_spending_chart` and `finance__get_budget_chart` — filed separately as **#258** rather than expanding this fix's scope.
- **#221 — vehicle-manager location disambiguation.** Turned out to be more than ambiguity: `docs/architecture.md`'s Platform Architecture diagram referenced `backend/tools/vehicle/`, which was actually deleted back in #138's 2026-07-03 extraction — a stale diagram, not just an unclear one. Fixed the diagram (pointing at the real `backend/core/vehicle_client/` thin HTTP client + the external service's real location), added a routing row to `CLAUDE.md`'s task-type table, fixed `docs/INDEX.md`'s vehicle row (was pointing at a session log instead of the service), and commented the cross-directory build context in `docker-compose.yml`.

### Investigated, deliberately not shipped
- **#256 — ExpensesStructureWidget vs Chart.tsx's PieChart duplication.** Checked before coding: the color-consistency half is already fixed (both already call the same `colorForKey()` from today's earlier `chartColors.ts` work) — what's left is a real layout decision, not a mechanical dedup. `Chart.tsx`'s `PieChart` is built for a standalone card (big donut + full-width list); `ExpensesStructureWidget` is built to fit the Dashboard's tighter widget grid (small donut + compact inline legend). Swapping one for the other would visibly change the Dashboard's layout. Same tradeoff `LineChart`'s `bare` prop already solved once (#134) — noted as the likely right shape (a compact/bare `PieChart` variant) without picking it unilaterally. Left a comment, issue stays open.

### Files modified
`backend/api/csv_import.py`, `backend/api/chat.py`, `CLAUDE.md`, `docker-compose.yml`, `docs/INDEX.md`, `docs/architecture.md`.

### Lessons
- **A "just add the missing bullet" task can reveal the tool has no real text/chart-twin relationship documented yet** — worth checking the sibling tool before writing an isolated bullet, same as #255 here. Doing this once already surfaced a second, identical gap (#258) rather than requiring a future audit to find it.
- **A docs staleness bug can hide behind what looks like a pure clarity issue.** #221 was filed as "ambiguous," but the ambiguous line was also just wrong — the directory it named didn't exist anymore. Worth actually checking a referenced path exists on disk before assuming a docs fix is purely cosmetic.

### Unresolved
- #257 (market-data source) needs Doru's input on refresh cadence / ticker coverage / outage tolerance before any code gets written against a provider.
- #258 (get_spending_chart / get_budget_chart bullets) not started.
- #256 needs a layout-shape decision (bare PieChart variant vs. accepting a Dashboard visual change) before implementing.

## 2026-09-11 — Third wave: two Phase C2 chat tools shipped (#153 item 1, #246), both investigated live before delegating

Continued after Doru said "Continua si intreaba daca e nevoie" (keep going, ask if actually needed). Picked up the next Phase-C2-sequenced items (`docs/product-plan.md`'s own ordering: administration → coaching/intelligence → UI polish → Phase D) rather than jumping straight to Phase D, since #257 (market data) still needs Doru's input before any code can be written against it.

### Resolved
- **#153, item 1 only (schedules summary with total).** Before delegating, live-queried the real local database to find out HOW Actual Budget/actualpy actually stores a schedule's amount — turned out to be a signed-cents value buried inside the linked `Rules` row's JSON `conditions` column, not a plain column anywhere (confirmed via `docker compose exec` against the running `majordom-api` container, reading a real schedule's `rule.conditions`). Wrote the delegation spec with that verified shape already in hand rather than letting Aider guess at an undocumented schema — same caution as the #124/goal-template precedent, except this one turned out to be fully solvable once investigated (unlike #124's genuinely-undocumented AB feature). New `finance__get_recurring_schedules_summary` chat tool, full provider-wiring chain (client → provider pass-through → Protocol → tool → registry → system-prompt bullet). Delegated to DeepSeek Flash. Live-verified by temporarily activating the one real (inactive) test schedule in the local fixture data, confirming the parsed amount and formatted summary were correct, then restoring it to its original inactive state exactly.
- **#246 (monthly savings-rate chart).** Checked #112's math first, per the issue's own note — found `get_monthly_stats()` (via the shared `_compute_monthly_totals()` helper) already correctly excludes starting-balance rows, transfers, and balance-adjustment rows from both income and spending, so no new filtering logic was needed at all, just composition of an existing, already-correct data source. Decided independently (a pure implementation-shape call, not a product question) to deliver this as a chat-card tool matching `get_spending_trend`'s existing pattern, not a new persistent Home/Analytics widget — sidesteps the "no new dashboard widgets" rule and the #239 Analytics-tab gate entirely. Along the way, extracted `get_spending_trend`'s month-range-building logic into a shared `_build_months_list()` helper (this was exactly the second occurrence of that loop, the documented point to extract, not before or after). Delegated to DeepSeek Flash. Live-verified across 3 real fixture months, including the zero-income-month division guard, and confirmed `get_spending_trend`'s own output was byte-identical after the extraction (no regression).

### Files modified
`backend/core/actual_client/client.py`, `backend/core/finance/{actual_budget_provider.py, provider.py}`, `backend/tools/finance/actual_budget.py`, `backend/tools/registry.py`, `backend/api/{chat.py, finance_charts.py}`.

### Lessons
- **When two pending branches touch the same file in different places, merge them into `main` with a real `git merge` before testing — never blindly `cp` one branch's file over the working tree once a second branch has already landed.** Caught live this session: overlaying #246's `chat.py`/`registry.py` on top of an already-#153-merged `main` via plain `cp` silently deleted #153's bullet and registry entry (they weren't in #246's branch, which was cut before #153 merged). `git merge --no-ff` on the second branch did the correct three-way merge instead, combining both cleanly with zero conflicts (the two additions sit in different parts of the same files). The earlier-documented "stale-base diff looks like a revert" lesson from wave one was the read-only version of this same trap; this is the write-side version, and it actually loses work if not caught before pushing.
- **Investigating the actual, real data shape before writing a delegation spec keeps a tool from becoming a second #124.** Both tools this wave touched a similarly "could be undocumented" area (a schedule's stored amount, a metric's exclusion rules) — the difference between "safe to delegate now" and "needs a dedicated research session" was simply whether 10 minutes of querying the real local database settled the question first.

### Unresolved
- #153 stays open for item 2 (goal-impact recommendation), which needs the goal system to know monthly contribution rate — not yet built.
- #167 (Expense Coverage / Coast-FIRE card) is the next well-scoped Phase C2 item (already has file references and done-when criteria) but is Senior-tier (income classification write-flow + calculation + new UI card, several files) — per the standing per-Senior-task rule, needs Doru's Pro-vs-subagent call before dispatching, not decided automatically.

## 2026-09-11 — #167 shipped via Claude subagent fork; two real bugs caught in review before merge

Doru chose "subagent Claude (fork)" over DeepSeek Pro when asked, for this Senior-tier dispatch (income classification write-flow + Expense Coverage calculation + new card, 12 files). This is Phase C2's last well-scoped item — #42 stays blocked on #257 (market data source), the rest of the coaching cluster (#153 item 2, #177) needs other prerequisites.

### Resolved
- **#167 — Expense Coverage card + income classification.** Fork's implementation matched the spec closely: `INCOME_TYPE:` notes-tag mechanism mirroring the existing goal `TARGET:`/`DEADLINE:` pattern, `_compute_expense_coverage()` shared between `get_home_data()`'s already-open session and the standalone chat-tool path (mirroring `_compute_goal_progress()`'s established shared-helper shape), outlier exclusion correctly adapted from `list_budget_realism_flags()`'s existing heuristic rather than inventing a new one. One subtle, correct judgment call worth noting: the fork used `cat.group.is_income` (not `cat.is_income`, which `_compute_monthly_totals()` uses) to decide which categories are eligible for income classification — verified against real data this was the RIGHT choice, not an inconsistency: "Freelance" has `cat.is_income=False` but `group.is_income=True`, and would have been wrongly excluded from classification eligibility with the other field.

### Found in review, fixed before merge
- **Two read-only tools (`get_income_classifications`, `get_expense_coverage`) were wrongly added to `_PROPOSAL_TOOLS`.** That set's real meaning (not obvious from its name) is "yield this tool's raw JSON straight to the frontend and stop the LLM loop" — correct for tools with a matching frontend card/chart renderer (`get_fire_chart`, `get_goals_chart`), wrong for these two, which have no renderer in `Chat.tsx`'s `parsed.type` dispatch and were designed (per their own system-prompt bullet) to have the LLM read the JSON and produce conditional natural-language commentary, same as the structurally-identical `get_unprotected_goals`/`get_reached_goals` (correctly NOT in that set). Uncaught, this would have dumped raw JSON into the chat UI — the exact "raw tool-error text leaking verbatim into Chat" bug class already fixed once this repo's history (2026-08-30 audit §4.3). Fixed by removing both from the set.
- **The system-prompt bullet didn't distinguish "not classified yet" from "classified, but 0% this specific month."** Live-tested via the real chat LLM (not just a unit-level check): asked "how much of my expenses does my passive income cover" before classifying anything (correct: 0%, not classified), classified "Freelance" as passive via the real confirmation-card flow, verified the backend immediately returned the correct updated state (`has_any_classified_income: true`) — then asked the same question again in the *same conversation*, twice, and got back the stale pre-classification answer both times. A brand-new, single-message conversation (no history) asking the identical question got the correct, updated answer immediately. Not a backend bug (verified the tool's own output was correct on every call) and not the #172 temperature issue (`_stream_with_tools()` already has `temperature: 0.2`) — narrowed to a conversational-memory characteristic the existing "answer fresh, don't reuse memory" system-prompt language doesn't quite cover (that language is framed around *different entities*, not *re-asking after your own action changed the answer*). Filed separately as **#259** rather than papering over it with a bigger prompt-engineering change during this review — tightened the bullet's has_any_classified_income-first phrasing anyway (a real improvement, verified via the fresh-conversation test), but the deeper issue needs its own session.

### Files modified
`backend/api/{category_actions.py, chat.py}`, `backend/core/actual_client/client.py`, `backend/core/finance/{actual_budget_provider.py, provider.py}`, `backend/tools/finance/actual_budget.py`, `backend/tools/registry.py`, `frontend/src/components/{CategoryActionCard.tsx, GoalsSection.tsx}`, `frontend/src/lib/api.ts`, `frontend/src/pages/{Dashboard.tsx, Planned.tsx}`.

### Lessons
- **A fork's own "live-verified" claim can still hide a real bug if the verification didn't exercise the exact failure path.** The fork's report described thorough testing (real LLM call, real confirm endpoint, screenshot-checked card) and all of it was true — but it never asked the *chat* about Expense Coverage through the actual `finance__get_expense_coverage` tool-call path, only checked the Home-page card and the classify-tool's own confirmation flow. The `_PROPOSAL_TOOLS` miscategorization would have been invisible to every test it ran. Reviewing the actual code against the established pattern (comparing against `get_unprotected_goals`/`get_reached_goals`) caught it before a live chat test would have.
- **"Never answer from memory of an earlier turn" (the existing system-prompt rule) apparently doesn't fully cover "the user's own action, mid-conversation, changed what the correct answer now is."** Worth remembering as a *category* of prompt gap distinct from the entity-confusion case that rule was originally written for — see #259.

### Unresolved
- #259 (LLM gives a stale answer when the same question is re-asked mid-conversation after a confirmed write) needs its own investigation session.
- Phase C2 is now effectively done except #42 (blocked on #257) and #153 item 2 (needs goal-contribution-rate awareness). Remaining backlog items need either a product decision (#218, #256) or are larger, less-scoped features (#177, #186, #157, #140, #136, #114) — natural next planning-session material rather than more same-session quick delegation.

## 2026-09-11 — #169 decided and shipped in one pass (real design discussion, not a delegation)

Checked the remaining backlog for anything else quick and well-scoped after #167 — found none (#157 needs real production infra Claude shouldn't touch alone, #136 explicitly flags restore as too risky to scope casually, #114 needs undesigned vault-file integration, #140/#218 are explicitly "decision needed, not a refactor"). Reported this honestly instead of forcing another delegation, and asked Doru which decision he wanted to have now. He picked #169 (Home account/balance overview) — a real 3-way design conversation happened in chat (single stat line vs. compact accounts card vs. no new UI at all), not a rubber-stamp.

### Resolved
- **#169 — decided: a single "On budget" line at the top of Home**, distinct from Portfolio Independence's off-budget total and vehicle values (both of which already exist elsewhere) — not a full accounts card (too close to duplicating the Accounts tab) and not a single net-worth figure (would hide the on-budget/off-budget split Portfolio Independence depends on). Implemented directly (small, mechanical once the design was settled) — reuses `get_home_data()`'s already-computed per-account balance/`off_budget` data, zero extra AB queries; `off_budget` already excludes vehicles by construction. Live-verified against real fixture data (€6.597) against a manual sum from the Accounts page.

### Files modified
`backend/core/actual_client/client.py`, `frontend/src/lib/{api.ts}`, `frontend/src/pages/Dashboard.tsx`.

### Unresolved
- Same short list as above, unchanged: #218/#256 need a product call, #177/#186/#157/#140/#136/#114 need a dedicated planning/scoping session.

## 2026-09-11 — #257 attempt surfaced a real dependency gap; #258 shipped

Picked #257 (market-data source decision) up next per the priority review, since it blocks #42 and Phase D. Got Doru's answers on refresh cadence (1x/day) and outage tolerance (OK, informational only) — but the ticker-coverage question needs real held tickers to test against, and there's currently no system that tracks holdings (Ghostfolio dropped 2026-08-28, no replacement built yet). Documented this as the real blocker on #257 (comment, not a re-scope) and moved to #258 instead.

### Resolved
- **#258 — added system-prompt bullets for `finance__get_spending_chart` and `finance__get_budget_chart`**, same #160/#166 pattern already fixed for `get_spending_trend` (#255). Small, manually written and read in this conversation — skipped the pre-commit-review subagent per the skill's own skip criterion. Live-verified against the real chat LLM in the browser: both new phrasings correctly rendered their chart cards (budget-vs-actual bar chart, spending donut chart).

### Files modified
`backend/api/chat.py`.

### Lessons
- **Deciding a market-data provider (#257) has a deeper prerequisite than the issue text assumed**: testing real ticker coverage needs a holdings tracker to supply the tickers, and that tracker doesn't exist yet (Ghostfolio dropped, no replacement). The dependency chain is holdings tracker → known ticker list → provider test → provider decision, not just "decide the provider."

### Unresolved
- #257 stays paused until a holdings tracker (Doru's own build, not Ghostfolio) exists to supply real tickers to test against.

## 2026-09-11 — #259 investigated and fixed: two real bugs, not one prompt tweak

Picked up #259 (LLM gives a stale answer when the same question is re-asked after a confirmed
write) — the one item flagged since the #167 session as needing its own investigation. Traced it
past what the issue text assumed.

### Resolved
- **#259 — two distinct causes, both fixed in `Chat.tsx`'s `handleSendText` history filter.**
  (1) The filter's `PROPOSAL_ROLES` set (5 hardcoded role names) was missing every card role added
  since it was written (`category_action`, `goal_proposal`, `fuel_log`, ...) — the user message
  that triggered one of those newer cards was left dangling with no reply in the LLM's context.
  Generalized to check role shape instead of a hardcoded list (`architecture.md` rule 43). (2) That
  alone wasn't sufficient — live-verified twice that the model repeated its own stale text answer
  with zero tool calls when the same question was re-asked right after a confirmed write, even with
  a new, explicit system-prompt instruction telling it not to. Researched the failure mode online
  first (WebSearch) rather than guessing at more prompt text — found the same pattern reported on
  the LangChain forum ("agent copies history without calling the tool again"), confirming this is a
  known class of bug with a known engineering fix, not something specific to this model. Fixed
  mechanically: any assistant text reply from before the most recent card is now dropped from the
  LLM's context; only text after the last card (already reflecting current state) is kept.
- Discussed the fix's scope with Doru in plain language before implementing (what "no more context"
  would and wouldn't mean) since it sounded, from the outside, like it might remove conversational
  memory entirely — clarified it only drops the assistant's own past text after a write, never the
  user's messages, and never touches server-side persisted history (what's shown on screen).

### Files modified
`frontend/src/pages/Chat.tsx`, `backend/api/chat.py`, `docs/architecture.md` (rule 43).

### Lessons
- **A live repro is worth more than the issue's own root-cause guess.** #259's text suspected the
  entity-scoped "don't answer from memory" instruction just needed broader wording. Reproducing it
  live in the browser (classify income → ask → ask again) surfaced a real, separate frontend bug
  (the stale `PROPOSAL_ROLES` list) the issue never mentioned, and then proved the prompt-only fix
  still failed twice in a row before writing any mechanical fix.
- **Checking how the wider community handles a failure mode is worth doing before committing to a
  fix design**, not just for exotic problems — a 2-minute search confirmed this exact bug shape is
  known and well-understood (LLM agents skipping tool calls when a similar answer sits in context),
  which validated the "fix it in code, not in the prompt" direction against outside precedent
  instead of just this repo's own prior #160/#166/#172 experience.
- **A rate-limit error (HTTP 429) can look exactly like "the model didn't call a tool" if you only
  check for the absence of a `tools=` log line.** Caught mid-investigation by reading the actual
  displayed error text before concluding the fix had failed a second time — re-ran the test once
  the rate limit cleared and got a clean, correct result.

## 2026-09-11 — #234 closed (fully resolved by earlier sessions), #236 shipped

Picked up the next UI-polish item after #259. Checking #234 against what earlier sessions had
already fixed (`retry: false`, raw exception text in Chat, the worst-offender skeleton) found
nothing real left — closed instead of "picked up." #236 (Accounts page) had a similar partial
surprise: one of its three bullets (disabled/dimmed unlinked-vehicle rows) was already resolved by
the existing `LinkVehicleSheet` flow, not mentioned as fixed anywhere the audit could have known
about it.

### Resolved
- **#234 closed**, no code change — all three bullets already fixed in prior sessions.
- **#236 — account-type icons + bigger "Add vehicle" button**, `Accounts.tsx`. Added a
  type→icon map (`TrendingUp`/`Car`/`Landmark`/`Home`, `Wallet` default) and switched the
  bare text link to the bordered-pill button style already used elsewhere (`Dashboard.tsx`).
  Live-verified by setting a real account's type to Investment via `AccountDetail`'s existing
  picker and confirming the icon changed on the list.

### Files modified
`frontend/src/pages/Accounts.tsx`.

### Lessons
- Re-verifying an audit item against current code before treating its bullets as still-open
  work paid off twice in one sitting (#234 fully stale, #236 one-third stale) — the UI audit
  is from 2026-08-30 and several sessions have shipped fixes since without always linking
  back to update it.

## 2026-09-11 — #231 closed (fully resolved), #237 shipped and closed (locale unification)

Continued the same re-verify-before-picking-up pattern: #231 turned out fully resolved too
(sizing/gridlines/color-registry all shipped in earlier sessions; the one literal remaining
sub-item — bar chart's permanent label showing only the first series — is covered by the
tap-tooltip shipped for #232, which the audit's own text explicitly accepts as a sufficient
fix ("or tooltip")). #237's five bullets were down to two real ones after checking: the
ISO-vs-formatted date claim was already fixed, sticky header and grouping already shipped —
only the locale mix was still real.

### Resolved
- **#231 closed**, no code change — all cited sub-items already fixed.
- **#237 — date-locale unification.** New `frontend/src/lib/formatDate.ts`
  (`formatDate`/`formatMonthYear`/`formatWeekdayDate`), mirroring `formatCurrency.ts`'s
  pattern and rationale, fixed at `en-GB`. Replaced 9 duplicated `toLocaleDateString(...)`
  call sites across `Transactions.tsx`, `TransactionListCard.tsx`, `DuplicatesReviewPage.tsx`
  (2 sites), `ProposalCard.tsx`, `VehicleDetail.tsx`, `NewGoalSheet.tsx`, `GoalsSection.tsx`,
  `groupByMonth.ts`, and `Dashboard.tsx`. One of them (`VehicleDetail.tsx`) was a real bug,
  not just an inconsistency — it used `'nl-NL'`, rendering genuinely Dutch text ("30 aug
  2026", "augustus") in an otherwise English UI, apparently `formatCurrency.ts`'s `LOCALE`
  constant copied for the wrong purpose. Chart.tsx's own axis-label formatters (different
  need — compact, no locale) were left untouched. Live-verified: Transactions list/table
  dates match, VehicleDetail now renders in English, Dashboard header unaffected.
- **#237 closed** — all 5 original bullets resolved across this and earlier sessions.

### Files modified
`frontend/src/lib/formatDate.ts` (new), `frontend/src/{components/{GoalsSection,NewGoalSheet,
ProposalCard,TransactionListCard}.tsx,lib/groupByMonth.ts,pages/{Dashboard,
DuplicatesReviewPage,Transactions,VehicleDetail}.tsx}`.

### Lessons
- A locale-consolidation bug hid a real, user-visible defect (Dutch text in an English app)
  behind what read like a purely cosmetic "mixed conventions" complaint — worth actually
  running the three locale strings against real output before assuming they're cosmetically
  equivalent, rather than picking one arbitrarily.

## 2026-09-12 (continued from the evening before) — #232 drill-down delegated to DeepSeek Flash, shipped

Doru gave two standing corrections this session: decide-it-yourself now also covers
sequencing/priority recommendations, not just implementation-shape calls (codified in
`CLAUDE.md`); and lean toward delegating well-scoped tasks to DeepSeek Flash more readily.
Picked #232's drill-down remainder as the next item using the first rule (clear
recommendation, no need to ask), then applied the second by delegating it instead of
implementing directly.

### Resolved
- **#232 drill-down half.** Delegated to DeepSeek Flash via `delegate-by-complexity`
  (isolated worktree `life-os-drilldown-232`) — a well-specified 3-file task (backend JSON
  contract addition, a tooltip button in `Chart.tsx`, a real bug fix in
  `Transactions.tsx`'s `getInitialFilters()`). One-shot, no back-and-forth needed. Reviewed
  the diff line-by-line against the spec (matched closely, including the exact last-day
  calculation asked for), tested on the main checkout via the documented
  checkout-branch-files-then-restore pattern rather than setting up a second npm
  environment in the worktree. Live-verified in the browser: tapping a bar's tooltip shows
  "View transactions", navigates to `/transactions` correctly filtered to that month
  (checked via the Filters panel), and back/forward doesn't re-apply the stale filter.
  Merged with `--no-ff`, worktree and branch cleaned up after. Entrance animation (this
  issue's third bullet) stays open.

### Files modified
`backend/tools/finance/actual_budget.py`, `frontend/src/components/Chart.tsx`,
`frontend/src/pages/Transactions.tsx`, `CLAUDE.md` (collaboration rules).

### Lessons
- First message to the freshly-rebuilt containers after a `docker compose up -d` can race
  the restart and silently go nowhere (no backend log line at all, not even an error) —
  caught by checking `docker compose logs` for the expected request instead of trusting an
  empty-looking chat scroll position, and simply resending fixed it.

## 2026-09-12 — #239/Analytics: idea captured on #218, not built

Started scoping #239 (Analytics placeholder) with the mandatory `product-plan.md` check —
surfaced it as a real product-direction fork (static reports page vs. the plan's own
"notices, not displays" bar) rather than deciding it solo. Doru brought a concrete reference
(MoneyMatter's own Analytics tab, screenshots) and a new option: fold the dead `Planned` tab
into Analytics specifically (not Dashboard), rather than the two options #218 already listed.
Explicitly not a priority to build now — captured as a comment on #218 (the issue that
already owns this exact "two dead nav slots" decision) with the concrete UI concepts worth
keeping (period-over-period comparison framing, category % change, net worth history) so the
idea isn't lost, and cross-linked #239 to #218 to avoid splitting the same decision across two
issues. No code changed.

**Same session, minutes later: Doru asked to implement it after all** ("Implementeaza daca
totul e clar cu flash daca se poate"). Two things weren't actually clear yet, surfaced before
touching code rather than guessed: (1) removing the Planned tab reverses an explicit prior
decision (`decisions.md#planned-tab-added` — Doru asked for it as a *persistent* tab, not a
button-launched page, after an earlier MoneyMatter review) — confirmed explicitly rather than
assumed, since reversing a past named request needs sign-off, not a silent call; (2) the
"notices" comparison-framing half needs new backend computation not yet speced — scoped down
to a clean, already-buildable v1 (reuse the 4 existing REST endpoints as-is) and confirmed
that split before dispatching, deferring the comparison layer to its own issue.

### Resolved
- **#218/#239 — Analytics v1 shipped, Planned tab removed.** Delegated to DeepSeek Flash via
  `delegate-by-complexity` (worktree `life-os-analytics-v1`) — new typed fetch functions in
  `lib/api.ts` for the 4 existing `/api/finance/*` endpoints, `Analytics.tsx` rewritten to
  render them via the existing `<Chart />` component (same one already used in chat), `Planned`
  route/nav-entry/page removed. One gap in my own dispatch caught at review: `Planned.tsx` was
  correctly left untouched by Aider (it was `--read`-only, not `--file`) but I'd asked for its
  deletion in the prompt anyway — fixed with a small manual follow-up commit rather than
  re-dispatching. Live-verified in browser: all 4 charts render real data, bottom nav is back
  to 5 tabs, and a chart's own month-nav arrows correctly refetch on this new standalone page
  (donut chart: September → August 2026, 30 transactions, correct category breakdown).
  New `docs/decisions.md#planned-folded-into-analytics` entry supersedes
  `#planned-tab-added`. Deferred comparison-framing half split out to #260.

### Files modified
`frontend/src/{lib/api.ts,pages/Analytics.tsx,components/BottomNav.tsx,App.tsx}`,
`frontend/src/pages/Planned.tsx` (deleted), `docs/decisions.md`.

### Lessons
- **A file only meant for context (`--read`) but named in the prompt's own "delete this
  file" instruction won't actually get deleted by Aider — the `--file`/`--read` split is a
  mechanical permission, not just an intent signal, and a prose instruction can't override
  it.** Caught at review (diff --stat didn't show `Planned.tsx`), fixed with one extra
  commit rather than treating it as a failed dispatch. Next time: if a prompt tells the model
  to delete a file, that file belongs on `--file`, not `--read`, even if nothing else in it
  needs editing.

## 2026-09-12 — vehicle-link bug fix, then a full backlog retrospective resolved the open packaging-vs-new-capabilities fork

Doru reported a real production bug (screenshot from his phone): linking an account to an
existing, already-listed vehicle profile failed with a raw `Vehicle N not found` error, and
asked for the fields to auto-fill when an existing profile is selected. Root-caused and fixed
`LinkVehicleSheet.tsx`'s instant-link-on-tap flow; NOT live-verified (Claude in Chrome was
disconnected this session, likely because Doru was away from his computer) — committed and,
on his explicit request, pushed anyway, flagged clearly as unverified.

Then, at Doru's request, ran a full retrospective across all 39 open issues + recent closures
to surface real priorities. The `gh issue list --milestone "M2.5"` check corrected a
misreading mid-retrospective (0 open/6 closed means *done*, not *not started*). The
retrospective's own conclusion — the real priority isn't a specific issue, it's the
still-open "package for others vs. new capabilities" fork from root `CLAUDE.md`, whose own
"revisit once stable" condition was actually just met — led Doru to resolve it for real.

### Resolved
- **Vehicle-link bug + UX fix** (`frontend/src/components/vehicles/LinkVehicleSheet.tsx`,
  `Accounts.tsx`): selecting an existing profile now fills the fields as a read-only preview
  and requires the same confirm button as creating new, instead of linking instantly on tap;
  a not-found response on link now shows a clear message and triggers a vehicle-list
  refetch instead of leaving the sheet stuck on stale data. Hit a real false positive from
  the private-data pre-commit scanner along the way: a short phrase describing that HTTP
  status coincidentally matched the Romanian-license-plate regex (1-2 letters, 2-3 digits,
  2-3 letters) — reworded the comment rather than touching the shared regex.
- **Backlog retrospective → the open "packaging vs. new capabilities" fork resolved.**
  Doru's actual answer wasn't either original option: `vehicle-manager` (currently
  backend-only) gets a real standalone frontend of its own (Fuelio referenced as the shape),
  and a *new*, separate investment/portfolio-tracking app takes over everything Phase D
  originally assigned to majordom-financiar itself — both API-connected to Majordom, same
  relationship majordom-financiar already has with vehicle-manager's backend today. Visual
  polish (MoneyMatter referenced again) comes after both; packaging for other users comes
  last. Recorded in root `CLAUDE.md`'s "Open fork" entry,
  `docs/decisions.md#portfolio-becomes-separate-service`, and `docs/product-plan.md`'s Phase D
  (re-scoped, original "inside Majordom" framing kept only for history). Tracked as new
  issues #261 (vehicle-manager frontend) and #262 (investment app) — neither specced further
  than "build it separately," both need their own dedicated scoping session before any code.

### Files modified
`frontend/src/components/vehicles/LinkVehicleSheet.tsx`, `frontend/src/pages/Accounts.tsx`,
`CLAUDE.md` (root), `docs/{decisions.md,product-plan.md}`.

### Lessons
- A retrospective asked for as "let's see what priorities we find" surfaced something bigger
  than a priority ranking — the actual blocker was a strategic decision sitting unmade since
  2026-07-05, waiting on a condition (Phase C/C2 stable) that had quietly already been met.
  Worth periodically checking not just "what's next in the backlog" but "is there an old
  open fork whose waiting condition already passed."

## 2026-09-12 (continued) — vehicle-manager standalone-app plan written, Phase 1 shipped via /loop

Doru refined the direction further: `vehicle-manager` becomes its own real app ("majordom
transport"), a new investment app the same way, "Majordom" itself eventually becomes a
separate MCP-hub — but sequenced last, after two real domain apps exist to design it against
(tracked as #263, deliberately deferred). Investigated vehicle-manager's actual current split
before writing anything: value/depreciation math already lives there; consumption/distance/
cost-per-km/monthly-cost/mileage/costs-summary charts were still computed inside
majordom-financiar itself, fetching raw rows over HTTP — the real gap between "has data" and
"has its own charts." Wrote `tools/vehicle-manager/docs/standalone-app-plan.md` (5 phases),
resolved one real open question with Doru (auth on vehicle-manager's internal
majordom-financiar↔vehicle-manager calls too, via a service token, not just the new frontend's
user login — cheaper to build now than retrofit once "standalone" means direct phone access).
Then started Phase 1 through the `/loop` skill at Doru's explicit request, to keep advancing
across phases without a prompt at every step.

### Resolved
- **Phase 1 — chart computation migrated from majordom-financiar into vehicle-manager.**
  Delegated to DeepSeek Flash. Two real problems caught at review, not left for later: Aider
  only refactored 3 of the 6 target functions before stopping (finished the remaining 3
  directly, same pattern, then deleted the now-dead `_get_fuel_intervals`/
  `_get_monthly_cost_and_distance` helpers it left behind), and it left a stray closing
  markdown code-fence at the end of the new `charts.py` that broke the file's Python syntax
  entirely — caught by `ast.parse`, not visible in the diff review alone. Live-verified: all 6
  new vehicle-manager endpoints tested directly against real fixture data, and
  majordom-financiar's chat tools compared point-value-for-point-value against the same data
  through the full proxy chain — byte-identical to before the migration.

### Files modified
`tools/vehicle-manager/docs/standalone-app-plan.md` (new), `tools/vehicle-manager/app/{charts.py
(new),main.py}`, `majordom-financiar/backend/{core/vehicle_client/client.py,tools/finance/
vehicle.py}`, root `CLAUDE.md`, `docs/decisions.md`.

### Lessons
- **`ast.parse` (or the equivalent for the language) on every file an Aider dispatch touches is
  worth doing every time, not just when something looks off** — the stray markdown fence this
  session would have crashed the container on import, and nothing in the diff itself screamed
  "broken" at a glance (it read as a plausible trailing artifact easy to skim past).
- A delegation that stops partway through a multi-function mechanical task (3 of 6 done, same
  pattern each time) isn't necessarily a sign the task was mis-scoped — finishing the rest
  directly, using the model's own already-correct first three as the template, was faster and
  just as reliable as a re-dispatch.

### Unresolved
- Phase 2 (auth on vehicle-manager, service-token pattern) is next, per the loop Doru asked to
  keep running.

## 2026-09-12 (continued) — Phase 2 (vehicle-manager auth) implemented directly, live-verified

Security-sensitive, so implemented directly rather than delegated — `delegate-by-complexity`'s
own rubric puts this at "Foarte greu," never Aider, stated explicitly to Doru before starting.

### Resolved
- **Phase 2 — every vehicle-manager route now authenticated.** New `tools/vehicle-manager/app/
  auth.py`: a per-user JWT (`POST /auth/login`, own secret/own users, mirrors majordom-financiar's
  own `backend/api/auth.py` shape) or a shared `X-Service-Token` header (constant-time
  `hmac.compare_digest`, fails closed if unconfigured) for majordom-financiar's internal calls —
  added to all 21 routes except `/health` (must stay open for the Docker healthcheck).
  `majordom-financiar/backend/core/vehicle_client/client.py` now sends the service token on every
  call via a new `_auth_headers()` helper (both the shared `_request()` path and the separate
  `import_fuelio()` upload path), reading it from a new `settings.vehicle_manager.service_token`
  field — settings singleton, not `os.environ` directly. New env vars wired through
  `docker-compose.yml`, both `.env.example` files (majordom-financiar's, and a new one for
  vehicle-manager itself for standalone/non-compose runs), and the real local `.env`.
  `requirements.txt` pins `python-jose==3.3.0`/`passlib[bcrypt]==1.7.4`/`bcrypt==3.2.2` — copied
  majordom-financiar's own pins exactly, including the `bcrypt==3.2.2` pin needed because passlib
  1.7.4 breaks on bcrypt 4.x+, rather than rediscovering that the hard way.
- **Live-verified**, not just typechecked (browser still unavailable this session, same as
  Phase 1 — curl/docker-exec substituted): `/health` unauthenticated → 200; `/vehicles`
  unauthenticated → 401; wrong JWT → 401; wrong service token → 401; a real login round-trip →
  valid JWT → 200; valid service token → 200. Confirmed majordom-financiar's existing chat tools
  still work end-to-end through the new auth — `get_vehicle_costs_summary()` and
  `get_vehicle_consumption_chart(vehicle_name="Duster")` called directly inside the `majordom-api`
  container, real fixture data, unchanged from before Phase 2.
- Ran the `pre-commit-review` subagent before committing (Phase 2 counts as security-sensitive
  code that went straight from direct implementation to merge without an in-conversation
  line-by-line review) — verdict: safe to commit. It flagged one pre-existing, non-blocking
  design point worth naming here rather than treating as a gap: the JWT path's fallback secret
  (`"change-me-this-is-not-secure"` if `VEHICLE_MANAGER_JWT_SECRET` is unset) doesn't fail closed
  the way the service-token path does — but that's an exact, deliberate mirror of
  majordom-financiar's own already-accepted `backend/api/auth.py:32` convention for this
  single/two-user, private-network threat model, not a regression introduced here.

### Files modified
`tools/vehicle-manager/app/{auth.py (new),main.py}`, `tools/vehicle-manager/{requirements.txt,
README.md,.env.example (new)}`, `majordom-financiar/backend/core/{vehicle_client/client.py,
config/settings.py}`, `majordom-financiar/{docker-compose.yml,.env.example,.env}`,
`scripts/check-private-data.sh`, `tools/vehicle-manager/docs/standalone-app-plan.md`.

### Lessons
- **A file only "read before compaction" and not re-read afterward is a real risk when
  reconstructing it from a conversation summary** — `main.py` was rewritten via `Write` from
  memory of the summary rather than a fresh `Read`, before realizing that risk; `git diff`
  immediately after showed the reconstruction was accurate (only the intended auth lines
  differed from the original), but this should have been a fresh `Read` first, not a
  reconstruct-then-verify. Caught this time; don't rely on catching it every time.
- **The private-data scanner only scans diff lines, not whole-file content** — a brand-new file
  makes every line in it "added," so a safe, already-committed-elsewhere code idiom
  (`X = os.getenv(...)`) can get flagged purely because it's never been part of a diff before,
  not because it's actually unsafe. Worth remembering before assuming a scanner block means the
  new code itself is wrong — check whether the identical pattern already lives unflagged
  elsewhere first.
- The scanner's placeholder whitelist (`your_`/`paste_`/`change_`/`example`) doesn't include
  `generate_` despite `generate_a_random_secret_here` already being used, unflagged, in an
  existing committed `.env.example` — hit again this session on two new files. Used
  `change_this_to_a_random_secret` instead rather than adding another whitelist prefix for a
  single incident.

### Unresolved
- Phase 3 (new frontend scaffold) is next, continuing the same `/loop`.

## 2026-09-12 (continued) — Phase 3 (vehicle-manager frontend scaffold) built via Claude fork

Foundational/convention-setting work (routing, auth pattern, API client shape, Docker/Nginx) —
per `delegate-by-complexity`'s standing rule, stopped and asked Doru Pro-vs-Claude-fork before
dispatching a Senior-tier task; he chose the fork.

### Resolved
- **Phase 3 — new standalone frontend for vehicle-manager, branded "Majordom Transport" in the
  UI** (Doru's own naming; kept separate from the `vehicle-manager` technical naming used
  everywhere in code/infra — #150's service-naming question stays open and unrelated). New
  `tools/vehicle-manager/frontend/`: Vite+React+Tailwind, mirrors majordom-financiar's own
  frontend conventions but trimmed to only what this phase needs (no push notifications, no
  receipt flow, no chat, no webfonts). Copied verbatim (confirmed portable): `Chart.tsx` +
  `formatCurrency.ts`/`chartColors.ts` — not wired to any page yet, Phase 4 does that. Adapted:
  `auth.ts` (own localStorage keys, dropped AB-down handling), `Login.tsx` (new branding), a
  minimal `api.ts` (`login()`/`getVehicles()` only), a placeholder `VehicleList.tsx` proving the
  pipeline works rather than final UI, trimmed `App.tsx`/`main.tsx` (one protected route, no
  bottom nav yet). New `vehicle-manager-web` docker-compose service (port 3010).
- **Found and fixed two real gaps along the way.** (1) `tools/vehicle-manager/frontend/` needed
  its own `.gitignore` for `node_modules/` — confirmed via `git check-ignore -v` that neither the
  repo root's `.gitignore` nor majordom-financiar's own reaches this new sibling directory. (2)
  `package-lock.json`'s randomly-generated sha512 integrity hashes coincidentally matched
  `check-private-data.sh`'s VIN-number regex (17 mixed alphanumeric chars) — fixed by excluding
  common lockfiles from the scanner's diff entirely (a structural, path-based exclusion, not a
  regex weakening), since lockfiles are machine-generated package metadata structurally
  incapable of containing any of the scanner's leak categories. Verified directly: all 189
  `resolved` entries in the new `package-lock.json` point to the public npm registry, nothing
  private embedded in it.
- **Live-verified through the real Nginx proxy** (browser still unavailable this session, same
  substitution as Phases 1-2): `GET /` serves the correct title; `POST /auth/login` through the
  proxy returns a real JWT; `GET /vehicles` with that JWT returns the real fixture vehicles
  (Duster/Test Car/kia) unchanged; the same request without a token → 401; `/health` → 200; the
  built JS asset serves with 200. `npm run build`/`npx tsc --noEmit` clean, `docker build`
  clean, `docker compose config` clean.
- Ran `pre-commit-review` (fork-produced code, no in-conversation line-by-line review) — safe to
  commit, no violations. One non-blocking nit noted (unused `clsx`/`tailwind-merge` deps in
  `package.json`) — left for Phase 4 rather than pruned now, per the reviewer's own suggestion,
  since Phase 4's real list/detail pages will very likely use them anyway.

### Files modified
`tools/vehicle-manager/frontend/` (new: `package.json`, `tsconfig.json`, `postcss.config.js`,
`tailwind.config.js`, `vite.config.ts`, `index.html`, `.gitignore`, `Dockerfile`, `nginx.conf`,
`src/{index.css,main.tsx,App.tsx,lib/{auth.ts,api.ts,formatCurrency.ts,chartColors.ts},
pages/{Login.tsx,VehicleList.tsx},components/Chart.tsx}`), `majordom-financiar/{docker-
compose.yml,.env.example}`, `scripts/check-private-data.sh`,
`tools/vehicle-manager/docs/standalone-app-plan.md`.

### Lessons
- **A new sibling directory outside an existing project's own root isn't automatically covered
  by that project's `.gitignore`** — `tools/vehicle-manager/frontend/` sits outside both the
  repo root's and majordom-financiar's own `.gitignore` reach. `git check-ignore -v` before
  assuming coverage, every time a new top-level-ish directory is created, not just once.
- **A character-pattern regex (VIN, license plate, IBAN) will eventually collide with
  high-entropy machine-generated content** (base64 hashes, UUIDs) purely by chance — the fix for
  that class of false positive is excluding the *file type* structurally incapable of containing
  the real thing, not patching the regex itself or rephrasing content (which doesn't apply to
  content nobody wrote by hand).

### Unresolved
- Phase 4 (real frontend pages: vehicle list UI, detail pages, charts wired up, Fuelio import)
  is next, continuing the same `/loop`.

## 2026-09-12 (continued) — Phase 4 (vehicle-manager frontend pages) built via Claude fork, one real bug found and fixed after merge

Senior-tier again (many files, defines the detail-page pattern) — asked Doru Pro-vs-fork again
per the standing per-dispatch rule; he chose the fork again. This time the fork's own explicit
"do not commit, do not start a further phase" instructions held — no commit, no stray
worktree/branch, confirmed via `git diff --stat` before proceeding.

### Resolved
- **Phase 4 — real pages on the Phase 3 scaffold.** New `VehicleDetail.tsx` (mirrors
  majordom-financiar's own reference page's structure, calls vehicle-manager's id-based REST
  endpoints directly): value/projection card, info rows, a new reminders card
  (apk_due/insurance_due/service_interval_km/_months — fields the reference page never showed),
  all five charts including distance (the reference page never wired that one up), fuel/service
  log list with delete, and a log-entry form (fuel vs. other). New `FuelioImport.tsx` — the only
  vehicle-creation path this app has. `VehicleList.tsx` polished (value/odo columns, tap-through,
  import link). `api.ts` extended with 12 typed functions. Edit-vehicle/override-value UI
  deliberately left out of scope (backend endpoints exist, no UI for them yet — later phase).
- **Real bug found in my own post-merge live verification, not caught by the fork's own
  curl-with-token checks.** Nginx's `/vehicles/` proxy prefix (from Phase 3) collided with the
  new `/vehicles/:id` *frontend* route this phase added — a direct request to `/vehicles/4` with
  no `Authorization` header (what an actual page load/hard-refresh/bookmark sends, unlike the
  fork's own curl checks which always attached a token) hit vehicle-manager's JSON API and
  returned a raw 401 instead of the SPA shell. The fork's verification never exercised this path
  because it only tested via authenticated curl calls, which behave like the SPA's own `fetch()`
  calls after the shell has already loaded — not like the initial page-load request. Fixed
  directly (small, mechanical, matches an existing pattern) by adopting the same `/api/` prefix
  majordom-financiar's own `nginx.conf` already uses for this exact reason: `nginx.conf`'s many
  per-route `location` blocks collapsed into one `location /api/` (prefix stripped), mirrored in
  `vite.config.ts`'s dev proxy (would have hit the identical bug in `npm run dev`, not just
  production), and `api.ts`'s `BASE` changed from `''` to `'/api'`.
- **Second, smaller false-positive hit while writing this up**: `check-private-data.sh`'s
  license-plate regex flagged one line in `FuelioImport.tsx` — two adjacent Tailwind padding
  utilities (2-letter prefix each, one with a 2-digit value, spaced apart) formed the
  letters-digits-letters shape the plate regex looks for, already a documented known
  false-positive category in the script's own comments. Diagnosed the exact matched substring
  mechanically before touching anything — any two such adjacent utilities trigger this regardless
  of order, since the pattern only needs [letters][digits][letters] spanning a space, not full
  class-name boundaries. Fixed by converting one value to Tailwind's arbitrary-value syntax
  (equivalent-pixel bracket form instead of the bare scale number) rather than touching the shared
  regex — verified the fixed string no longer matches via a standalone regex test before rerunning
  the full scanner.
- **Live-verified after the routing fix**, full round-trip through the real Nginx proxy:
  `/vehicles/4` with no auth header now serves the SPA shell (200, real `index.html`); `/import`
  (SPA route) → 200; `/api/auth/login` issues a real token; `/api/vehicles/4`,
  `/api/vehicles/4/consumption-chart`, `/api/vehicles/4/log` all return real fixture data with a
  token, 401 without one; `POST`/`DELETE` on `/api/vehicles/4/log` and `/api/log/{id}` both
  confirmed end to end (added a test entry, saw it, deleted it, confirmed gone). `npx tsc
  --noEmit` and `npm run build` clean before and after both fixes; final Docker rebuild +
  container smoke test clean.

### Files modified
`tools/vehicle-manager/frontend/src/{App.tsx,lib/{api.ts,formatDate.ts (new)},
pages/{VehicleList.tsx,VehicleDetail.tsx (new),FuelioImport.tsx (new)},
nginx.conf,vite.config.ts}`, `tools/vehicle-manager/docs/standalone-app-plan.md`.

`pre-commit-review` subagent skipped this time (per `/task-complete`'s own skip condition) —
every touched file was read in full, in this conversation, and every new API function was
live-verified end to end via curl through the real Nginx proxy, including the routing fix.

### Lessons
- **A fork's own "I verified it" claim needs checking against what a *real* page load actually
  sends, not just what a convenient curl command sends.** Attaching a Bearer token on every curl
  call (the fork's whole verification suite) exercises the exact same code path an already-loaded
  SPA's `fetch()` calls take — it never exercises the *first* request a browser makes for a page
  (no token, `Accept: text/html`), which is exactly where this bug lived. Test the unauthenticated
  first-load path explicitly, every time a proxy/routing layer changes, not just the authenticated
  steady-state calls.
- **A same-origin SPA-plus-API layout needs its API under a distinct path prefix (`/api/`) from
  the start, the moment the SPA gets more than one route** — Phase 3 had exactly one frontend
  route (`/`) so the bare `/vehicles`/`/auth`/etc. proxy prefixes never collided with anything;
  the collision was latent from Phase 3, just not yet triggered. majordom-financiar's own
  `nginx.conf` already made this choice for the same reason and I should have applied it in
  Phase 3's own spec instead of discovering it as a Phase 4 bug.
- Second confirmation of last phase's finding: a character-pattern regex will keep colliding with
  legitimate-but-coincidentally-shaped content (this time ordinary Tailwind spacing utilities, not
  a hash) — diagnosing the exact matched substring mechanically (a standalone `grep -P` test)
  before deciding how to fix it is faster and safer than guessing at a rephrase.

### Unresolved
- Phase 5 (retire majordom-financiar's old in-app `VehicleDetail.tsx`) is next, continuing the
  same `/loop` — but per the plan doc, only once Phase 4 is confirmed live-verified, which it now
  is.

## 2026-09-12 (continued) — Phase 5 shipped directly: old in-app vehicle page retired

Small, well-scoped deletion — implemented directly rather than delegated (verification cost of a
delegation round-trip would exceed the gain for a task this size).

### Resolved
- Removed `majordom-financiar/frontend/src/pages/VehicleDetail.tsx` and its
  `/accounts/vehicle/:vehicleId` route/import in `App.tsx` — the standalone vehicle-manager app
  (Phases 3-4) is now the real destination for vehicle detail/charts.
- **Followed through on the two real navigation links into the removed route, not just the route
  itself** — `Accounts.tsx` had two `navigate('/accounts/vehicle/...')` calls (tapping a
  vehicle-linked account row, and after creating a new vehicle) that would have 404'd into the
  catch-all redirect. Repointed the row-tap to the generic `/accounts/:id` account-detail page
  (already exists, works for every other account type) rather than inventing a link out to the
  standalone app — matches the plan's own framing that majordom-financiar keeps only
  cross-cutting/summary pieces now. The post-create navigation was dropped entirely rather than
  redirected somewhere new — staying on Accounts (which already shows the new vehicle via query
  invalidation) is simpler than fabricating a target, and creating a vehicle doesn't always
  produce an AB account id to link to anyway (only when a purchase price is set).
- **Found and removed now-orphaned dead code in the same task** (`duplication-prevention.md`):
  `OverrideValueModal.tsx` had no importer left anywhere once `VehicleDetail.tsx` was gone —
  deleted. `vehicleValueApi.ts` had 8 functions and 6 types (`getVehicle`, `submitValueOverride`,
  `getValueHistory`, `getValueProjection`, and the 4 chart-getters, plus their result types) with
  zero remaining callers anywhere in the frontend, confirmed by grep before removing each one —
  trimmed the file down to the 4 functions (`listVehicles`/`createVehicle`/`patchVehicle`/
  `linkVehicleAccount`) still genuinely in use.
- Live-verified: `npx tsc --noEmit` clean across the whole majordom-financiar frontend (caught
  one now-unused `navigate` binding in `Accounts.tsx`'s outer component, fixed), Docker rebuild
  of `majordom-web` clean, `/`, `/accounts`, and the now-removed `/accounts/vehicle/4` URL all
  still serve 200 (the last one via the SPA's existing catch-all → client-side redirect home, not
  a server error).

### Files modified
`majordom-financiar/frontend/src/{App.tsx,pages/{Accounts.tsx,VehicleDetail.tsx (deleted)},
lib/vehicleValueApi.ts,components/vehicles/OverrideValueModal.tsx (deleted)}`.

Not touched, deliberately: `backend/api/vehicle_value.py`/`vehicle_charts.py`'s REST proxy
routes that only `getVehicle`/the chart-getters/`getValueHistory`/`getValueProjection` called —
this session's Phase 5 scope (per the plan doc) is the frontend page/route/links only. Checked in
a follow-up pass the same day (documentation sweep, see below): confirmed via grep that 9 of
those routes now have no frontend caller anywhere. Not removed here — filed as
[#264](https://github.com/Dorusto/life-os/issues/264) rather than expanding this task's scope.

### Lessons
- "Remove the old page" is rarely just the page file + its route — this session's actual diff
  was 1 deletion (the page) plus 2 edits to fix links that pointed into it, plus 1 more deletion
  (an orphaned modal) plus 6 dead functions/types in a shared API file. Grepping for every
  identifier the file exported/was-imported-as, not just the file's own path, is what surfaces
  the full blast radius — a path-only grep would have missed `OverrideValueModal` and the
  `vehicleValueApi.ts` cleanup entirely.

### Unresolved
- [#264](https://github.com/Dorusto/life-os/issues/264) — whether the 9 now-caller-less REST
  routes in `vehicle_value.py`/`vehicle_charts.py` should be retired. Confirmed dead via grep,
  not yet removed (dead-code cleanup, not part of this plan's scope).
- The vehicle-manager standalone app (Phases 1-5) is now feature-complete per the original plan
  doc and live-verified end to end (standalone) — per Doru's original request, the next checkpoint
  is his own review/testing of the whole thing (standalone app + majordom-financiar integration),
  not another autonomous phase. The investment-tracking app (same process, once vehicle-manager
  is confirmed working for him) is the next major thread after that review.

## 2026-09-12 (continued) — Push, then a documentation-completeness sweep + reusable playbook

Doru asked to push everything, then to verify nothing was left undocumented and to write up the
loop process itself as something reusable for the investment app.

### Resolved
- Pushed all 14 local commits (`d37cdff..7880690`) — Saturday afternoon, inside the weekend
  timing window, no override needed. Left [#261](https://github.com/Dorusto/life-os/issues/261)
  open deliberately (next checkpoint is Doru's own test, not auto-closing on push).
- **Documentation audit found and fixed 3 real gaps, beyond what each phase's own commit had
  already covered:**
  1. `docs/architecture.md`'s live-architecture prose (not the aspirational target tree) never
     mentioned vehicle-manager's new standalone frontend, the `/api/` proxy convention, or the
     service-token auth requirement — added a paragraph.
  2. `README.md`'s vehicle-manager profile description was written for the backend-only era
     (only mentioned `vehicle-manager` + `vehicle-manager-sqlite-web`, missing the new
     `vehicle-manager-web` frontend on `:3010`), and its public "Roadmap" section still listed
     "extract vehicle tracking into its own service" as upcoming (done in an earlier session) and
     "Investment portfolio tracking (Sure)" (stale — Sure/Ghostfolio dropped, the real plan is
     now a separate standalone app, see `decisions.md#portfolio-becomes-separate-service`) —
     rewrote both.
  3. `DEPLOY.md`'s Tailscale `tailscale serve` port-mapping guidance (written for `sqlite-web`'s
     lack of built-in TLS) didn't mention that the new `vehicle-manager-web` frontend is the same
     plain-Nginx-no-TLS shape and needs the identical treatment for remote HTTPS access — added a
     note with the concrete command.
- **Filed [#264](https://github.com/Dorusto/life-os/issues/264)**: confirmed via grep that 9
  routes in `backend/api/vehicle_value.py`/`vehicle_charts.py` have no remaining frontend caller
  after Phase 5's cleanup (the ones Phase 5 itself flagged as "not checked yet") — not acted on
  (dead-code removal, separate scope), but now tracked on GitHub per `priority-tracking.md`'s
  rule instead of sitting only in a session-log note.
- **Wrote `tools/standalone-app-playbook.md`** — the reusable process extracted from building
  vehicle-manager's standalone app end to end this session, explicitly for reuse on the
  investment app (#262): what to decide upfront (API path-prefix convention, dual auth shape),
  the delegation-tier ask-first rule, the fork-dispatch prompt discipline that held vs. didn't
  (state "do not commit" as its own section, top and bottom — not once, buried mid-prompt), how
  to actually verify a fork's "done" report (rebuild yourself, test the *unauthenticated* first-
  load path specifically, not just steady-state authenticated calls), and the recurring
  `check-private-data.sh` false-positive classes. Linked from root `CLAUDE.md`'s "Open fork"
  entry, `tools/vehicle-manager/docs/standalone-app-plan.md`'s closing section, and as a comment
  on #262 itself so it's found without needing to search.

### Files modified
`majordom-financiar/{docs/architecture.md,README.md,DEPLOY.md}`, root `CLAUDE.md`,
`tools/vehicle-manager/docs/standalone-app-plan.md`, `tools/standalone-app-playbook.md` (new).

### Lessons
- A phase's own commit documenting *what it did* isn't the same pass as checking whether *other*
  docs (public README, deploy guide, live-architecture prose) still describe the old state
  accurately — those don't get touched by a feature commit unless someone deliberately greps for
  the feature's old name/shape across the whole doc set, not just the files the feature itself
  touched.
- Writing the reusable-process doc immediately after finishing the concrete instance (same
  session, mistakes still fresh) captured two things that would likely have been flattened into
  vaguer advice a week later: the exact prompt-structure difference between the fork that ignored
  "don't commit" and the one that didn't, and the specific reason authenticated-curl verification
  missed the routing bug (it never exercised the *unauthenticated first-load* path).

### Unresolved
- None — this was itself the closing-out pass for the vehicle-manager thread. Next real thread is
  the investment app (#262), whenever Doru starts it, using `tools/standalone-app-playbook.md`.

## How to resume this overnight loop cold (no conversation memory)

Written 2026-09-13 after Doru asked for an explicit recovery system in case the session hits a
credit/usage limit mid-loop and can only resume once the window reopens, possibly as a fresh
context with none of this conversation's memory. **This is exactly the situation this entry's own
author was in at the start of tonight's loop** — git history and this log were the only way state
got reconstructed, and it worked. Do the same thing on any cold resume:

1. `date` — check real time against the CLAUDE.local.md weekday/weekend windows before any commit
   or `gh issue`/`gh pr` write.
2. `git -C /home/doru/Proiecte-AI/life-os log --oneline -20 --all` + `git worktree list` — see
   what's already committed/merged and what's still mid-flight in a worktree. Never assume a task
   described as "in progress" in an old prompt still is — check.
3. Read this file's own tail (`docs/sessions/2026-W37.md`, or the current week's file) for the most
   recent entries — narrates what a bare `git log` can't (why, what was tried and rejected).
4. `gh issue list` — anything closed/opened since the last log entry that isn't reflected here yet.
5. `git status --short` at the repo root — an interrupted direct-implementation task (not a
   delegated one) can leave real, tested, uncommitted work sitting in the main tree with no
   worktree to signal it (happened once already tonight, see the rate-limit-cooldown fix below).
6. Resume from wherever that leaves off — don't restart a phase that's already merged, don't
   re-investigate an issue already closed with a documented reason.

Standing rules that don't change across a resume: DeepSeek Flash v4.1 only via
`delegate-by-complexity` (no Pro, split bigger tasks smaller instead), local commits/merges only
(no `git push`), no GitHub-write bookkeeping outside the timing window, worktrees nest at
`.worktrees/<slug>/` inside the repo (never as sibling folders — corrected mid-session, see below),
document each merge in this file as it happens, and any decision only Doru can make gets filed as
a GitHub issue rather than guessed at.

## 2026-09-12/13 (overnight autonomous /loop) — investment-manager merged, dead-code sweep, rate-limit hardening

Doru went to sleep and asked for maximum unsupervised progress overnight via ScheduleWakeup's
`/loop` dynamic mode: DeepSeek Flash v4.1 only (via Aider, `delegate-by-complexity`), local
commits/merges only (no push), no GitHub bookkeeping outside the Fri-17:00→Sun-23:00 weekend
window, decisions only Doru can make → filed as an issue, not implemented. This entry backfills
several commits from that loop that landed without an accompanying log entry (rule violation —
being corrected here and going forward this same session).

### Resolved
- **investment-manager (#262) build reviewed and merged** (`1dc23fb`, on top of the opencode/
  DeepSeek build described in the previous entry): audited `auth.py`/`nginx.conf`/both
  `Dockerfile`s/`auth.ts` as byte-identical to the hand-written reference files (not regenerated
  by the model). Found and fixed a real bug before merge — `build_summary()` fed `compute_twr()`
  cash flows in XIRR's sign convention (buy = negative) instead of TWR's own (buy = positive
  contribution), silently doubling returns on any portfolio with more than one buy at different
  times; caught by hand-tracing a flat-price two-buy scenario, fixed, pinned with a new
  integration-level regression test since the existing unit tests only fed already-correctly-
  signed synthetic data. Live-verified: full stack healthy, cold unauthenticated load returns the
  SPA shell (the `/api/` prefix convention held), a real `.xlsx` built from the anonymized sample
  imported correctly and a re-import confirmed idempotent. Followed by dark mode/theme toggle
  (`e67cc18`) and a read-only `InvestmentClient` (`a10be94`, #167/#177 groundwork — degrades to
  `None` rather than raising, since investment-manager is an optional compose profile).
- **Settings page fixed** (`fa68f03`): the "Vehicle Manager" connection link pointed at port 8889
  (the raw sqlite-web debug viewer) instead of 3010 (the real standalone frontend) — pre-existing
  bug, not introduced this session. Added the matching Investment Manager link (port 3020),
  replacing the old static "Portfolio source: Not connected" placeholder.
- **Dead-code sweep, both delegated to Aider/Flash and verified**: `6f64433` removed all 9
  now-caller-less routes in `vehicle_charts.py`/`vehicle_value.py` plus the matching
  `vehicle_client` methods flagged in #264 — backend import and full frontend `tsc --noEmit`
  clean after removal, **closed #264**. `9afc059`/`62e5396` routed `ExpensesStructureWidget`
  (Dashboard.tsx) through `Chart.tsx`'s shared `PieChart` instead of its own CSS conic-gradient
  implementation, resolving the duplication `#256` flagged — `tsc --noEmit` clean, **closed
  #256**.
- **Twelve Data rate-limit hardening, two passes.** First pass (`f0e6158`/`2bf89be`, delegated):
  throttled calls to 6/min and fixed a history-cache coverage check. Second pass, found live
  during this loop and finished directly (not delegated — small, single-file-focused, needed live
  behavioral reasoning about the running dashboard rather than a spec Aider could execute blind):
  a bare 429 from Twelve Data used to leave every *other* ticker in the same dashboard load still
  queuing behind the sliding-window throttle (each one sleeping out its turn before also being
  rejected) — a brief rate-limit hit turned into a 15-20 minute blocking call on the single
  uvicorn worker, starving `/health` too and leaving the dashboard showing €0.00 the whole time.
  Fixed with a cooldown flag (`_rate_limited_until`, 5 min, extended by a real `Retry-After`
  header when present) that short-circuits further calls immediately once a 429 lands, instead of
  re-entering the throttle's sleep queue. A shorter 90s cooldown was tried first and rejected —
  live-observed re-triggering a fresh 429 on the very first retry, spending another quota credit
  and pushing the real reset further out. New regression test
  (`test_429_enters_cooldown_and_skips_remaining_calls_without_waiting`) pins the exact failure
  mode. Same commit also fixed two real bugs found live while testing against a real anonymized
  XTB export: `_find_header_row`/column lookup was case-sensitive against inconsistent header
  casing XTB's own exports use across accounts/export dates, and `load_workbook`'s `read_only`
  mode trusts a worksheet's stale `<dimension>` XML tag for row bounds and can silently truncate
  `iter_rows()` — switched off (files are at most a few thousand rows, full in-memory load is
  cheap). `build_value_series()` also now skips the redundant live-price fallback call when price
  history already covers the target date (halves API calls in the common case, matters more now
  that one 429 costs every remaining ticker a cooldown too). All 27 tests pass (container rebuild
  + restart), committed as `9ffa256`.
- Chart.tsx line-chart dot-vs-crosshair fix (Task A from tonight's kickoff, `88d32c2`) and the
  investment-manager design-system port to vehicle-manager's frontend (`afebdd8` + the
  `task/vm-design-port` squad: `3d678dc`/`086f407`/`0c48e9d`/`b2124f8`/`063f7cd`, merged `f3b67fd`)
  both landed as part of this same loop, ahead of this backfilled entry — see the commits
  themselves, no further detail added here since the crosshair fix's own commit message and the
  design-port squad's commit messages already describe their own scope adequately.
- `tools/vehicle-manager/frontend/package-lock.json` was out of sync with `package.json`'s
  already-committed `@fontsource/*` entries (missing from the lockfile, presumably from an
  `npm install` that ran without the lockfile being committed alongside) — synced (`95115b2`).

### Files modified
See individual commits (`33d2c70`..`95115b2`) — not re-listed here since each commit's own
message already names its files; this entry exists to narrate what a purely mechanical
`git log` wouldn't (why, what was found live, what got rejected and why).

### Lessons
- **Rule 4 ("document as you go") actually failed here for a stretch of ~8 commits** — likely a
  context compaction or a fresh wake-up cycle losing the thread mid-loop, with no session-log
  checkpoint written before it happened. `docs/decisions.md`-style narrative reconstructed here
  after the fact from `git log`/`git show`/`gh issue view` is a workable recovery path when it
  happens, but it's strictly worse than writing it live: no record of *why* something was tried
  and rejected unless the commit message itself happened to capture it (the 90s-cooldown
  rejection did; a differently-worded commit might not have). Going forward this session: write
  the session-log entry immediately after each merge, not batched.
- The uncommitted rate-limit/CSV-import fix was sitting directly in the main working tree (not an
  isolated worktree) with a complete diff + passing tests but no commit — consistent with having
  been implemented directly (per the >2-tightly-coupled-files / needs-live-reasoning rule) and
  interrupted before the commit step, not consistent with a delegated Aider run (those land via a
  merged worktree branch, not bare uncommitted changes in `main`). Good sign the interruption was
  clean (no partial/inconsistent state), but worth remembering that an interrupted loop can leave
  real, tested, uncommitted work sitting in the tree — always check `git status`/`git diff`
  first-thing on any resumed loop iteration before assuming the tree matches the last commit.

### Unresolved (carried forward, none require Doru except where noted)
- **#267** (two stale worktrees, `categories-watchlist`/`category-group-editing`) — needs the
  actual diff-against-main confirmation described in its own body before any deletion; not done
  this pass (lower priority than Task B below, and deleting the wrong thing is a real risk).
- **#268** (OpenBanking sync failing for one account) — **needs Doru**: the fix (if it's a
  consent-expiry, the likely cause) is re-authorizing through Actual Budget's own UI, a live
  production banking flow only Doru can do. Left open, flagged for morning.
- Task B (port the investment-manager design system into majordom-financiar's own frontend, root
  `CLAUDE.md`'s "Open fork" step 3) not started yet as of this entry — next.

## 2026-09-13 (same overnight loop, continued) — #267 investigated/closed, Task B phase 1 shipped, worktree convention fixed mid-session

- **#267 resolved**: `git diff main...task/categories-watchlist` and `...task/category-group-editing`
  both came back completely empty — every commit on both branches already reflected in `main` via
  a different merge path. Only uncommitted Aider artifacts and `node_modules` were left, nothing
  unique. Both worktrees and branches removed, issue closed.
- **Task B phase 1 shipped**: `majordom-financiar/frontend` now has the shared design-token
  foundation ported from investment-manager (`131289e`/`e9114af`/`886c448`) — `src/styles/
  tokens.css` copied verbatim, new colors nested under a `token.*` Tailwind namespace (to avoid
  colliding with existing flat keys like `surface`/`info` that already had different hardcoded
  values), `plex-sans`/`plex-mono` font keys added, `data-theme="dark"` set on `<html>` (matching
  vehicle-manager's own precedent — this ports the shared token *architecture*, not a light-mode
  switch; majordom-financiar stays dark-default). Delegated to Aider/Flash, purely additive by
  design (zero existing color/font keys touched) — `tsc --noEmit` and `npm run build` both clean,
  verified myself after merge (Aider reported "done" but explicitly couldn't run the verification
  commands itself, consistent with `standalone-app-playbook.md`'s "never trust the done report"
  rule). **Found and fixed one real gap in my own task scoping**: I'd excluded all `.tsx`/`.ts`
  files from Aider's edit scope to prevent page-level re-skinning drift, but that also blocked the
  actual `@fontsource/.../*.css` import lines investment-manager keeps in `main.tsx` — without
  them the new `plex-sans`/`plex-mono` Tailwind keys would silently fall back to system fonts.
  Fixed directly (mechanical, additive, matches the existing Syne/DM Mono import pattern already
  in that file) and re-verified the build. No page re-skinning yet — that's the next, separate
  phase(s), per the playbook's "split per page/component group" guidance.
- **Worktree convention changed mid-session, per Doru's explicit request** (sent live, not part of
  the overnight kickoff): delegation worktrees were being created as sibling folders next to
  `life-os/` in `Proiecte-AI/` (`git worktree add ../life-os-<slug>`), which Doru found confusing
  clutter — "tot ce tine de life-os vreau sa ramana in life-os." Fixed: worktrees now nest inside
  the repo at `.worktrees/<slug>/` (gitignored), and `~/.claude/skills/delegate-by-complexity/
  SKILL.md` updated to the new convention so future dispatches (any project, not just this one)
  don't reintroduce sibling folders. The two stale worktrees found via #267 and the just-finished
  Task-B-phase-1 worktree were all removed as part of this same cleanup, confirming no sibling
  worktree folders remain in `Proiecte-AI/`.

### Unresolved
- **#268** — still needs Doru (live banking re-auth), unchanged.
- Task B phases 2+ (component primitives, then per-page/component-group re-skins) — next, now that
  the foundation phase is merged and verified.

## 2026-09-13 (continued) — Task B phase 2 shipped: UI primitive library ported

New `majordom-financiar/frontend/src/components/ui/` (Button, Card, Pill, Delta, MetricTile,
Form, Modal, Feedback, Segmented) plus `src/lib/ui.ts` (`cn`/`seriesColor`/`changeTextClass`),
ported from investment-manager. Deliberately a **new, separate directory** rather than replacing
the existing top-level `Card.tsx`/`PageHeader.tsx` — those are a different, still-in-use
convention (~20 existing components), and retiring them is a real, separate decision for a later
phase, not something to fold into a porting task.

### Resolved
- Delegated to Aider/Flash with an explicit class-renaming table: Phase 1 nested the new tokens
  under a `token.*` Tailwind namespace (to avoid colliding with pre-existing flat keys like
  `surface`/`info`), so every bare `bg-brand`/`text-ink`/etc. from investment-manager's source had
  to become `bg-token-brand`/`text-token-ink`/etc. Verified myself via grep after — zero
  unprefixed token classes slipped through.
- **Aider's circuit-breaker instruction worked as intended**: it stopped before writing `Delta.tsx`
  rather than guessing, because investment-manager's `Delta` takes an arbitrary `digits: number`
  for its own `formatReturn()`, but majordom-financiar's single shared formatter
  (`lib/formatCurrency.ts`) only supports `decimals: 0 | 1` — a real, if small, type mismatch, not
  something to paper over. Decided directly (zero current behavioral impact — nothing calls `Delta`
  yet): narrowed `Delta`'s own `digits` prop to `0 | 1` (default 1) rather than widening the shared
  formatter for a not-yet-used consumer. Wrote `Delta.tsx` myself (one small file, the ambiguity
  was already resolved, a re-dispatch round-trip wasn't worth it).
- Live-verified: grepped all 9 new files for the renaming rule, ran `npm install` + `npx tsc
  --noEmit` + `npm run build` in the worktree myself (Aider explicitly said it couldn't execute
  commands in its environment, consistent with the earlier phase's caveat) — all clean, and the
  built JS bundle size was byte-identical to before (767.67 kB), confirming the new files are
  correctly unreferenced/tree-shaken, zero behavioral change to any existing page.

### Files modified
`majordom-financiar/frontend/src/components/ui/{Button,Card,Pill,Delta,MetricTile,Form,Modal,
Feedback,Segmented}.tsx` (all new), `majordom-financiar/frontend/src/lib/ui.ts` (new).

### Unresolved
- **#268** — still needs Doru, unchanged.
- Task B phase 3+ — re-skinning actual pages/components to use the new `components/ui/` primitives
  and `token-*` classes, split per page/component group per the playbook's guidance. The bigger
  question of whether/when to retire the existing `Card.tsx`/`PageHeader.tsx` convention in favor
  of the new one is a real design decision, not decided here — flag to Doru before acting on it if
  it comes up during page re-skinning, rather than deciding unilaterally which convention "wins."

## 2026-09-13 — Loop stopped by Doru's own choice; backlog sweep found nothing else safely delegable

Asked Doru directly (awake, watching live at this point) whether to push into Task B phase 3
(page re-skinning) tonight or stop here — framed honestly that phase 3 runs straight into the
Card.tsx/PageHeader.tsx retirement question above. **Doru chose to stop the loop**, not push
further tonight.

### Backlog sweep conclusion (Task C), before stopping
Checked `gh issue list`, `--label tier-2`, `--label bug` for anything else safely delegable:
- **#203** (tier-2, planned-transactions toggle) — its own body says "Paused — not needed right
  now" and has a real unresolved investigation gap (how a Schedule's transactions get a category
  assigned isn't confirmed against real Actual Budget data) — not a blind-delegate candidate.
- **#204** (bug/tier-3, Income exclusion) — its own body says "Needs a real design decision, not a
  quick patch."
- **#96** (ops: dev branch to stop deploying straight from main) — changes the production deploy
  pipeline itself; left for Doru rather than treated as a pure technical call.
- **#210** (vehicle value display unclear) — explicitly "note it, don't fix now... needs a
  dedicated look at the actual live page with Doru."
- **#65** (chat hallucinates account creation) — real, well-scoped bug, but deliberately labeled
  `deferred-local-first` (high priority again once local LLM is back in use) — respected the
  label rather than picking it up just because it was convenient tonight.
Conclusion: no further autonomous work was safely available without either overriding an existing
deferral decision or making a call that belongs to Doru. Stopping here, rather than manufacturing
a task, was the correct call per the playbook's own "when to stop the loop" section.

### State handed off for the morning
- All local commits reviewed, tested, and merged to `main` — nothing pending in a worktree or
  uncommitted. `git push` still not done (never authorized this session — Doru's own rule, push is
  a separate explicit step).
- `ScheduleWakeup` loop stopped cleanly (`stop: true`), no further autonomous wakeups pending.
- Open, waiting on Doru specifically: **#268** (OpenBanking re-auth, live banking UI action),
  Task B phase 3 (page re-skin + the Card.tsx/PageHeader.tsx question), and the five backlog items
  listed just above (each needs discussion or is intentionally deferred, not blocked on anything
  technical).
- The "How to resume this overnight loop cold" section near the top of tonight's entries stays
  accurate and reusable for any future loop, not just tonight's.

## 2026-09-13 (loop resumed, live) — Doru commissioned a full cross-app visual unification; shell phase shipped across all three apps

Doru came back online mid-loop, confirmed #268 done himself, and gave a much bigger, explicit
mandate: all three apps (majordom-financiar, investment-manager, vehicle-manager) should look
*identical* — same cards, fonts, menus, charts — not just similar, with Wealthfolio
(https://wealthfolio.app/) as the mobile-nav reference. Root cause of majordom-financiar's
reported Dashboard mess (uneven widget widths, forced two-column) traced to a `sm:` (640px)
two-column grid breakpoint colliding with the app's own `md:max-w-[480px]` phone-frame treatment.
Full plan written and checked into `tools/design-unification-plan.md` (spans three codebases, not
just this one — kept there rather than duplicated here). This entry covers what shipped tonight
against that plan's Phase 1 (shell unification) and Phase 2 (the grid bug).

### Resolved
- **investment-manager (Phase 1a)**: replaced the mobile top-bar+drawer in `AppShell.tsx` with a
  Wealthfolio-style bottom tab bar — new `MobileBottomNav.tsx` (5 tabs: Dashboard/Holdings/
  Transactions/Goals/Settings) + `MoreSheet.tsx` (Income/Rebalancing, via the existing shared
  `Modal`). Desktop rail untouched. Delegated to Aider/Flash; it stopped cleanly with zero
  ambiguity (a well-scoped, well-precedented task — vehicle-manager's `BottomNav.tsx` was given as
  the exact reference shape). Live-verified in browser (resize_window doesn't actually change the
  rendered viewport in this environment — verified instead via `getComputedStyle`/DOM inspection
  at the real viewport width, and by force-displaying the bottom nav to visually confirm the tab
  bar and More sheet both render correctly).
- **vehicle-manager (Phase 1b)**: audited first — it had no shell wrapper at all, `BottomNav`
  rendered individually inside 5 of 7 pages. Added a **desktop-only** `AppShell.tsx` (implemented
  directly, not delegated — a routing-structure decision) wrapping all protected routes in
  `App.tsx` with a `lg:` rail; mobile deliberately untouched (every page keeps its own header/
  BottomNav/full-bleed layout exactly as before) to keep the change low-risk. A full per-page
  teardown so mobile also shares one central shell is noted as a separate, larger follow-up, not
  done here. Live-verified: rail hidden at mobile width, renders correctly when force-displayed.
- **majordom-financiar (Phase 1c + Phase 2, the app Doru actually flagged)**: same rail pattern
  added via a new `AppShell.tsx`, **replacing** the old `md:max-w-[480px] md:mx-auto md:border-x`
  treatment that squeezed the entire app into a narrow centered column at every desktop width
  (the real reason it never looked like a real desktop app) with a `lg:mx-auto lg:max-w-5xl`
  content column instead. `BottomNav.tsx` reskinned onto the new `token-*` colors — and given
  `lg:hidden`, which it never had before (only a `md:` centering, no upper cutoff), a real bug
  caught live: after adding the rail, the bottom nav kept rendering underneath it simultaneously
  until this fix. `Dashboard.tsx`'s own two-column widget grid moved from `sm:` (640px) to `lg:`
  (1024px) to match the new rail breakpoint — **this is the exact fix for Doru's original
  complaint**, live-verified: the Balance-trend/Latest-Transactions columns now read evenly
  balanced instead of cramped/uneven.
- **A real transient tooling issue, not a code bug**: after editing majordom-financiar's frontend,
  `npm run build` failed on a missing `baseline-browser-mapping` module (a `browserslist`
  transitive dependency) — a stale/corrupted `node_modules`, not caused by these changes. An
  accidental `npm install browserslist@latest` (meant as a diagnostic step) bumped `package.json`/
  `package-lock.json` unintentionally — reverted immediately (`git checkout --`) once noticed,
  before it could be committed; a clean `rm -rf node_modules && npm install` was the actual fix.

### Files modified
`tools/investment-manager/frontend/src/components/{AppShell.tsx,MobileBottomNav.tsx (new),
MoreSheet.tsx (new)}`, `tools/vehicle-manager/frontend/src/{App.tsx,components/AppShell.tsx
(new)}`, `majordom-financiar/frontend/src/{App.tsx,components/{AppShell.tsx (new),BottomNav.tsx},
pages/Dashboard.tsx}`, `tools/design-unification-plan.md` (new, plus per-phase checkbox updates).

### Lessons
- `resize_window` (Claude in Chrome) does not reliably change the actual rendered viewport in
  this environment — screenshots keep coming back at a fixed resolution regardless of the
  requested size. `getComputedStyle`/DOM queries at the real (whatever it happens to be) viewport
  width, plus temporarily force-displaying a breakpoint-hidden element via `element.style.
  setProperty('display', ..., 'important')`, is a reliable enough substitute for visually
  confirming both sides of a responsive breakpoint when the tool can't actually emulate one.
- Adding a new nav surface (a rail) doesn't automatically hide an old one (a bottom nav) unless
  the old one is explicitly given the matching breakpoint cutoff — checked this explicitly for
  majordom-financiar after getting it right by copying the pattern for investment-manager/
  vehicle-manager's own *new* bottom-nav components, but almost missed it here since
  majordom-financiar's bottom nav was pre-existing, not newly written alongside its own cutoff.

### Unresolved
- Phase 3 (retire `Card.tsx`/`PageHeader.tsx`, migrate ~20 components/13 pages to the new
  `components/ui/` primitives + `token-*` classes) — next, per the plan doc's own phase order.
  Not started this entry.
- Phase 4 (font cleanup — remove Syne/DM Mono once nothing references them) and Phase 5
  (cross-app visual consistency pass) both wait on Phase 3.
- vehicle-manager's mobile pages still don't share one central shell with the desktop rail (noted
  above) — acceptable for now, flagged as a real follow-up in the plan doc, not silently dropped.

## 2026-09-13 (continued) — Phase 3a: color-token migration, Groups 1-13

Real inventory (not the original guess): only 1 file imports the old `Card` component, but 76
occurrences of the same raw Tailwind card pattern were repeated across 30 files, never through a
shared component. Split into a mechanical color-rename pass (this entry) and a later structural
de-duplication (Phase 3b, not started). Full rename table lives in
`tools/design-unification-plan.md` — not duplicated here.

### Resolved
- **Groups 1-5 (24 files)** delegated to Aider/Flash with the exact rename table given directly in
  each prompt. Every group needed at least one follow-up fix after Claude's own grep-based
  re-verification (never trusted a "done" report as-is, per `standalone-app-playbook.md`):
  - Group 1: missed 2 `accent-accent` checkbox classes (not yet in the table at dispatch time).
  - Group 2 + Group 4 (v2): both stopped cleanly via the circuit breaker on real ambiguity (raw
    Tailwind palette colors like `text-red-400`/`text-yellow-500`, an opacity modifier on a
    `var()`-backed color, a dead `bg-surface-hover` class never defined in the config, hardcoded
    inline hex) — resolved directly (pure technical calls, zero product ambiguity since nothing
    yet renders through most of these files) and re-dispatched with the decisions appended.
  - **Group 4's first re-dispatch produced a genuine model failure**: the run got stuck in a long
    internal monologue hallucinating a nonexistent prior turn ("the user added tsconfig.json to
    the chat") and made zero edits despite claiming to be finishing up. Caught via `git status`/
    `git log` on the worktree (empty) rather than trusting the log's prose — a plain retry with
    the identical prompt succeeded cleanly the second time, confirming it was a one-off model
    hiccup, not a prompt problem.
  - Groups 3, 5a, 5b: each still had real leftovers after their own "done" claim (`ReceiptCard.tsx`
    and `SetupBalancesCard.tsx` in Group 3 were barely touched; `Settings.tsx` in Group 5b likewise;
    a few stray `border-border`/`text-white`/`hover:text-accent`/`accent-accent` elsewhere) — found
    via a grep re-check scoped to each group's own file list (not a global grep, which produces
    false positives from files not yet migrated) and fixed directly.
- **A real cross-file coupling caught before it could dispatch as two independent, possibly-
  inconsistent tasks**: `Chart.tsx`'s `isWarning` check compares `color === '#FF2D2D'` against a
  magic string `BudgetDashboard.tsx`'s `getBudgetColor()` produces for over-100%-budgeted
  categories. These two files were going to separate delegation groups — migrating one side's
  literal without the other would silently break the comparison. Fixed both directly, together,
  before any delegation: both now use `'var(--loss)'`. Two more hardcoded hex values in
  `Chart.tsx` (a pie "Other"-slice fallback, a donut track stroke) fixed the same way.
- **A global sweep after Group 5** (grepping the whole `frontend/src` for old color classes,
  not just the files already suspected) found the true remaining scope was much bigger — Groups
  6-13 (27 more files, including the still-live old `Card.tsx`/`PageHeader.tsx` themselves,
  and the 923-line `Chart.tsx` shared by every chart on every page) dispatched in parallel.
- **A real, recurring nuance found and documented mid-pass**: `text-white`/`bg-white` used
  deliberately for contrast on a colored fill (e.g. white text on a solid brand button) must NOT
  migrate to `text-token-ink` — that token is a themed near-white meant for the app's own
  background, not an arbitrary contrast color. The original table already had a parenthetical
  about this but it wasn't explicit enough for every dispatch to catch — strengthened and called
  out by name in `components/ui/Button.tsx`'s own `primary` variant as the reference example.

### Lessons
- **Never trust a delegated "done" report — re-grep scoped to that task's own file list, every
  time**, even after Aider explicitly lists which files it edited. Three separate groups tonight
  (1, 3, 5b) had real, non-trivial leftovers despite a clean-sounding completion message.
- **A global grep for "is anything still using the old pattern" surfaces work the original,
  narrower inventory missed** — worth running once after the first batch of an inventory-driven
  migration, not just at the very end.
- **Cross-file magic-string/constant coupling is invisible to a per-file class-rename table** —
  worth a deliberate grep for the specific hardcoded values (not just class names) before splitting
  work across independent delegation batches, when a codebase mixes Tailwind classes with raw hex
  in inline styles/JS logic.

### Unresolved
- Groups 6-13 dispatched, not yet reviewed/merged as of this entry (see next entry once they land).
- Phase 3b (structural de-duplication into `components/ui/Card`) and Phase 4 (font cleanup) still
  not started.

## 2026-09-13 (continued) — Phase 3a complete: Groups 6-13 merged, whole-app color migration done

All 8 remaining groups (27 files, spanning small shared components through the 923-line shared
`Chart.tsx`) dispatched in parallel, reviewed, and merged. Full per-group notes in
`tools/design-unification-plan.md` — this entry covers what's not already there.

### Resolved
- Groups 6, 8, 10, 11, 13 came back completely clean on first review. Groups 7, 9, 12 each had
  1-3 real leftovers (a dead `bg-surface-hover`-style class, stray `border-border`/`bg-background`
  the dispatch missed) — found via a grep scoped to each group's own file list, fixed directly,
  each re-verified with `tsc --noEmit` before merging.
- **A genuinely new, correctly-applied nuance**: `ActionCardButtons.tsx`'s `text-white` (white
  text on a solid `bg-token-loss`/`bg-token-brand` button) and similar cases elsewhere (a badge's
  white "M" letter, a white checkmark icon on a green circle) were correctly left un-migrated by
  Aider this round — confirming the `text-white`-on-colored-fill exception documented after Group
  5's sweep actually reached later dispatches when stated explicitly in the prompt.
- **Final whole-`frontend/src` sweep, run twice**: the first pass after Groups 6-13 still found
  leftovers in 4 files from the *original* Group 1 dispatch (before the red/green/yellow-500
  pattern was known, so its table never covered them) plus one `hover:border-interactive` in
  `Dashboard.tsx`. Fixed directly; the second, repeat sweep came back completely clean — confirmed
  there is no more old-flat-color usage anywhere in `majordom-financiar/frontend/src`.
- **Live-verified in the real browser**, full rebuild of `majordom-web`: Dashboard, Accounts,
  Transactions, and Settings all render correctly and consistently — brand-blue actions, amber/
  green semantic colors, IBM Plex typography throughout, no console errors. Confirmed the exact
  Dashboard grid complaint that started this whole thread now reads as two evenly-balanced
  columns instead of cramped/uneven.
- **Tooling note, not a code bug**: Claude-in-Chrome's `zoom` action rendered a solid black frame
  at least twice this session despite the underlying page having real, correctly-colored content
  (confirmed both times via `getComputedStyle` on the actual DOM and via a plain `screenshot`,
  which rendered correctly) — a rendering quirk of that specific tool on this dark theme, not a
  regression. `resize_window` also never actually changed the rendered viewport all session (a
  separate, previously-noted tool limitation). Neither blocked verification, just needed a
  different tool/approach each time.

### Files modified
See `tools/design-unification-plan.md`'s Phase 3a section for the full per-group file lists —
27 files across Groups 6-13, plus the 6 Group-1 files and `Dashboard.tsx` touched again in the
final cleanup round.

### Unresolved
- **Phase 3a is fully complete.** Next: Phase 3b (route the ~30 migrated files' repeated card
  markup through `components/ui/Card` structurally, retiring the old `Card.tsx`/`PageHeader.tsx`
  once nothing imports them) and Phase 4 (remove Syne/DM Mono once nothing references them).
  Neither started this session — a natural stopping/handoff point given how much has already
  landed and verified tonight.

## 2026-09-13 (loop resumed after a wakeup) — Phase 4 (font cleanup) done; commented on a related open issue

Resumed cold per the loop's own checklist (checked real time, git log, worktrees, `gh issue list`
— all clean, no stray state). Found `gh issue list` surfaced **#238**, an existing open issue
whose first bullet ("no max-width container, rows stretch to ~1400px at desktop widths") is
exactly what Phase 1c already fixed — commented with the specific fix and what's still open in
that issue (a mobile double-bottom-bar and missing pull-to-refresh/infinite-scroll, neither
touched tonight), rather than closing an issue that bundles unresolved items too.

### Resolved
- **Phase 4 shipped** — see `tools/design-unification-plan.md`'s own Phase 4 entry for the full
  detail (not duplicated here): `font-display`/`font-mono` confirmed unused app-wide, Syne/DM Mono
  packages and imports removed, `tailwind.config.js`'s base `sans` key changed to IBM Plex Sans
  itself (not just the opt-in `plex-sans` key) so the Tailwind preflight default and any untagged
  text also pick up the new typography. Found and fixed a real bug along the way:
  `components/ui/MetricTile.tsx` (a Phase 3a-era file nothing used yet, never reviewed for its
  font class) had a bare `font-mono` that resolved to the just-retired DM Mono via this app's own
  config override — fixed to `font-plex-mono`. Live-verified via `getComputedStyle` on `<body>`
  and a page's `<h1>`, both reporting `"IBM Plex Sans"`.

### Unresolved
- Phase 3b (structural de-duplication into `components/ui/Card`) — deliberately not started.
  It's real code-quality work but produces zero visible difference (the goal was already achieved
  visually by Phase 3a), and it's the riskiest remaining phase (JSX restructuring, not just class
  renames, across ~30 files) — better picked up as its own deliberate task with fresh attention
  than squeezed into an already very long session.
- Phase 5 (final cross-app visual consistency pass) — next, low-risk, closes out this thread.

## 2026-09-13 (continued) — Phase 5: cross-app visual check, one more real bug found and fixed

### Resolved
- Live-checked all three apps' Dashboard/Home screens, nav shells, and one chart each.
  majordom-financiar and investment-manager rendered cleanly. **vehicle-manager had a real bug**:
  its `BottomNav.tsx` predates tonight's shell work and never had a `lg:hidden` cutoff, so once
  Phase 1b's desktop rail was added, the bottom nav kept rendering underneath the rail
  simultaneously at desktop widths — visually confirmed (both nav surfaces on screen at once) and
  fixed with a one-line `lg:hidden` addition, rebuilt, re-verified clean. This is the same bug
  class already caught for majordom-financiar's own bottom nav during Phase 1c, missed here
  because vehicle-manager's file was pre-existing rather than newly written alongside its own
  breakpoint (investment-manager's brand-new `MobileBottomNav` never had this problem since it
  was built with `lg:hidden` from its first line).

### Lessons
- **When adding a new nav surface to an app that already has one, explicitly check the existing
  surface for a matching breakpoint cutoff — don't assume it has one just because a sibling app's
  newly-written equivalent does.** Two of three apps needed this exact fix tonight (majordom-
  financiar during Phase 1c, vehicle-manager here), both caught only by actually looking at a
  live desktop-width screenshot, not by code review or `tsc`/`build`.

### Unresolved
- **All 5 phases of `tools/design-unification-plan.md` are now complete** except Phase 3b
  (structural de-duplication into `components/ui/Card`), deliberately deferred — real code-
  quality work, zero visible difference, and the riskiest remaining phase. Good stopping point:
  the visual problem Doru originally reported (messy, uneven Dashboard layout) is fixed and
  verified, and all three apps now share one consistent design system end to end.
